# 工作对象与管辖范围规范

```yaml
ldvh_spec:
  spec_key: "work-object-governance-scope"
  spec_id: "07"
  spec_kind: "spec"
  title: "工作对象与管辖范围规范"
  canonical_path: "specs/07-工作对象与管辖范围规范.md"
  parent_spec: ""
  relation: ""
  positioning: "定义管辖项目登记制度（固定位置 `~/dsh-ldvh/` 登记文件）、管辖判定语义、工作对象边界与消费方"
  scope: "适用于 LDVH 管辖项目的登记、判定、工作对象定位；不定义登记文件的具体字段 Schema、Web 交互细节、Helper 传输 envelope、环境接入或 Code 实现"
  basis:
    - "ldvh-root"
    - "specification-model-foundation"
    - "six-dimension-work-model"
  authorized_attachments: []
  dimensions: ["read", "write", "comply", "review", "execute", "reflect"]
```

> **本文为 v5 重建草案，尚未进入当前规则源。文中的"必须/不得"不约束当前来源或实现。**

## 1. 价值判断


## 1. 价值判断

| 价值维度 | 判断要点 |
|---------|----------|
| V1 定位 | 明确 LDVH 管辖项目的边界，为多维度工作提供统一的工作对象锚点 |
| V3 边界识别 | 通过固定位置的登记文件，实现管辖范围的显式声明与快速判定 |
| V6 接续 | 为未来多项目聚合预留单项目单 worktree 的工作对象模型 |
| HV2 授权受控 | 管辖判定结果作为授权前置条件，确保所有 LDVH 操作在受控范围内执行 |


## 2. 规范依据

| 来源规范 | 依据章节 | 关联说明 |
|---------|----------|----------|
| 00-ldvh-root | §5.2 授权包 | 管辖判定作为授权前置，确保操作合法性 |
| 00-ldvh-root | §5.3 授权包 | 登记文件作为授权依据的事实源 |
| 01-规范体系基础 | §6.1 编号登记 | 规范文件自身的编号与路径登记机制 |
| 02-执行维度 | 执行维/审查维 | 管辖判定作为执行前的审查门控 |


## 3. 职责边界

### 3.1 负责

| 职责项 | 说明 |
|--------|------|
| 定义管辖登记制度 | 固定位置、登记文件、判定逻辑 |
| 定义工作对象边界 | Git 根目录 + 事实源路径 + 规范路径 |
| 定义管辖判定语义 | 只读登记文件，判断当前项目是否在列表 |
| 定义消费方行为 | Helper、Git Gate、Skill、Web 如何消费判定结果 |

### 3.2 不负责

| 不负责项 | 说明 |
|----------|------|
| 登记文件具体 Schema | 字段定义留实施时讨论 |
| Web 交互细节 | 归 10-Web 界面规范 |
| Helper 传输 envelope | 归 05-Helper 规范 |
| 环境接入与 Git Hook | 归 08-环境接入规范 |


## 4. 管辖登记制度

### 4.1 固定位置
- **Mac**: `~/dsh-ldvh/`
- **Windows**: `%USERPROFILE%\dsh-ldvh\`
- 该文件夹为 LDVH 管辖登记的唯一固定位置

### 4.2 登记载体
- 文件夹内包含一个登记文件（具体文件名/格式本规范不定死，实施时确定）
- 登记文件记录所有 LDVH 管辖项目的列表

### 4.3 判定方式
- 每次判定时，只读该登记文件
- 判断当前项目是否在登记列表中
- **不做向上遍历**（废弃 v4 向上找 `LDVH-GOVERNED-PROJECTS.yaml` 的做法）

### 4.4 登记入口
1. **AI 入口**: 在项目目录内，用户对 AI 说"帮我把当前项目设为 LDVH 管辖" → Helper 写入登记文件
2. **Web 入口**: 通过 Web 界面（归 10 规范）管理登记列表

### 4.5 前置条件
- **必须**是 Git 项目（存在 `.git` 目录）
- 非 Git 项目不允许登记

### 4.6 自动创建
- 首次登记时，若 `~/dsh-ldvh/` 文件夹不存在，AI **自动创建**该文件夹并写入登记文件

### 4.7 取消登记
- AI 入口：用户一句话要求取消 → Helper 从登记文件中移除
- Web 入口：通过 Web 界面操作移除


## 5. 工作对象

- **定义**: 工作对象 = 当前项目 Git 根目录 + 事实源路径（`ldvh-base/`）+ 规范路径（`specs/`）
- **边界**: 工作对象**不跨越多个项目**（单项目单 worktree）
- **未来方向**: 多项目聚合归未来规范（v4 多项目聚合做法废弃）


## 6. 管辖判定消费方

| 消费方 | 消费方式 |
|--------|----------|
| Helper | 在执行 LDVH 操作前，先调用管辖判定；若未管辖，拒绝操作 |
| Git Gate | 在 Git Hook 中检查管辖状态，决定是否允许提交/推送 |
| Skill | Skill 执行前检查当前项目是否在管辖列表 |
| Web | Web 界面根据管辖判定结果决定显示/隐藏 LDVH 功能 |


## 7. 验证与证据边界

| 验证项 | 证据来源 | 边界说明 |
|--------|----------|----------|
| 登记文件存在性 | 文件系统 | 只验证 `~/dsh-ldvh/` 文件夹及登记文件存在 |
| 登记内容有效性 | 登记文件 | 只验证项目路径格式，不验证项目实际存在性 |
| Git 项目检查 | `.git` 目录 | 必须存在 `.git` 目录才允许登记 |
| 判定结果一致性 | 登记文件 | 多次判定结果应一致 |
| 消费方行为 | 各消费方日志 | 验证消费方是否按判定结果执行 |


## 8. Human Gate / Stop Conditions

| 条件类型 | 具体条件 | 处理方式 |
|----------|----------|----------|
| 非 Git 项目 | 无 `.git` 目录 | 拒绝登记，提示用户 |
| 登记文件损坏 | 文件格式错误 | 拒绝操作，提示修复 |
| 文件夹创建失败 | 权限问题 | 拒绝登记，提示权限 |
| 项目已登记 | 已在列表中 | 提示已登记，不重复写入 |
| 项目未登记 | 不在列表中 | 拒绝 LDVH 操作 |


## 9. 反过度设计红线

| 红线项 | 说明 |
|--------|------|
| 不得向上遍历 | 严禁向上查找登记文件（废弃 v4 做法） |
| 不得跨项目操作 | 工作对象严格限定在单个项目内 |
| 不得硬编码路径 | 登记文件名/格式不得在本规范中硬编码 |
| 不得绕过 Git 检查 | 必须验证 `.git` 目录存在 |


## 10. 起草验证清单

| 验证项 | 验证方式 | 期望结果 |
|--------|----------|----------|
| 规范头部 | 检查 YAML identity 块 | 与模板一致 |
| 章节完整性 | 检查所有章节是否存在 | 10 个章节齐全 |
| 固定位置描述 | 检查 Mac/Windows 路径 | 正确映射 |
| 判定逻辑 | 检查判定描述 | 只读登记文件，不向上遍历 |
| 边界声明 | 检查"不负责"列表 | 与 05/08/10 边界清晰 |
| 格式检查 | `git diff --check` | 无空白错误 |
