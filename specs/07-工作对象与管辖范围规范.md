# 工作对象与管辖范围规范

```yaml
ldvh_spec:
  spec_key: "work-object-governance-scope"
  spec_id: "07"
  spec_kind: "spec"
  title: "工作对象与管辖范围规范"
  canonical_path: "specs/07-工作对象与管辖范围规范.md"
  parent_spec: ""
  relation: ""
  positioning: "定义用户目录管辖登记方向、管辖判定语义、工作对象边界与消费方；精确载体在实现时经 Human Gate 定案"
  scope: "适用于 LDVH 管辖项目的登记、判定、工作对象定位；不定义登记文件的具体字段 Schema、Web 交互细节、Helper 传输 envelope、环境接入或 Code 实现"
  basis:
    - "ldvh-root"
    - "specification-model-foundation"
    - "eight-dimension-work-model"
  authorized_attachments: []
  dimensions: ["read", "write", "comply", "deliberate", "review", "execute", "reflect", "consolidate"]
```

> **本文为 v5 重建草案，尚未进入当前规则源。文中的"必须/不得"不约束当前来源或实现。**

## 1. 价值判断

| 价值维度 | 判断要点 |
|---------|----------|
| V1 定位 | 管辖判定引用的登记文件位置固定可回指；工作对象涉及的 Git 根、事实源路径（`ldvh-base/`，由 06 认定为受管载体位置）、规范路径（由 01 定义）均有明确来源 |
| V3 边界识别 | 非管辖项目时 Web 功能隐藏、操作被拒绝；登记文件不存在或损坏时暂停操作并提示 Human 处理 |
| V6 工作接续 | 登记文件保留项目登记状态，新会话可读取登记列表恢复管辖判定结果 |
| HV2 授权受控 | 管辖判定结果作为 Helper 操作与 Git Gate 的机械门禁输入，未登记项目被拒绝受控操作 |

登记文件存在、判定成功、路径解析或文件写入，均不单独证明以上价值成立；价值由实际使用、回读核对与后续验证共同核对。


## 2. 规范依据

| 来源规范 | 依据章节 | 关联说明 |
|---------|----------|----------|
| 00-ldvh-root | §3.3 规范源与事实源、§3.4 fail-closed、§4 Human 决定权 | 管辖判定作为受管操作前置；登记载体是项目外受控配置，不属于项目事实源 |
| 01-规范模型基础规范 | §6.1 编号登记 | 规范文件自身的编号与路径登记机制 |
| 02-八维工作模型基础规范 | 遵守维/执行维/复核维 | 管辖判定作为执行前的边界落实与复核 |


## 3. 职责边界

### 3.1 负责

| 职责项 | 说明 |
|--------|------|
| 定义管辖登记制度 | 固定位置、登记文件、判定逻辑 |
| 定义工作对象边界 | Git 根目录 + 事实源路径 + 规范路径 |
| 定义管辖判定语义 | 只读登记文件，判断当前项目是否在列表 |
| 定义消费方行为 | Helper、Git Gate、Skill、Web 如何消费判定结果 |

### 3.2 不负责

| 不负责项 | 说明 |
|----------|------|
| 登记文件具体 Schema | 字段定义留实施时讨论 |
| Web 交互细节 | 归 10-Web 呈现与交互规范 |
| Helper 传输 envelope | 归 05-Helper 服务规范 |
| 环境接入与 Git Hook | 归 08-DSH 环境接入与插件发布规范 |


## 4. 管辖登记制度

### 4.1 用户目录登记方向

v5 采用用户目录下的 `dsh-dvh` 登记制度，不再使用 v4 的项目外独立配置和向上遍历。精确路径、目录名在不同系统上的解析、文件名、Schema、权限、迁移与兼容规则在实现该能力时经 Human Gate 定案；定案前不得把候选路径写成已经成立的固定位置。

### 4.2 登记载体
- 文件夹内包含一个登记文件（具体文件名/格式本规范不定死，实施时确定；实施确定后须回写本规范或新增授权附件登记）
- 登记文件记录所有 LDVH 管辖项目的列表

### 4.3 判定方式
- 每次判定时，只读该登记文件
- 判断当前项目是否在登记列表中
- **不做向上遍历**（废弃 v4 向上找 `LDVH-GOVERNED-PROJECTS.yaml` 的做法）

### 4.4 登记入口
1. **AI 入口**: 在项目目录内，用户对 AI 说"帮我把当前项目设为 LDVH 管辖" → Helper 先只读登记文件确认当前项目不在列表中，再写入登记文件（写入应使用原子操作防并发竞态，宿主机制支持情况由 08/09 实现时核验）
2. **Web 入口**: 通过 Web 界面（归 10 规范）管理登记列表

### 4.5 前置条件
- **必须**是 Git 项目（存在 `.git` 目录）
- 非 Git 项目不允许登记

### 4.6 创建与呈现

精确载体定案后，Helper 才可按来源规定受控创建用户目录登记载体；创建前读取当前状态，写入使用原子或版本守卫机制并回读。实际路径由 Web 和 AI 登记响应如实呈现，不得在定案或创建前展示为已成立。

### 4.7 取消登记
- AI 入口：用户一句话要求取消 → Helper 从登记文件中移除
- Web 入口：通过 Web 界面操作移除


## 5. 工作对象

**前提**: 工作对象成立的前提是项目已登记且为 Git 项目（§4.5）。
- **定义**: 工作对象 = 当前项目 Git 根目录 + 事实源路径（`ldvh-base/`）+ 规范路径（`specs/`）
- **登记载体边界**：用户目录登记载体位于项目 Git Working Tree 之外，不属于项目事实源、受管事实载体或 Git Gate 候选；它是项目外受控配置载体，由 07 定义语义、08/09 承接观察、权限和原子写入。
- **边界**: 工作对象**不跨越多个项目**（单项目单 worktree）
- **未来方向**: 多项目聚合归未来规范（v4 多项目聚合做法废弃）


## 6. 管辖判定消费方

| 消费方 | 消费方式（语义接口，不描述具体 UI/传输细节） |
|--------|-----------------------------------------------|
| Helper | 在执行 LDVH 操作前调用管辖判定，未管辖时拒绝操作 |
| Git Gate | 在 Git Hook 事件中检查管辖状态，未管辖时阻断提交 |
| Skill | 执行前检查当前项目是否在管辖列表 |
| Web | 参照管辖判定结果决定功能呈现与隐藏 |


## 7. 验证与证据边界

| 验证项 | 证据来源 | 边界说明 |
|--------|----------|----------|
| 登记文件存在性 | 文件系统 | 只验证 Human 已定案的用户目录登记载体存在 |
| 登记内容有效性 | 登记文件 | 只验证项目路径格式，不验证项目实际存在性；登记文件可定期或在启动时校验项目路径存在性与 `.git` 目录，发现孤立记录时标记为"幽灵登记"并提示 Human |
| Git 项目检查 | `.git` 目录 | 必须存在 `.git` 目录才允许登记 |
| 判定结果一致性 | 登记文件 | 多次判定结果应一致 |
| 消费方行为 | 各消费方日志 | 验证消费方是否按判定结果执行 |


## 8. 拒绝、Human Gate 与 Stop Conditions

- **机械拒绝**：非 Git 项目拒绝登记；项目未登记时拒绝受管操作；已登记时不重复写入。
- **Stop Conditions**：登记载体损坏、不可唯一解析、无法受控创建或读取、权限不足或判定范围不清时，按 00 §7.2 暂停受影响操作；恢复条件必须对应触发原因。
- **Human Gate**：精确载体制度、权限与迁移方案的首次定案，以及是否移除幽灵登记，按 00 §4.2 提请 Human。

本节只具体化管辖域的拒绝、暂停和 Human 决定，不新增根触发类型。


## 9. 反过度设计红线

| 红线项 | 说明 |
|--------|------|
| 不得向上遍历 | 严禁向上查找登记文件（废弃 v4 做法） |
| 不得跨项目操作 | 工作对象严格限定在单个项目内 |
| 不得硬编码路径 | 登记文件名/格式不得在本规范中硬编码 |
| 不得绕过 Git 检查 | 必须验证 `.git` 目录存在 |


## 10. 起草验证清单

| 验证项 | 验证方式 | 期望结果 |
|--------|----------|----------|
| 规范头部 | 检查 YAML identity 块 | 与 01 §5.4 身份块模板一致 |
| 章节完整性 | 检查所有章节是否存在 | 10 个章节齐全 |
| 固定位置描述 | 检查 Mac/Windows 路径 | 正确映射 |
| 判定逻辑 | 检查判定描述 | 只读登记文件，不向上遍历 |
| 边界声明 | 检查"不负责"列表 | 与 05/08/10 边界清晰 |
| 格式检查 | `git diff --check` | 无空白错误 |
