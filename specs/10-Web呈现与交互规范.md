# Web 呈现与交互规范

```yaml
ldvh_spec:
  spec_key: "web-presentation-interaction"
  spec_id: "10"
  spec_kind: "spec"
  title: "Web 呈现与交互规范"
  canonical_path: "specs/10-Web呈现与交互规范.md"
  parent_spec: ""
  relation: ""
  positioning: "定义 LDVH Web 呈现的四层阅读器（字段级直读/卡片网格/语义详情/扩展阅读）与交互入口（结构化问询/计划审查/授权流/验收交还）"
  scope: "适用于 DSH Web GUI 中 LDVH 项目信息的呈现与 Human 交互；不定义 Helper 服务契约、事实模型、环境接入或 Code 实现"
  basis:
    - "ldvh-root"
    - "specification-model-foundation"
    - "six-dimension-work-model"
  authorized_attachments: []
  dimensions: ["read", "write", "comply", "review", "execute", "reflect"]
```

> **本文为 v5 重建草案，尚未进入当前规则源。文中的"必须/不得"不约束当前来源或实现。**

## 1. 价值判断

Web 呈现与交互面向 Human，主要承接 00 §4.2 的 HV 组价值：
- **HV1 决策提请清晰可决**：结构化问询与计划审查使提请对象、依据、选项与风险清晰可决。
- **HV2 授权执行受控可续**：授权流使授权范围、边界与执行状态在 Web 中可追溯，越权被机械拒绝。
- **HV3 入档闭环节点可验**：四层阅读器使受控创建、状态变更、关闭等关键节点可回读核对。
- **HV4 积累效用直观可见**：卡片网格与扩展阅读使积累与实际复用效用直观可见。
- **HV5 项目演进脉络可循**：语义详情与扩展阅读使演进脉络可循，未记录范围如实可见。

页面呈现、工具成功或组件加载不单独证明以上价值成立。

## 2. 规范依据

1. 00 §3.1 呈现面/交互面：定义四层阅读器与交互入口的架构定位；
2. 05 Helper 服务规范：公开操作声明在 Web 中的只读呈现入口；
3. 06 事实源与信息溯源规范：受控提交契约与提交历史在 Web 中的呈现；
4. 07 工作对象与管辖范围规范：管辖登记制度及 Web 侧消费方式；
5. 08 DSH 环境接入规范：插件 manifest 呈现与 `ctx.userQuestions`/`ctx.authorization` 的 Web 承载；
6. 09 Code 实现规范：界面实现承载；本规范只定义呈现语义与交互契约。

## 3. 职责边界

### 3.1 负责

- 定义四层阅读器的呈现语义与切换契约（字段级直读 / 卡片网格 / 语义详情 / 扩展阅读）；
- 定义交互入口的触发条件与 Human 响应契约（结构化问询 / 计划审查 / 授权流 / 验收交还）；
- 定义管辖项目在 Web 中的切换呈现规则（07 §4 登记的管辖项目可切换）；
- 定义 Web 验证项的证据来源与边界，保证可回读、可核对、可回指。

### 3.2 不负责

| 不负责项 | 归属 |
|----------|------|
| Helper 服务契约、公开操作声明、envelope | 05 |
| 事实字段、状态机、CRUD、CAS、content_fingerprint | 03 |
| 行动模板步骤、触发条件、完成门禁 | 04 |
| 事实源定义、受控提交契约、Git Gate | 06 |
| 管辖登记 Schema、判定逻辑 | 07 |
| 插件 manifest、工具注册缝、引导面注入、事件时机 | 08 |
| 代码实现、组件库、构建、测试、CI/CD | 09 |

## 4. 四层阅读器

四层阅读器承载 Human 对 LDVH 项目信息的呈现消费。四层**互不替代、不互锁**：Human 可随时切换至任一或同时展开多层，不要求从低层向高层递进。

### 4.1 字段级直读

- 呈现事实对象的核心字段（名称、状态、指纹、时间戳、来源引用）
- 面向快速定位与核对：Human 可一眼确认某条记录是否存在、状态如何
- 不渲染关联关系或语义解释

### 4.2 卡片网格

- 以卡片形态呈现事实对象摘要（标题、状态标签、单行摘要、关联计数）
- 支持按类型、状态、维度、时间等条件筛选与排序
- 面向浏览与概览：Human 在卡片间扫描，定位感兴趣的对象

### 4.3 语义详情

- 完整呈现单条事实对象的所有字段与关联引用，含来源回指
- 显示该对象的变更历史（摘要形态，不要求完整 diff）
- 面向深度核查：Human 逐条审阅内容、判断适用性与完整性

### 4.4 扩展阅读

- 跨对象关联呈现：按话题、决策链、问题-方案-结果线索串联多条事实
- 呈现事实的积累效用与复用情况（HV4/HV5 的承载面）
- 面向脉络探索：Human 沿关联追踪项目演进路径

## 5. 交互入口

交互入口承载 Human Gate 与交还流程。每个入口定义触发条件与 Human 响应契约，并在 Web 中呈现对应面板。

### 5.1 结构化问询

- **触发条件**：AI 需要 Human 判断、选择或补充信息，问题已组织为带选项、影响与风险的结构化形式
- **Human 响应契约**：呈现代选项与自由输入，响应须含对象、范围与处理结论

### 5.2 计划审查

- **触发条件**：AI 在 Gate 1 / Gate 2 呈交候选计划，等待 Human 批准
- **Human 响应契约**：呈现计划依据、范围、步骤与风险，Human 批准/拒绝/要求修改

### 5.3 授权流

- **触发条件**：AI 需要 Human 授权才能执行或继续某项受控操作
- **Human 响应契约**：呈现授权对象、范围、边界与执行状态，Human 授予/拒绝/限制，越权动作被机械拒绝

### 5.4 验收交还

- **触发条件**：AI 完成当前工作，呈交验收结果
- **Human 响应契约**：呈现交还报告与验证证据，Human 验收/退回/要求补充，交还状态可回读核对

## 6. 管辖登记与项目切换

07 §4 登记的管辖项目在 Web 中可切换呈现，规则如下：
- Web 读取 07 §4.1 固定位置的登记文件，列出所有管辖项目；
- Human 在 Web 中切换当前呈现的管辖项目，切换后四层阅读器与交互入口均指向目标项目的事实源；
- 切换只作用于 Web 呈现层，不改变当前会话工作目录，不触发 Helper 或 Git 操作；
- 管辖判定消费方式沿用 07 §6 消费方表。

## 7. 验证与证据边界

| 验证项 | 证据来源 | 边界说明 |
|--------|----------|----------|
| 四层阅读器呈现 | 各层渲染结果 | 层间切换不丢失已加载数据 |
| 交互入口触发 | 四个交互面板 | 面板出现时机与触发条件一致 |
| 管辖项目切换 | 07 登记文件 | 切换后呈现目标项目的事实源 |
| 授权范围可追溯 | 授权流面板 + 执行状态 | 授权边界与实际执行范围一致 |
| 关键节点可回读 | 语义详情 + 扩展阅读 | 受控创建/状态变更/关闭节点可核对 |
| 价值声明可观察 | 卡片网格 + 扩展阅读 | HV4/HV5 的积累与脉络可追踪 |

## 8. Human Gate / Stop Conditions

| 条件类型 | 具体条件 | 处理方式 |
|----------|----------|----------|
| 登记文件缺失 | 07 登记文件不存在或不可读 | 显示"未管辖"，LDVH 功能隐藏 |
| 项目未管辖 | 当前项目不在登记列表 | 隐藏 LDVH 功能，提示登记入口 |
| 授权范围不清 | 授权对象/范围/边界不明确 | 拒绝授权，提示 Human 补充范围 |
| 交还未通过 | 验收交还条件不满足 | 退回至 AI 补充或修正 |
| 切换失败 | 目标项目登记文件损坏 | 保留当前项目呈现，提示问题 |

## 9. 反过度设计红线

- 四层不得互锁：不要求低层向高层递进；
- 不得预设组件：只定义呈现语义与交互契约，不指定 UI 框架或组件库；
- 不得绕过 07 登记：管辖项目列表以登记文件为唯一来源；
- 不得在 Web 中写改规范或写入事实：均经受控 Git / Helper 流程；
- 不得重复请求授权：已授权范围内自动执行，不重复提请 Human Gate。

## 10. 起草验证清单

| 验证项 | 验证方式 | 期望结果 |
|--------|----------|----------|
| 规范头部 | 检查 YAML identity 块 | 与模板一致，无 `status` 字段 |
| 章节完整性 | 检查所有章节是否存在 | 10 个章节齐全 |
| 价值判断 | 检查 HV1-HV5 各一句 | 面向 Human，不超范围 |
| 依据覆盖 | 检查 00/05/06/07/08/09 | 引用完整 |
| 边界声明 | 检查"不负责"列表 | 与 03/05/06/07/08/09 边界清晰 |
| 格式检查 | `git diff --check` | 无空白错误 |
