# Code 实践与测试规范

```yaml
ldvh_spec:
  spec_key: "code-engineering-practices"
  spec_id: "09"
  spec_kind: "spec"
  title: "Code 实践与测试规范"
  canonical_path: "specs/09-Code实践与测试规范.md"
  parent_spec: ""
  relation: ""
  positioning: "定义 LDVH Code 的工程纪律（目录结构/命名/测试基线/工具链）与契约实现纪律（05/06/07/08 规则的机械实现须与规范一致）"
  scope: "适用于 specs/ 下规范定义的机械规则的 Code 实现与测试；不定义事实类型、行动模板、Web 呈现或环境接入"
  basis:
    - "ldvh-root"
    - "specification-model-foundation"
    - "six-dimension-work-model"
  authorized_attachments: []
  dimensions: ["read", "write", "comply", "review", "execute", "reflect"]
```

> 本文为 v5 重建草案

## 1. 价值判断

- **V4 稳定推进**：工程纪律确保既有行动模板的 Code 实现可被复用，复用依据由语义显式对应（§5）；
- **V5 据实判断**：声明中已证实与未证实范围分层清晰——验证表（§6）的三列对应已证明/待证明边界；
- **V8 持续积累**：单一实现原则（§5）使相似事项的代码实现可复用既有经验与结论，不重复造轮子；
- **HV3 闭环节点可验**：测试基线确保每个机械规则有对应测试文件，可回读核对。

代码文件存在、测试通过、Lint 无错误或编译成功，均不单独证明以上价值成立；价值由实际使用、回读核对与后续验证共同核对。

## 2. 规范依据

1. 00 §3.3 机械守护、§5.5 Stop Conditions；
2. 05 Helper 服务规范：公开操作与共同 envelope 的接口约束；
3. 06 事实源与信息溯源规范：Git Gate 拦截、提交消息验证的接口；
4. 07 工作对象与管辖范围规范：管辖判定读取的接口；
5. 08 DSH 环境接入与插件发布规范：插件激活、工具注册与事件对接的接口。

05/06/07/08 为协作来源，非本文上位依据。

## 3. 职责边界

### 3.1 本文负责
- 定义 LDVH 插件代码结构：目录规范、代码与测试文件命名约定、打包构建工具链与 lint 基准；
- 定义契约实现纪律：统一 Helper、precheck、管辖读取与 Git Gate 的单点实现与调用链，禁止副本逻辑；
- 定义测试基线：规定机械规则覆盖测试的要求，包括正反向用例、边界值及模拟数据；
- 明确代码实现的验证边界、证据形式与 Human Gate/Stop Conditions 的触发定义。

### 3.2 本文不负责
| 不负责项 | 归属 |
|----------|------|
| Helper 服务契约、公开操作声明、envelope | 05 |
| 事实字段、状态机、CRUD、CAS | 03 |
| 行动模板步骤、触发条件、完成门禁 | 04 |
| 事实源定义、受控提交契约、precheck 语义 | 06 |
| 管辖登记 Schema、判定逻辑 | 07 |
| DSH 插件接入、部署、发布与公共门面 | 08 |

## 4. 工程纪律

**目录结构**：规范实现位于 `src/rules/`（按规范 ID 分文件夹），测试文件位于 `tests/` 或与实现同目录，工具配置位于 `tools/`。

**命名约定**：文件与子目录小写 kebab-case，测试以 `.test.ts` 结尾，函数 camelCase，类 PascalCase。

**测试基线**：每个机械规则必须有对应单元或集成测试，覆盖正反向用例、边界条件、异常处理，拒绝零测试实现。

**Lint 与格式化**：ESLint + Prettier 统一配置，提交前必须通过静态分析，`git diff --check` 无空白错误。

**工具链**：TypeScript（Strict 模式），pnpm 管理依赖，Vitest 测试运行器，Vite/Rollup/ESBuild 构建工具。

## 5. 契约实现纪律

**单一实现原则**：05 操作、06 校验、07 管辖判断及 08 插件行为在代码中必须是单点实现，严禁复制副本逻辑。

**Helper 操作契约**：Helper 仅负责共同请求/响应的解析与异常拦截，具体领域逻辑通过调用规范独立实现完成，不得在 Helper 中直接实现领域逻辑。

**Git Gate 共享**：Git commit-msg 与 pre-push 的机械预检逻辑必须与 Helper precheck 共享同一校验器库函数，校验器约定位于 `src/rules/06-facts/validators.ts`（路径为设计约定，实现前须确认存在且通过测试）。

**管辖登记共享**：所有判定当前项目是否被管辖的代码必须调用 07 实现的唯一管辖判定逻辑，判定逻辑约定位于 `src/rules/07-governance/judge.ts`（路径为设计约定，实现前须确认存在且通过测试），拒绝在各处独立解析文件。

## 6. 验证与证据边界

| 验证对象 | 可证明范围 | 不能单独证明 |
|----------|------------|--------------|
| 静态分析 | 文件命名、目录结构与 Lint 完全合规 | 逻辑无缺陷、功能实现完整 |
| 测试基线 | 每一机械规则均关联对应测试文件且运行通过 | 测试用例完备、无边缘幻觉遗漏 |
| 单一实现 | 仅存在唯一的管辖判定与 Gate 校验逻辑实现 | 依赖方调用无绕过、调用语义无漂移 |

## 7. Human Gate / Stop Conditions

**Human Gate**：变更工具链配置、目录结构或命名约定；放宽测试覆盖要求；修改验证边界与证据定义；新增或修改契约实现纪律。

**Stop Conditions**：编译失败或 Lint 检查未过；机械规则缺少对应测试；在代码中发现针对同一规则的多份副本实现；管辖判定或校验器逻辑与规范发生漂移。

恢复条件：修复对应代码使之与规范一致，且通过全部测试。

## 8. 反过度设计红线

1. 不写通用抽象层：不得为非 LDVH 规范需求编写任何跨平台、多宿主的通用 Hook 或中间件适配层；
2. 不写超前实现：严禁实现未在 00–08 规范中被确立为生效（或 active）的机械规则与属性；
3. 不写重复逻辑：严禁因懒于重构而复制已有的校验器、管辖判定或 Helper 封包逻辑。

## 9. 起草验证清单

1. 身份块 spec_key 为 `code-engineering-practices` 且无 lifecycle `status` 字段；
2. 头部声明"本文为 v5 重建草案"；
3. 价值判断覆盖 V4/V5/V8/HV3 且各占一行；
4. 职责边界清晰划分代码工程与外部规范边界；
5. 明确测试基线要求（每条机械规则必须有测试）；
6. 确立单一实现原则，严禁复制副本逻辑；
7. 验证与证据边界采用三列 markdown 表格形式呈现；
8. 停止条件明确包含副本实现与测试缺失；
9. 反过度设计红线包含三条禁止项；
10. `git diff --check` 无空白错误。
