---
fact_type_key: workcase
object_uid: 99957f65-b6f7-4387-b153-457da80a11f9
title: WorkCase card 与详情对齐设计语言
status: open
gist: 修 WorkCase 列表卡与详情阅读面的设计语言：补齐缺失的样式类、按字段真实类型渲染、清除零消费者的导出与拼写错误类。
serves: SG-3
summary: |-
  ### 修 WorkCase 列表卡与详情阅读面对齐 Web 设计语言
  详情阅读面引用了 7 个 index.css 中不存在的类（ldvh-reading-grid/ldvh-reading-main/ldvh-fact-section/ldvh-fact-fields/ldvh-fact-field-key/ldvh-fact-field-value），外壳与正文段全程无样式；

  ### 字段渲染类型错误
  result.satisfied 是布尔却按字符串渲染成裸 true、residual 是数组却按段落渲染；

  ### 零消费者的导出
  WORKCASE_CRITERIA_SURFACE_CLASS 导出零消费者、WorkCaseProgressTrack 零消费者且带 ldh- 拼写错误类。
scope: 做什么：把 WorkCaseReadingLayout 的外壳与正文段改用设计语言既有承载——外壳对齐其余六类阅读布局的 mb-6 flex flex-col gap-5，正文段改用 ReadingNodeSection（可折叠节点，10 §12 长文须有折叠入口），字段键值改用 DetailInlineField；修正 WorkCaseDetailData 的 result.criteria_checks[].satisfied 为 boolean、result.residual 为 string[]，并在详情按 satisfied 呈现可读文本+可区分形态（10 §12.8 不用颜色/图标单独承载状态），residual 按列表呈现；为 WORKCASE_CRITERIA_SURFACE_CLASS 找到消费者或删除，删除零消费者的 WorkCaseProgressTrack；补设计语言契约测试（断言 WC 详情外壳与正文段使用已定义类，并加一条「引用的 ldvh-* 类必须在 index.css 中定义」的机械守卫）；重建 dist；受控提交。明确不做什么：不改 21 号三态直读的字段闭集与分组语义；不改列表卡的筛选面（lifecycle 五档与 SG 筛选均不动）；不改卡片标签序（类型→优先级→SG→修改次数为 2026-09-13 Human 定案）；不改后端 API 与事实源；不动其余六类阅读布局（它们是本次对齐的样板）；不改 10 号规范文本。
plan:
  - done_criteria: 列出 WC 详情引用的全部 ldvh-* 类与 index.css 定义集比对结果、result 字段实际类型（对照 ldvh-base 真实对象）、零消费者导出与拼写错误类，机械可查
    step: 现状核实与缺陷清单固化
  - done_criteria: WorkCaseReadingLayout 不再出现 index.css 未定义类；外壳与六类阅读布局同款；正文段用 ReadingNodeSection、字段用 DetailInlineField；tsc 0 错误
    step: 详情阅读面改用设计语言既有承载
  - done_criteria: satisfied 为 boolean、residual 为 string[]；详情按可读文本呈现 satisfied（非裸 true/false），residual 按列表呈现；随类型修正消除消费方 tsc 错误
    step: 修正 result 类型与呈现
  - done_criteria: WORKCASE_CRITERIA_SURFACE_CLASS 有消费者或已删除；WorkCaseProgressTrack 已删除；仓库内无 ldh- 拼写错误类
    step: 清理零消费者与拼写错误
  - done_criteria: 新增守卫断言 WC 详情引用的 ldvh-* 类均在 index.css 定义、正文段使用 ReadingNodeSection、result 呈现 satisfied 可读文本；测试全绿
    step: 补设计语言契约测试
  - done_criteria: tsc 0 错误、web 测试全绿、eslint 无新增、dist 重建且 index.html 引用新 hash、Git Gate passed
    step: 验证三件套、dist 重建与受控提交
gate_1:
  approved_at: 2026-09-16T03:43:22.788Z
  approver: Human（Web 会话授权）
  authorization_fingerprint: 85a23330cdd0411b7808f9c8243fc3d5f7da813d6d0699dc4ce69934b8b8df0a
  scope_snapshot: 做什么：把 WorkCaseReadingLayout 的外壳与正文段改用设计语言既有承载——外壳对齐其余六类阅读布局的 mb-6 flex flex-col gap-5，正文段改用 ReadingNodeSection（可折叠节点，10 §12 长文须有折叠入口），字段键值改用 DetailInlineField；修正 WorkCaseDetailData 的 result.criteria_checks[].satisfied 为 boolean、result.residual 为 string[]，并在详情按 satisfied 呈现可读文本+可区分形态（10 §12.8 不用颜色/图标单独承载状态），residual 按列表呈现；为 WORKCASE_CRITERIA_SURFACE_CLASS 找到消费者或删除，删除零消费者的 WorkCaseProgressTrack；补设计语言契约测试（断言 WC 详情外壳与正文段使用已定义类，并加一条「引用的 ldvh-* 类必须在 index.css 中定义」的机械守卫）；重建 dist；受控提交。明确不做什么：不改 21 号三态直读的字段闭集与分组语义；不改列表卡的筛选面（lifecycle 五档与 SG 筛选均不动）；不改卡片标签序（类型→优先级→SG→修改次数为 2026-09-13 Human 定案）；不改后端 API 与事实源；不动其余六类阅读布局（它们是本次对齐的样板）；不改 10 号规范文本。
attempt:
  attempt_id: 1
  started_at: 2026-09-16T03:43:22.788Z
  controller: workbuddy/deepseek-v4.1-flash
  heartbeat_at: 2026-09-22T14:50:15.618Z
reviews:
  - at: 2026-09-22T14:50:15.618Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 对象：workcase-99957f65（open，待 Gate 2），六步判据逐条核对。基线：HEAD a56857f；用 git worktree 在提交 ba8d71a（及其父 10e3f79）的真实树上复核，避免后续提交污染。方法：git cat-file/show 验提交与删除；两棵树实跑 eslint、tsx --test 与 tsc；注入缺陷做变异测试；grep 复核计数断言。覆盖：提交真实性、6 类未定义类清零、ProgressTrack 删除、WORKCASE_CRITERIA_SURFACE_CLASS 落地消费、类型与三态呈现、契约测试与变异可失败——均达成。未覆盖：dist 双 hash 不可验证（dist 被 gitignore、非版本化，仓库内无该文件、无提交含该 hash）。发现：①eslint 实为 16 非 13（基线 18，净减 2、零新增的增量结论成立）；②dist 声称无证据可回读；③「ldh- 计数 0」不确（守卫内 2 处为否定模式，判据原意「无拼写错误类」已达成）。保证边界：仅对 ba8d71a 时点做树级复核，未做浏览器视觉验证、未普查其余六类阅读布局。
created_at: 2026-09-16T03:42:15.038Z
change_log:
  - at: 2026-09-16T03:42:15.038Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 WorkCase 工单（draft，21 §14 C1 提案对象模式）
  - at: 2026-09-16T03:43:22.788Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: Gate 1 批准——Human 授权执行 WorkCase card 与详情对齐设计语言，分配 attempt 1 [gate_1 approved by Human（Web 会话授权）; attempt 1 allocated to workbuddy/deepseek-v4.1-flash]
  - at: 2026-09-16T13:43:30.808Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 补记执行进展与结果草稿——六步完成、提交 ba8d71a，另记执行期两处制度缺口的处置去向；提请 Gate 2 [attempt 1 heartbeat refreshed]
  - at: 2026-09-18T02:00:42.427Z
    provider: workbuddy-global
    model: deepseek-v4.1-flash
    summary: 落档独立结果复核概要（reviews）：六步判据经 git worktree 在 as-of 提交真实树上逐条核实全部达成；记载三处证据失真（eslint 实为 16 非 13、dist 双 hash 不可回读、ldh- 计数口径）及其未覆盖范围 [attempt 1 heartbeat refreshed]
  - at: 2026-09-18T11:21:15.391Z
    provider: workbuddy-global
    model: deepseek-v4.1-flash
    summary: 迁移：补齐 gist 要点字段（21 §8，WorkCase d5273e1c） [attempt 1 heartbeat refreshed]
  - at: 2026-09-22T14:50:15.618Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: "格式治理：摘要分块（忠实重排）——把作者自撰的行内「：」提为 ### 块首并插入空行，使摘要可读且符合 21 §8 书写结构（H3 骨架）；作者原文逐字未改，仅新增标记与空行 [attempt 1 heartbeat refreshed]"
---

# WorkCase card 与详情对齐设计语言

## 摘要

### 修 WorkCase 列表卡与详情阅读面对齐 Web 设计语言
详情阅读面引用了 7 个 index.css 中不存在的类（ldvh-reading-grid/ldvh-reading-main/ldvh-fact-section/ldvh-fact-fields/ldvh-fact-field-key/ldvh-fact-field-value），外壳与正文段全程无样式；

### 字段渲染类型错误
result.satisfied 是布尔却按字符串渲染成裸 true、residual 是数组却按段落渲染；

### 零消费者的导出
WORKCASE_CRITERIA_SURFACE_CLASS 导出零消费者、WorkCaseProgressTrack 零消费者且带 ldh- 拼写错误类。

## 授权范围

做什么：把 WorkCaseReadingLayout 的外壳与正文段改用设计语言既有承载——外壳对齐其余六类阅读布局的 mb-6 flex flex-col gap-5，正文段改用 ReadingNodeSection（可折叠节点，10 §12 长文须有折叠入口），字段键值改用 DetailInlineField；修正 WorkCaseDetailData 的 result.criteria_checks[].satisfied 为 boolean、result.residual 为 string[]，并在详情按 satisfied 呈现可读文本+可区分形态（10 §12.8 不用颜色/图标单独承载状态），residual 按列表呈现；为 WORKCASE_CRITERIA_SURFACE_CLASS 找到消费者或删除，删除零消费者的 WorkCaseProgressTrack；补设计语言契约测试（断言 WC 详情外壳与正文段使用已定义类，并加一条「引用的 ldvh-* 类必须在 index.css 中定义」的机械守卫）；重建 dist；受控提交。明确不做什么：不改 21 号三态直读的字段闭集与分组语义；不改列表卡的筛选面（lifecycle 五档与 SG 筛选均不动）；不改卡片标签序（类型→优先级→SG→修改次数为 2026-09-13 Human 定案）；不改后端 API 与事实源；不动其余六类阅读布局（它们是本次对齐的样板）；不改 10 号规范文本。

## 计划

- 现状核实与缺陷清单固化：判据——列出 WC 详情引用的全部 ldvh-* 类与 index.css 定义集比对结果、result 字段实际类型（对照 ldvh-base 真实对象）、零消费者导出与拼写错误类，机械可查
- 详情阅读面改用设计语言既有承载：判据——WorkCaseReadingLayout 不再出现 index.css 未定义类；外壳与六类阅读布局同款；正文段用 ReadingNodeSection、字段用 DetailInlineField；tsc 0 错误
- 修正 result 类型与呈现：判据——satisfied 为 boolean、residual 为 string[]；详情按可读文本呈现 satisfied（非裸 true/false），residual 按列表呈现；随类型修正消除消费方 tsc 错误
- 清理零消费者与拼写错误：判据——WORKCASE_CRITERIA_SURFACE_CLASS 有消费者或已删除；WorkCaseProgressTrack 已删除；仓库内无 ldh- 拼写错误类
- 补设计语言契约测试：判据——新增守卫断言 WC 详情引用的 ldvh-* 类均在 index.css 定义、正文段使用 ReadingNodeSection、result 呈现 satisfied 可读文本；测试全绿
- 验证三件套、dist 重建与受控提交：判据——tsc 0 错误、web 测试全绿、eslint 无新增、dist 重建且 index.html 引用新 hash、Git Gate passed

## 执行

- attempt 1 started at 2026-09-16T03:43:22.788Z (controller: workbuddy/deepseek-v4.1-flash)；Gate 1 授权范围见 gate_1.scope_snapshot。
- 六步全部完成，受控提交 ba8d71a（8 files changed, +565/−131）。
- 2026-09-16 补记：首次执行后对象未记进展（isolation 核对发现），本次补齐。执行期间另发现并处置两处制度缺口：Friction bf73fad4（open 工单无法写入结果节）已修（提交 e4e178f）；Gate 1 提请要素无承载断线已修（提交 40d6ac5）。
- 2026-09-18 独立结果复核落盘（reviews）：用 git worktree 在提交 ba8d71a（及其父 10e3f79）的真实树上复核，六步判据逐条核实全部达成，含实跑测试与注入缺陷的变异验证。复核同时记载三处证据失真，详见 reviews 概要——eslint 实测 16 errors（基线 18），非本节所记的 13/15；dist 双 hash 无版本化证据、不可回读；「ldh- 计数 0」的口径为 src 内为 0（守卫内 2 处为否定模式）。上述三项以 reviews 记载为准。

## 结果

- 判据逐条核对：
  - 现状核实与缺陷清单固化：达成——证据：机械比对确认 6 个类 used>0/defined=0（ldvh-reading-grid 1/0、ldvh-reading-main 1/0、ldvh-fact-section 1/0、ldvh-fact-fields 2/0、ldvh-fact-field-key 5/0、ldvh-fact-field-value 5/0）；对照 ldvh-base/workcases/workcase-435183c2-41eb-42eb-bb43-83a4d7884ed1 的 result 实际值确认 satisfied: true（boolean）、residual: []（数组）；grep 确认 WORKCASE_CRITERIA_SURFACE_CLASS 与 WorkCaseProgressTrack 均为零消费者，后者含 ldh- 拼写错误类（仓库内仅该 2 处）
  - 详情阅读面改用设计语言既有承载：达成——证据：根容器改为 mb-6 flex flex-col gap-5（新增守卫断言其与此前六类阅读布局的 6 处外壳一致）；正文段走 ReadingNodeSection、字段走 DetailInlineField；6 个未定义类仓库内引用数全部归零；tsc 0 错误
  - 修正 result 类型与呈现：达成——证据：api.ts 中 satisfied?: boolean、residual?: string[]；详情按 workcaseCheckSatisfied/Unsatisfied/Unknown 可读文本加三态 chip 呈现，不再把布尔 join 进字符串（守卫断言 doesNotMatch filter(Boolean) 形态）；residual 经 Array.isArray 判定并按 WorkCaseCriteriaList 列表呈现；tsc 0 错误
  - 清理零消费者与拼写错误：达成——证据：WORKCASE_CRITERIA_SURFACE_CLASS 在 WorkCaseReadingLayout 的 PlanNode/ResidualNode 落地消费；WorkCaseProgressTrack.tsx 已删除（守卫断言文件不存在）；仓库内 ldh- 计数 0
  - 补设计语言契约测试：达成——证据：新增 plugin/web/tests/api/workcase-design-language-contract.test.ts 共 6 项断言全绿；其中「引用的 ldvh-* 类必须在 index.css 定义」守卫经变异验证——重新注入原始缺陷后该守卫与外壳断言双双失败（fail 2），恢复后 6/6 通过
  - 验证三件套、dist 重建与受控提交：达成——证据：tsc 0 错误；web 测试 247/247 全绿（原 241 加新增 6）；eslint 13 errors（基线 15，净减 2 且零新增）；dist 重建为 index-DxJPCqF9.js 与 index-BSuEL8QU.css，index.html 引用且文件存在；Git Gate passed，提交 ba8d71a
- 已证实范围：WorkCase 详情阅读面（WorkCaseReadingLayout.tsx）与判据组件（WorkCaseCriteriaList.tsx）已对齐 10 §5.3/§12.7/§12.8 与其余六类阅读布局的既有承载；result 字段类型与真实事实源一致；零消费者死代码与拼写错误类清除；并留下可失败的机械守卫防复发。
- 残留责任：
  - 列表卡（ObjectList 的 WorkCase 分支与 ObjectCardFrame）本次未改动——经核实其使用的是已定义类，本就符合设计语言，不在缺口内；若期望卡片视觉形态另有调整，属新需求。
  - 详情页 SG 筛选（WC-C be30b5f7）、全类型 YAML 呈现（WC-A 63700bd2）、spark 非终态卡关联（WC-B 18fcee2c）三单仍为 draft，未纳入本单。
  - 其余六类阅读布局本次作为样板未改动，是否也存在同类引用未定义类问题未在本单普查——建议以同一守卫扩大扫描面（已参数化为 WORKCASE_SURFACES 列表，扩面成本低）。
  - 执行期发现的两处制度缺口（bf73fad4、Gate 1 提请要素无承载）已另修，不在本单授权范围内完成，其残留责任归各自事项。
  - 证据强度边界（2026-09-18 独立复核记载）：本节「验证三件套」条目所记的 eslint 数字（13/基线 15）经复核实测为 16/基线 18，增量结论（净减 2、零新增）成立但绝对值失真；dist 双 hash 因 dist 被 gitignore、非版本化而不可独立回读；「ldh- 计数 0」的口径为 src 内 0（守卫内 2 处系否定模式）。三项以 `reviews` 记载为准，本节保留原记录不改写（03 §6.1）。

