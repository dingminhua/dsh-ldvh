---
title: 工具参数校验层不生效
status: open
question: LDVH 工具的参数面校验应由谁承担——接入 defineTool 使 DSH 校验真正生效，还是在 LDVH 侧建立等效的显式校验，抑或确认「writer 闭集即充分」并据此修正规范表述？
scope_boundary: 当「LDVH 参数校验的责任归属」有定论并落地（无论接入 defineTool、自建等效校验、还是显式裁定 writer 闭集为唯一防线并据此修订相关规范表述）时停止。不含 writer 内部各字段的语义校验（属 20–27 号规范）、不含 b9601356 记的两份字段清单漂移（该问题已有守卫覆盖）、不含 Git Gate 对事实对象内容的校验（属 64754323 与 2074408b）。
intent: 保留理由——这不是理论风险而是已实测的机制缺口：LDVH 工具的参数校验层实际不存在，而现有规范表述（b9601356 的「在进入 writer 前即被 DSH 参数校验拦下」）建立在一个并不存在的环节上。若不裁定责任归属，后续任何依赖「工具面会拦住非法参数」的设计都会落空。后续方向——先裁定归属（接入 defineTool / 自建等效校验 / 显式认定 writer 闭集为唯一防线），再据裁定结果决定是否修订 b9601356 与相关规范的表述。
summary: LDVH 全部工具描述符由 toolDescriptorFor 返回裸对象，从不经过 DSH 的 defineTool；而 DSH 的参数校验只存在于 defineTool 闭包内（ToolRuntime.register() 原样存、派发时不再校验）。实测：schema 明令禁止的属性仍能抵达 handler。因此 LDVH 工具的参数面约束不来自 DSH 运行时，writer 的闭集校验是实际生效的最后一道防线。此结论修正了 b9601356 的成因描述——它假定存在一个会 throw 的运行时 DSH 参数校验环节。
serves: SG-3
priority: P1
object_uid: 5b0b380e-c56f-40f8-bdb7-9645d8a54730
fact_type_key: spark
created_at: 2026-09-13T20:48:59.027Z
change_log:
  - at: 2026-09-13T20:48:59.027Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 记录 LDVH 裸描述符绕过 DSH 参数校验的实测结论，及由此引出的参数校验责任归属问题
---

# 工具参数校验层不生效

## 当前理解

LDVH 全部工具描述符由 toolDescriptorFor 返回裸对象，从不经过 DSH 的 defineTool；而 DSH 的参数校验只存在于 defineTool 闭包内（ToolRuntime.register() 原样存、派发时不再校验）。实测：schema 明令禁止的属性仍能抵达 handler。因此 LDVH 工具的参数面约束不来自 DSH 运行时，writer 的闭集校验是实际生效的最后一道防线。此结论修正了 b9601356 的成因描述——它假定存在一个会 throw 的运行时 DSH 参数校验环节。

（以上首段即 frontmatter `summary` 的逐字承载。）

证据链（读自 `plugin/node_modules/@deepseek-ai/dsh-tools/lib/index.js`）：

1. `defineTool` 构造 `execute` 的方式是「先校验再执行」：`const violations = validate(args); if (violations.length > 0) throw new ToolArgsError(violations);` 之后才 `return userExecute(args, exec)`（约 863 行）。参数校验在闭包内，不是运行时的公共环节。
2. `ToolRuntime.register(definition)` 只做 `output.schema`/`timeoutMs`/保留名的形状断言，然后 `layer.tools.insert(name, definition)` 原样存入，不包装、不注入校验。
3. `ToolRuntime` 派发时是 `const returned = await tool.execute(exec.arguments, exec)`（约 3192 行），不再校验。

实测（本仓库内探针，非推断）：以 `toolDescriptorFor("spark-write-object", …)` 构造描述符，传入一个 schema 明令禁止的属性（`frontmatter_draft.bogus`），`d.execute(...)` 的返回值显示 handler 照常运行——参数校验没有拦截它。

后果：模型侧之所以不会乱传，是因为 DSH 从描述符的 `parameters` 生成给模型的参数声明，模型按声明构造调用；一旦调用携带未声明字段，抵达的是 writer 而非校验层。writer 的闭集校验（如 `spark-writer.js` 的 `VALID_FM_KEYS`）因此不是「第二道防线」，而是实际生效的最后一道。

两项未被削减的能力：writer 闭集校验有效；`schema-writer-agreement` 守卫（2026-09-14 加固后）以 DSH 真实使用的 `validateJsonSchemaValue` 直测每个可选字段，能捕获字段漂移。

## 调查问题

LDVH 工具的参数面校验应由谁承担——接入 defineTool 使 DSH 校验真正生效，还是在 LDVH 侧建立等效的显式校验，抑或确认「writer 闭集即充分」并据此修正规范表述？

## 调查边界

当「LDVH 参数校验的责任归属」有定论并落地（无论接入 defineTool、自建等效校验、还是显式裁定 writer 闭集为唯一防线并据此修订相关规范表述）时停止。不含 writer 内部各字段的语义校验（属 20–27 号规范）、不含 b9601356 记的两份字段清单漂移（该问题已有守卫覆盖）、不含 Git Gate 对事实对象内容的校验（属 64754323 与 2074408b）。
