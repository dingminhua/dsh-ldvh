---
title: Norm 承载层补建
status: open
question: Norm 事实类型的 writer、受控入口与唯一性三层防线应如何建成？
scope_boundary: Norm 可受控创建、可受控更新、可退役（retired），且 direction_key 唯一性三层防线全部具备时停止。不含 WorkCase 承载层（另有独立对象承载），不含 27 号自身的规范修订与方向清单增减（属 00 §4.2 Human Gate）。
intent: 保留理由——27 号规范已定稿，但 Norm 零实现：无 writer、无 tools 受控入口、无 Git Gate 断言、无消费端 fail-closed，方向规范在管辖项目中无法出生，SG-2 的一半卡点落在这里。与 WorkCase 拆开的理由——两者卡点性质不同：Norm 是纯实现工作量（规范自洽、无外部依赖），WorkCase 卡在跨规范接缝（attempt 无宿主实现，见 21 §10.4 第 4 项）。合并承载会掩盖这一区别。后续方向——按 writer → tools 受控入口 → 唯一性三层防线（写入前拒绝 / Git Gate 拦截 / 消费端 fail-closed）次序实现，完成后以真实出生→消费→关闭一轮验证。
summary: 27 号规范已定稿；`FACT_DIRECTORIES`（`plugin/lib/governed-projects.js:17`）已含 `norms`；Web 侧字段契约已接入（`factFieldContract.ts` 的 `FACT_TYPES`/`FACT_TYPE_DIRS`/`FACT_TERMINAL_STATUSES`）。真实缺口为整套承载层：无 `plugin/lib/norm-writer.js`、无 `ldvh_norm_*` 受控入口、Git Gate 未覆盖 `norms/` 的 direction_key 排他断言、消费端无 `direction_collision` fail-closed。`ldvh-base/norms/` 目录于 2026-09-13 由 Human 裁定补建（此前并不存在；早前「已建、空」的转述失实，参见 6f5adc82 的二次更正）。27 §11 定义唯一性三层设计契约，并规定「三层全部具备之前不得声称唯一性已获机械保障」——当前三层一层都无。27 §11 三层是否具备属实现现状，按 01 §7.2 由读取时的观察取得。
serves: SG-1
priority: P0
object_uid: 4cb237fe-7f13-41cc-9f85-530ef329653c
fact_type_key: spark
created_at: 2026-09-13T18:36:56.261Z
change_log:
  - at: 2026-09-13T18:36:56.261Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 由 c94ea382「Norm 与 WorkCase 承载层补建」按 20 §9.4 情形二拆出：Norm 侧为纯实现工作量，独立承接
---

# Norm 承载层补建

## 当前理解

27 号规范已定稿；`FACT_DIRECTORIES`（`plugin/lib/governed-projects.js:17`）已含 `norms`；Web 侧字段契约已接入（`factFieldContract.ts` 的 `FACT_TYPES`/`FACT_TYPE_DIRS`/`FACT_TERMINAL_STATUSES`）。真实缺口为整套承载层：无 `plugin/lib/norm-writer.js`、无 `ldvh_norm_*` 受控入口、Git Gate 未覆盖 `norms/` 的 direction_key 排他断言、消费端无 `direction_collision` fail-closed。`ldvh-base/norms/` 目录于 2026-09-13 由 Human 裁定补建（此前并不存在；早前「已建、空」的转述失实，参见 6f5adc82 的二次更正）。27 §11 定义唯一性三层设计契约，并规定「三层全部具备之前不得声称唯一性已获机械保障」——当前三层一层都无。27 §11 三层是否具备属实现现状，按 01 §7.2 由读取时的观察取得。

逐项现状（已机械核实）：

| 维度 | 现状 |
|---|---|
| 规范 | ✅ 已定稿（27 号） |
| `FACT_DIRECTORIES` | ✅ 已含 `norms`（`governed-projects.js:17`） |
| Web 字段契约 | ✅ 已接入（`factFieldContract.ts`、`localFactReader.ts`） |
| 目录 `ldvh-base/norms/` | ✅ 2026-09-13 由 Human 裁定补建（此前不存在） |
| writer | ❌ 无 `norm-writer.js` |
| tools 受控入口 | ❌ 无 `ldvh_norm_*` |
| 唯一性第一层（写入前拒绝） | ❌ 无 `ERR_DIRECTION_ALREADY_EXISTS` |
| 唯一性第二层（Git Gate 拦截） | ❌ `git-gate-runner.js` 不扫 `norms/` |
| 唯一性第三层（消费端 fail-closed） | ❌ 无 `direction_collision` |

**唯一性三层防线是本对象的核心约束**：27 §11 明确「三层全部具备之前，同一 `direction_key` 在 active 集合中至多一个只有规范要求、不获机械保障」，并要求「三层实现前不得声称唯一性已被机械保障」（回指 00 §7.1）。因此本对象完成与否的判据不是「writer 能跑」，而是三层齐备。

`direction_key` 的校验须复用既有权威而非另建一套：27 §13.1 规定其正则的唯一权威来源是 `01.Att.02` §2 的职责标识符闭集，全串匹配语义等价于 `plugin/lib/spec-registry.js` 的 `ID_PATTERN`。

**本对象由 `c94ea382` 按 20 §9.4 情形二拆出**。原对象把 Norm 与 WorkCase 两支合并为「承载层补建」，理由是「缺口性质完全相同」；但实际两者的剩余风险不同——Norm 侧规范自洽、无外部依赖，是纯实现工作量；WorkCase 侧卡在 `attempt` 令牌无宿主实现这一跨规范接缝（21 §10.4 第 4 项声明由 08/09 承接，而 08/09 现无对应内容）。拆开后两条各自独立处置。

## 调查问题

Norm 事实类型的 writer、受控入口与唯一性三层防线应如何建成？

**当前状态：完全未答**。待决的具体问题：

1. writer 的模块边界——是完整复刻 `adr-writer.js`/`spark-writer.js` 的形态，还是抽取两支共用管线？
2. `title ≤ 40 字` 的判定单位——27 §8 只说「≤ 40 字」；`disposition` 的先例（§9.1）明确为 UTF-16 码元计数、实现按 `.length`，`title` 是否照此？
3. 退役 `retirement_reason = superseded` 时「替代 Norm 已存在且可解析」如何校验——Norm 明确不使用 `relations` 字段（27 §9），无 `superseded-by` 关系可写。
4. 唯一性第二层（Git Gate 拦截）是否纳入本轮——它会改动保护全仓库提交的机制，代价高于另外两层。
5. 创建提案中的查重结论（27 §13.1）如何留痕——frontmatter 是闭集，无法新增字段承载。

## 调查边界

Norm 可受控创建、可受控更新、可退役（retired），且 direction_key 唯一性三层防线全部具备时停止。不含 WorkCase 承载层（另有独立对象承载），不含 27 号自身的规范修订与方向清单增减（属 00 §4.2 Human Gate）。

边界说明：本停止条件针对的是**承载层建成**，不是「规范质量已达标」。规则内容质量由 AI 语义审核与 Human Gate 承担（27 §14「规则内容质量」行），不在本对象范围内。另需注意 27 §14 的声明限制——三层全部具备之前，不得声称唯一性已获机械拦截；该节在依赖闭合后应相应调整或删除，但规范修订本身不属本对象。
