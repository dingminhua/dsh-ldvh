---
title: 插件绑定计划与落地的差距
status: open
question: LDVH 插件需绑定的 DSH 原生能力面如何确定与落地？
scope_boundary: 绑定能力面清单确定、各能力已落地或已明确不采用、与当前实现一致时停止。
intent: 保留理由——绑定计划自述仍为「候选，待独立审核与 Human 确认」，但插件已实际运行，计划与实现之间已出现未经核对的差距；不保留则差距不可召回。
summary: |-
  合并 B-01+B-02，并并入 `investigation-report-dsh-subagent-default-model.md`。`dsh-plugin-binding-plan.md`（471 行）自述「候选，待独立审核与 Human 确认」，定四类价值交付（AI 工作引导／Helper 确定性执行／Web 信息呈现／Git 机械守护）与需绑定的 DSH 原生能力面。`ldvh-plugin-implementation-plan.md`（167 行）自述「方案已定，待按序实施」，已定：DSH 注册插件 ID `dsh-ldvh`；**方案 A 复用宿主 webServer**（注册 `/ldvh/api` 后端 + `/ldvh` 前端 SPA 两 prefix 路由，零端口管理，随插件启停，卸载经 `ctx.effect` 清理）；主界面呈现为会话区视图 tab（`conversation.view`，list/session 作用域，id: `ldvh`）；设置面板参考 dsh-subagent-default-model（`@deepseek-ai/schemastery` Schema + `installSettingsSection` + `ctx.slots.inject("settings.plugin.item")`）；发布工程规范对齐（plugin/ 子目录、package.json、cordis.patch.yml、node --test、双语 README、CHANGELOG、MIT、图标）；Web 代码先完整迁入 v4 `web/` 建立基线再按 v5 调整。**现状核对（本轮机械核验）**：`plugin/` 已具备完整结构（CHANGELOG.md／LICENSE／README.en.md／README.md／cordis.patch.yml／eslint.config.js／icons／lib／package.json／test／web）。

  **并入的调研报告**：`docs/investigation-report-dsh-subagent-default-model.md`（768 行，2026-09-01）为工程规范调研，含 §1 目录结构图、§2 package.json 逐项解读、§3 配置 Schema + 设置面板实现模式、§4 Host/Client 分工、§5 cordis.patch.yml、§6 测试模式、§7 发布流程、§8 插件管理、§9 可复用规范 + 待调研缺口、§10 开放问题（17 项 Q1–Q17）。**§10 的多数已被运行中的实现回答**（本轮机械核验：`plugin/lib/index.js` 已使用 `webServer` 注入与路由、`installSettingsSection` + `settingsNamespace` + `@deepseek-ai/schemastery`，`plugin/lib/bin.js` 已有 `uninstallHook` 等子命令）——即 Q5/Q6/Q14/Q15 等已由实现落地。**未核实**：计划各项与已实现部分的具体差距未逐项比对；§10 剩余未答项（Q1–Q4 的 Tab/Sidebar 注册冲突、Q7 热重载、Q8–Q10 Client/Host 边界、Q11–Q13 多 profile、Q16–Q17 发布）是否仍开放未逐项核。
serves: SG-3
object_uid: af3f5fd7-dff3-4fd5-9599-d68563140b05
fact_type_key: spark
created_at: 2026-09-12T21:52:00.952Z
change_log:
  - at: 2026-09-12T21:52:00.952Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 Spark 对象
  - at: 2026-09-12T21:56:11.986Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 并入 investigation-report-dsh-subagent-default-model.md：其 §10 十七项开放问题中多项已由当前实现回答（已核 webServer/installSettingsSection/schemastery 在用），补记该报告与剩余未答项归属本 Spark
---

# 插件绑定计划与落地的差距

## 当前理解

合并 B-01+B-02，并并入 `investigation-report-dsh-subagent-default-model.md`。`dsh-plugin-binding-plan.md`（471 行）自述「候选，待独立审核与 Human 确认」，定四类价值交付（AI 工作引导／Helper 确定性执行／Web 信息呈现／Git 机械守护）与需绑定的 DSH 原生能力面。`ldvh-plugin-implementation-plan.md`（167 行）自述「方案已定，待按序实施」，已定：DSH 注册插件 ID `dsh-ldvh`；**方案 A 复用宿主 webServer**（注册 `/ldvh/api` 后端 + `/ldvh` 前端 SPA 两 prefix 路由，零端口管理，随插件启停，卸载经 `ctx.effect` 清理）；主界面呈现为会话区视图 tab（`conversation.view`，list/session 作用域，id: `ldvh`）；设置面板参考 dsh-subagent-default-model（`@deepseek-ai/schemastery` Schema + `installSettingsSection` + `ctx.slots.inject("settings.plugin.item")`）；发布工程规范对齐（plugin/ 子目录、package.json、cordis.patch.yml、node --test、双语 README、CHANGELOG、MIT、图标）；Web 代码先完整迁入 v4 `web/` 建立基线再按 v5 调整。**现状核对（本轮机械核验）**：`plugin/` 已具备完整结构（CHANGELOG.md／LICENSE／README.en.md／README.md／cordis.patch.yml／eslint.config.js／icons／lib／package.json／test／web）。

**并入的调研报告**：`docs/investigation-report-dsh-subagent-default-model.md`（768 行，2026-09-01）为工程规范调研，含 §1 目录结构图、§2 package.json 逐项解读、§3 配置 Schema + 设置面板实现模式、§4 Host/Client 分工、§5 cordis.patch.yml、§6 测试模式、§7 发布流程、§8 插件管理、§9 可复用规范 + 待调研缺口、§10 开放问题（17 项 Q1–Q17）。**§10 的多数已被运行中的实现回答**（本轮机械核验：`plugin/lib/index.js` 已使用 `webServer` 注入与路由、`installSettingsSection` + `settingsNamespace` + `@deepseek-ai/schemastery`，`plugin/lib/bin.js` 已有 `uninstallHook` 等子命令）——即 Q5/Q6/Q14/Q15 等已由实现落地。**未核实**：计划各项与已实现部分的具体差距未逐项比对；§10 剩余未答项（Q1–Q4 的 Tab/Sidebar 注册冲突、Q7 热重载、Q8–Q10 Client/Host 边界、Q11–Q13 多 profile、Q16–Q17 发布）是否仍开放未逐项核。

（来源：docs/dsh-plugin-binding-plan.md、docs/ldvh-plugin-implementation-plan.md、docs/investigation-report-dsh-subagent-default-model.md；普查条目 B-01+B-02 + 第三批并入）

## 调查问题

LDVH 插件需绑定的 DSH 原生能力面如何确定与落地？

## 调查边界

绑定能力面清单确定、各能力已落地或已明确不采用、与当前实现一致时停止。
