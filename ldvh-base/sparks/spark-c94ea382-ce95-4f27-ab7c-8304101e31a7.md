---
title: Norm 与 WorkCase 承载层补建
status: open
question: 事实类型承载层（writer、受控入口、Git Gate 断言）如何为 Norm 与 WorkCase 两支补建？
scope_boundary: Norm 与 WorkCase 均可受控创建、可召回、Git Gate 覆盖，且两支共用管线与既有五类的对齐方式已确定时停止。不含 `dimensions` 七元组扩维（另有 `bdb9623d` 承载）与各类型自身的规范修订（已定稿）。
intent: 保留理由——SG-2（七类型完成一轮出生→消费→关闭）当前的真实卡点不在规范侧（27/21 均已定稿），而在承载层：两支类型无 writer、无受控入口、无 Git Gate 断言，对象根本无法出生，SG-2 因此推不动。合并承载的理由是两支缺口性质完全相同（均 FACT_DIRECTORIES 已登记、目录已建、仅缺管线与校验），散成两条会掩盖这一唯一卡点。后续方向——先定两支共用管线与既有五类 writer 的对齐方式，再分别实现，最后以真实出生→消费→关闭一轮验证。
summary: "`plugin/lib/` 六个类型 writer 齐全，但 27 号 Norm 与 21 号 WorkCase 只有规范、无承载层：两支均缺 writer、缺 tools 受控入口、缺 Git Gate 断言，而 FACT_DIRECTORIES（`governed-projects.js:17`）与目录（`ldvh-base/norms/`、`ldvh-base/workcases/`）均已就位。即两支类型的对象在当前实现下无法出生，SG-2 的真实卡点从规范侧转为承载层。本对象由 2e79939a（21 号立项，question 已答）与 6f5adc82（Norm 实现侧空缺）合并而来，两支缺口性质相同故合并承载。"
serves: SG-1
priority: P0
object_uid: c94ea382-ce95-4f27-ab7c-8304101e31a7
fact_type_key: spark
created_at: 2026-09-13T08:54:24.618Z
change_log:
  - at: 2026-09-13T08:54:24.618Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 Spark 对象
  - at: 2026-09-13T08:55:04.848Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 补记合并来源流水：本对象由 2e79939a（split-into）与 6f5adc82（merged-into）汇聚（20 §9.4 步骤三）
  - at: 2026-09-13T15:08:54.063Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 回填 priority=P0（20 §8 新增字段，Human 裁定 A；AI 出初值，Human 可随时调整）
---

# Norm 与 WorkCase 承载层补建

## 当前理解

`plugin/lib/` 六个类型 writer 齐全，但 27 号 Norm 与 21 号 WorkCase 只有规范、无承载层：两支均缺 writer、缺 tools 受控入口、缺 Git Gate 断言，而 FACT_DIRECTORIES（`governed-projects.js:17`）与目录（`ldvh-base/norms/`、`ldvh-base/workcases/`）均已就位。即两支类型的对象在当前实现下无法出生，SG-2 的真实卡点从规范侧转为承载层。本对象由 2e79939a（21 号立项，question 已答）与 6f5adc82（Norm 实现侧空缺）合并而来，两支缺口性质相同故合并承载。

逐项现状（已机械核实）：

| 维度 | Norm（27） | WorkCase（21） |
|---|---|---|
| 规范 | 已定稿（27） | 已定稿（354 行，`d4ee39e`） |
| 目录 | `ldvh-base/norms/` 已建、空 | `ldvh-base/workcases/` 已建、空 |
| `FACT_DIRECTORIES` | **已含** `norms` | **已含** `workcases` |
| writer | ❌ 无 `norm-writer.js` | ❌ 无 `workcase-writer.js` |
| tools/受控入口 | ❌ 无 | ❌ 无 |
| Git Gate 断言 | ❌ 未覆盖 | ❌ 未覆盖 |

**合并来源**（依 `20 §9.4` 情形记录）：本对象由两条原对象汇聚——`2e79939a`（21 号 WorkCase 规范立项，经 `split-into` 拆出遗留的承载层缺口）与 `6f5adc82`（Norm 类型实现侧空缺，经 `merged-into` 并入）。两条的规范侧工作均已完成，遗留的是同一承载层问题，故收敛为单一承载，使 SG-2 的真实卡点显现为一个待办而非两条。

## 调查问题

事实类型承载层（writer、受控入口、Git Gate 断言）如何为 Norm 与 WorkCase 两支补建？

## 调查边界

Norm 与 WorkCase 均可受控创建、可召回、Git Gate 覆盖，且两支共用管线与既有五类的对齐方式已确定时停止。不含 `dimensions` 七元组扩维（另有 `bdb9623d` 承载）与各类型自身的规范修订（已定稿）。
