---
title: pre-step 预热的规范授权缺口
status: open
question: pre-step 自动预热的规范授权缺口应如何闭合？
scope_boundary: 预热注入的授权条款写入相应规范（含预算、触发时机、去重与失败语义）、或 Human 判定不授权自动注入时停止。
intent: 保留理由——这是一个**明确的规范授权缺口**（Human Gate 事项）：v4 05 §8.1 明文保留「不自动触发」，而 Spark 098 的预热方案与其实现意图（pre-step 第④通道）均以「目标驱动的有差别注入」为基础；无授权条款则预热落地即越权。含 Spark 098 遗留的 5 个待调查问题。
summary: |-
  **缺口来源**：`architecture-dialogue-2026-09-03-mnemon-base.md` §5.2 记录——v4 `05 §8.1` 对自动预热有**明文保留**：「`find-fact-object-candidates` 仍只在 AI 已明确进入事实消费分支后由 AI 显式请求，**不因环境 lifecycle、当前工作对象、关系存在或本段语义自动触发**」；且「本节不因定义语义模型而使……环境触发或**上下文注入能力**自动成立；这些能力必须在后续增量中另行定义预算、请求结果契约、失败语义」。

  **缺口**：Spark 098 的论证（目标驱动的有差别注入 ≠ 无差别 dump）正是对该保留的回应，但 **v5 规范层尚无对应授权条款**。`architecture-dialogue` §6.1 明确：「03 §8.2 当前只授权『已满足事实消费进入条件时』的预热；**自动 pre-step 注入需要规范增量**定义预算、触发时机、去重、失败语义」。该文自述「**pre-step 预热落地前需要规范增量（大概率是 03 §8 修订 + 08 §5.3 核验登记），这是 Human Gate 事项**」。Spark 098 留下的 5 个待调查问题（注入时机／注入量／去重／与引导分工／搜索 profile）要在此一并回答。

  **相关实现意图**（`technical-framework-mnemon-base.md` §2.1/§2.2）：注入走第 ④ 通道（`agent/pre-step` 瀑布追加 `{role:'user', source:{kind:'plugin', plugin:'dsh-ldvh', form}}`）；工程细节＝prepend 拿全量批次、只在 `step === 1` 注入、`ownRequest` 防自激、渲染文本 digest 去重、`cueAlreadyVisible()` 扫 `session.surface.nodes` 判可见性（rewind 不发 session-start，会话级 flag 不可靠）、快照追加在本插件消息块最底部、`form` 区分语义（LDVH 增 `template`）。原③通道（共享 runtime context 投影）已由 mnemon 实证弃用——一次写入导致所有贡献者 section 全部重发。

  **关联**：`architecture-dialogue` §6 另有三项待 Human 拍板（子代理会话策略、unavailable 引导段文案、实施批次启动确认），指向同一文档的实施安排，但本条 question 聚焦「预热注入的规范授权」。
serves: SG-1
priority: P2
object_uid: e173488d-be1a-4993-8e23-2be141bac461
fact_type_key: spark
created_at: 2026-09-12T22:05:48.712Z
change_log:
  - at: 2026-09-12T22:05:48.712Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 Spark 对象
  - at: 2026-09-13T15:08:54.071Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 回填 priority=P2（20 §8 新增字段，Human 裁定 A；AI 出初值，Human 可随时调整）
---

# pre-step 预热的规范授权缺口

## 当前理解

**缺口来源**：`architecture-dialogue-2026-09-03-mnemon-base.md` §5.2 记录——v4 `05 §8.1` 对自动预热有**明文保留**：「`find-fact-object-candidates` 仍只在 AI 已明确进入事实消费分支后由 AI 显式请求，**不因环境 lifecycle、当前工作对象、关系存在或本段语义自动触发**」；且「本节不因定义语义模型而使……环境触发或**上下文注入能力**自动成立；这些能力必须在后续增量中另行定义预算、请求结果契约、失败语义」。

**缺口**：Spark 098 的论证（目标驱动的有差别注入 ≠ 无差别 dump）正是对该保留的回应，但 **v5 规范层尚无对应授权条款**。`architecture-dialogue` §6.1 明确：「03 §8.2 当前只授权『已满足事实消费进入条件时』的预热；**自动 pre-step 注入需要规范增量**定义预算、触发时机、去重、失败语义」。该文自述「**pre-step 预热落地前需要规范增量（大概率是 03 §8 修订 + 08 §5.3 核验登记），这是 Human Gate 事项**」。Spark 098 留下的 5 个待调查问题（注入时机／注入量／去重／与引导分工／搜索 profile）要在此一并回答。

**相关实现意图**（`technical-framework-mnemon-base.md` §2.1/§2.2）：注入走第 ④ 通道（`agent/pre-step` 瀑布追加 `{role:'user', source:{kind:'plugin', plugin:'dsh-ldvh', form}}`）；工程细节＝prepend 拿全量批次、只在 `step === 1` 注入、`ownRequest` 防自激、渲染文本 digest 去重、`cueAlreadyVisible()` 扫 `session.surface.nodes` 判可见性（rewind 不发 session-start，会话级 flag 不可靠）、快照追加在本插件消息块最底部、`form` 区分语义（LDVH 增 `template`）。原③通道（共享 runtime context 投影）已由 mnemon 实证弃用——一次写入导致所有贡献者 section 全部重发。

**关联**：`architecture-dialogue` §6 另有三项待 Human 拍板（子代理会话策略、unavailable 引导段文案、实施批次启动确认），指向同一文档的实施安排，但本条 question 聚焦「预热注入的规范授权」。

（来源：docs/architecture-dialogue-2026-09-03-mnemon-base.md §5.2/§6/§7、docs/technical-framework-mnemon-base.md §2.1/§2.2；复核 §10.2 漏判 #1/#2）

## 调查问题

pre-step 自动预热的规范授权缺口应如何闭合？

## 调查边界

预热注入的授权条款写入相应规范（含预算、触发时机、去重与失败语义）、或 Human 判定不授权自动注入时停止。
