---
title: 启动引导对 specs/ 的悬空假定
status: open
question: 启动引导假定 `specs/00` 存在而安装流程从不创建 `specs/`，该假定如何消解？
scope_boundary: 安装流程处理 `specs/` 缺失的路径定案（创建/降级/如实报告）并写入规范时停止。
intent: 保留理由——C12 为 `[未承接/待决]`；这是**管辖项目首次接入即会触发**的缺口，不解决则新项目引导注入指向不存在的规范。
summary: "`fact-norm-design-consolidated` C12 记录：启动引导文本假定 `specs/00` 存在，但**安装流程从不创建 `specs/`**——悬空假定，标 `[未承接/待决]`。相关事实：`ldvh-base/norms/` 为空目录（C11）；本仓库 `specs/` 由人工建设，非安装流程产出。待定：新管辖项目的 `specs/` 由谁、何时、以何形态创建；缺失时引导应降级呈现还是报 `unavailable`（07 §7 fail-closed 语义）。"
serves: SG-3
object_uid: 89df677f-a2f8-4978-a117-bda89b146cbe
fact_type_key: spark
created_at: 2026-09-12T21:46:09.730Z
change_log:
  - at: 2026-09-12T21:46:09.730Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 Spark 对象
---

# 启动引导对 specs/ 的悬空假定

## 当前理解

`fact-norm-design-consolidated` C12 记录：启动引导文本假定 `specs/00` 存在，但**安装流程从不创建 `specs/`**——悬空假定，标 `[未承接/待决]`。相关事实：`ldvh-base/norms/` 为空目录（C11）；本仓库 `specs/` 由人工建设，非安装流程产出。待定：新管辖项目的 `specs/` 由谁、何时、以何形态创建；缺失时引导应降级呈现还是报 `unavailable`（07 §7 fail-closed 语义）。

（来源：docs/fact-norm-design-consolidated.md §0 C12）

## 调查问题

启动引导假定 `specs/00` 存在而安装流程从不创建 `specs/`，该假定如何消解？

## 调查边界

安装流程处理 `specs/` 缺失的路径定案（创建/降级/如实报告）并写入规范时停止。
