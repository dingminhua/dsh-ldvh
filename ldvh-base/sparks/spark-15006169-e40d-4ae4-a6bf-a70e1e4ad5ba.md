---
title: 任务勾选框形态是否入规范
status: open
question: WorkCase 任务级进度的复选框形态是否应当进入规范条文？
scope_boundary: 该形态明确落入某号规范条文、或明确决定不进入规范并记录理由时停止。
intent: 保留理由——F1 被重建总纲列为 21 号本轮新增设计输入第 5 条并给出具体形态，但经核实仅判据语义落地、勾选框形态未落；且总纲自设「每加一层机制须回答防 v4 哪个实际问题」门槛，本条可能同样得出「不必进入规范」的结论，两种结果都需显式判断，不保留则该判断无承载。
summary: WorkCase 内部的任务级进度呈现，其设计形态已定但未落地。重建总纲第 7 节新增设计输入第 5 条记为 F1「复选框任务级进度」：以 tasks.md 形态（[ ]/[x] + T 编号 + [P] 并行标记）作为 work items 的下限形态，并要求「30-38 模板从勾选框下限开始长，每加一层机制须回答『防 v4 哪个实际发生过的问题』，答不出不加」。该机制源自 Spec Kit 与 specflow 的 tasks.md 实践（勾选框 + T001 编号 + [P] 并行标记，记录、进度、选择器三合一），总纲第 8 节吸收总账列为 F1、标注落点为「21 号 / 04 号」。总纲第 1 节三层概念图的第三层也写着「任务勾选：今天做什么，WC 内部（T 编号 + [P] 并行标记 + 勾选）」。现状核实结果：21 号第 8 节的 plan 字段是对象数组，每项为 {step, done_criteria}，承载「步骤 + 逐条完成判据」——判据语义确已落地，但 tasks.md 的勾选框形态（[ ]/[x]、T 编号、[P] 并行标记）三样均未落；04 号中「任务勾选框」「T 编号」「并行标记」亦零命中。即判据语义已落、呈现形态未落。需要判断的核心：这一形态是否应当进入规范条文。总纲自设一道门槛——「每加一层机制须回答『防 v4 哪个实际发生过的问题』，答不出不加」；因此本条不只是补写条文，还可能得出「判定该形态不必进入规范、作为实现层或模板层呈现即可」的结论，那同样是合法终点。来源：本条由「重建总纲的剩余承接项」（spark-3acec019）拆分而来。
serves: SG-1
priority: P1
object_uid: 15006169-e40d-4ae4-a6bf-a70e1e4ad5ba
fact_type_key: spark
created_at: 2026-09-13T07:58:09.446Z
change_log:
  - at: 2026-09-13T07:58:09.446Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 受控创建 Spark 对象
  - at: 2026-09-13T15:08:53.999Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 回填 priority=P1（20 §8 新增字段，Human 裁定 A；AI 出初值，Human 可随时调整）
---

# 任务勾选框形态是否入规范

## 当前理解

WorkCase 内部的任务级进度呈现，其设计形态已定但未落地。重建总纲第 7 节新增设计输入第 5 条记为 F1「复选框任务级进度」：以 tasks.md 形态（[ ]/[x] + T 编号 + [P] 并行标记）作为 work items 的下限形态，并要求「30-38 模板从勾选框下限开始长，每加一层机制须回答『防 v4 哪个实际发生过的问题』，答不出不加」。该机制源自 Spec Kit 与 specflow 的 tasks.md 实践（勾选框 + T001 编号 + [P] 并行标记，记录、进度、选择器三合一），总纲第 8 节吸收总账列为 F1、标注落点为「21 号 / 04 号」。总纲第 1 节三层概念图的第三层也写着「任务勾选：今天做什么，WC 内部（T 编号 + [P] 并行标记 + 勾选）」。现状核实结果：21 号第 8 节的 plan 字段是对象数组，每项为 {step, done_criteria}，承载「步骤 + 逐条完成判据」——判据语义确已落地，但 tasks.md 的勾选框形态（[ ]/[x]、T 编号、[P] 并行标记）三样均未落；04 号中「任务勾选框」「T 编号」「并行标记」亦零命中。即判据语义已落、呈现形态未落。需要判断的核心：这一形态是否应当进入规范条文。总纲自设一道门槛——「每加一层机制须回答『防 v4 哪个实际发生过的问题』，答不出不加」；因此本条不只是补写条文，还可能得出「判定该形态不必进入规范、作为实现层或模板层呈现即可」的结论，那同样是合法终点。来源：本条由「重建总纲的剩余承接项」（spark-3acec019）拆分而来。

## 调查问题

WorkCase 任务级进度的复选框形态是否应当进入规范条文？

## 调查边界

该形态明确落入某号规范条文、或明确决定不进入规范并记录理由时停止。
