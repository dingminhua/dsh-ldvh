---
title: 事实对象载体内聚校验后置
status: open
phenomenon: 写 Spark/Pitfall 等事实对象时首次提交被载体内聚与载体结构校验拒绝，需按规范原文重新对齐 frontmatter 与正文后才通过。
attribution: 20/23/26 各 §14.1 类型特有验证表与写工具（spark-write、pitfall-write）的提交期校验时机；规则存在但位于 01.Att.03 §4 的 L3/L4 披露层，构造对象时不在视野
impact: light
serves: SG-3
object_uid: 3048937d-a463-4bea-9d10-7cafde116d16
fact_type_key: friction
created_at: 2026-09-13T21:07:17.755Z
change_log:
  - at: 2026-09-13T21:07:17.755Z
    provider: workbuddy
    model: deepseek-v4.1-flash
    summary: 初次创建：事实对象载体内聚与结构校验的提示滞后于构造阶段，同一会话内 5 次拒绝（Spark 3 次、Pitfall 2 次）——三条件逐项核对成立，本账本首笔
---

# 事实对象载体内聚校验后置

## 现象

写 Spark/Pitfall 等事实对象时首次提交被载体内聚与载体结构校验拒绝，需按规范原文重新对齐 frontmatter 与正文后才通过。本次会话内命中 5 次同一机制（Spark 3 次：正文重复 H1、`summary` 未在正文逐字出现、结构拒绝；Pitfall 2 次：无 `urls` 时多写「证据」段、`scope` 用中文引号与 frontmatter 不一致），均发生在首次受控写入，每次代价是一轮重写后才落盘。

## 入账依据

三条件逐项核对：

**重复出现**——本次会话内 5 次命中同一机制，非单次卡顿：Spark 受控更新被拒 3 次（`spark/body_invalid` 正文 H1 重复、`spark/coherence_invalid` 当前理解段未逐字含 `summary`、结构问题），Pitfall 受控创建被拒 2 次（`pitfall/body_invalid` 无 `urls` 却含「证据」段致 H2 段数不符、`pitfall/coherence_invalid` 影响与适用范围段未逐字含 `scope`）。五次拒绝均由同一类规则触发，属证据等价的重复性。

**可归因**——机械校验由 `20 §14.1`、`23 §14.1`、`26 §14.1` 的类型特有验证表定义，校验时机在写工具（`spark-write` / `pitfall-write`）的提交阶段；相关不变量（正文自 H2 起、H1 由 Code 从 title 生成、frontmatter 是唯一权威文本且对应段须逐字包含、条件段与 `urls` 共现）散居各类型 `§8` 字段契约与 `§14.1` 验证表，按 `01.Att.03 §4` 属 L3/L4 披露层，构造对象时通常不在视野。

**修了会更好**——若这些不变量在构造阶段前置可见（写工具 schema 声明，或创建提案模板列出「正文自 H2 起」「frontmatter 为唯一权威文本、对应段逐字包含」「无 urls 不写证据段」等核对项），首次通过率提高，重写轮次减少。**修复不改变任何机械校验的强度**——不是放松校验以换取通过，而是把同一组不变量提前告知，故与 `00 §7` 的机械保障方向一致。规则本身完备且正确，摩擦在「规则位置与使用时机错位」：校验点后置于提交，而构造时缺少同源提示。
