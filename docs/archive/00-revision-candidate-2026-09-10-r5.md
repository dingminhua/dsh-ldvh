# 00 修订候选 r5（终稿候选 · 2026-09-10）—— 七项决定落地文本

> 状态：**待窄范围复核 → 待 Human 放行写入 → 未写入 00**。
> 依据：Human 对第 ①–⑧ 项的逐项决定（见 §一），及此前已落地项（r1 的 §2、`50fa5da` 的存取记忆与 §3.3 首句）。
> 写入范围：`specs/00-理念与构成.md` 的 §3 导语、§3.1–§3.6、§4.2、§4.5、§5.5、§6.1、§7.1。提交仍只含 00 一个文件。

---

## 一、七项决定（本包即其落地）

| # | 决定 | 落地位置 |
|---|---|---|
| ① | 业务系统作为**承诺**写入 00（记忆／调研／讨论／执行／遵守五个），增删改须修改本规范 | §3 导语、§3.1、§4.2 |
| ② | 甲：§4.2 保留「业务系统或制度的**数量、名称或职责**」；§3.1 制度句连带对齐（增删按第四章） | §4.2、§3.1 |
| ③ | 甲：§4.5／§5.5 的「模板／行动模板」改「**既有行动结构**」 | §4.5、§5.5 |
| ④ | 乙：窗口期以 00 为准＋强制报备＋具体依赖冲突时才暂停（不落 00 正文，写入本包与提交说明） | 本包 §三 |
| ⑤ | 丙：05 修订与 00 本批**并行起草、紧邻提交** | 本包 §三 |
| ⑥ | 甲：边界原则三句入 §3.3（含「具体归属判断由规范模型定义」） | §3.3 |
| ⑦ | 两层记忆（结构名不进总纲）；§2 用「存取记忆」；**规范源回归**（两级承载） | 已落地（`50fa5da`） |
| ⑧ | 甲：§7.1 加治理诚信锚点 | §7.1 |

---

## 二、逐节最终文本（可直接替换）

### 2.1 §3 导语（现行 `:92`）

```text
LDVH 的构成包括工作模型、两类语义构成要素、规范源、事实源、能力承载和 DSH 原生插件：工作模型界定 AI 的工作职责——能力承载（LDVH CLI 确定性服务与宿主原生机制两族）提供实际手段，记忆、调研、讨论、执行、遵守五个业务系统承载业务过程，制度贯穿其间；两类语义构成要素组织规则与事实；规范源与事实源分别承载可强制执行规则与稳定事实，为行动确立边界与依据；DSH 原生插件承载价值交付。它们职责分工、互不替代；具体技术、流程、字段和实现由下位规范承接。
```

### 2.2 §3.1 标题（现行 `:94`）

```text
### 3.1 工作模型（能力承载、业务系统与制度）
```

### 2.3 §3.1 正文（现行 `:96–116`，整段替换）

```text
LDVH 的工作模型由能力承载、业务系统与制度构成：

**能力承载**（两族，提供实际手段，不独立产生业务结论）：

- **LDVH CLI 确定性服务**：承载读与写——取得当前工作所需且来源明确的信息；把已经明确去向的内容受控写入，并核对实际结果；
- **宿主原生机制**：承载编排与注入——调度子代理执行委派的工作，管理其通信、防护、中断恢复与结果回收；并在会话中注入规则引导。

**业务系统**（承诺；承载业务过程，直接产生决策、行动或结论）：

- **记忆**：留存和取回项目的经验认识，从目标、判断、行动、结果、摩擦和反馈中形成候选认识；沉淀的分流路由与反思子机制由本系统承接；
- **调研**：围绕不掌握的问题搜集证据，直到不再产生新的确认认识；
- **讨论**：把 Human 意图经扩散和约束收敛为方案，由主控选定；
- **执行**：执行已批准的工作，按工作包生命周期收口；
- **遵守**：识别并落实适用的规则、事实、决定和授权。

各业务系统以自身事实对象承载其结论。新增、删除业务系统，或改变业务系统的数量、名称或职责，均须修改本规范，并按第四章取得 Human 决定。

**制度**（开放集合，贯穿所有系统，不独立产出；本规范登记当前已知的制度，新增、删除或改写制度按第四章取得 Human 决定）：

- **复核**：依据已经确定的目标、方案和成功标准，检查实际工作和结果；
- **沉淀**：判断哪些认识和结果值得跨行动保留，以及应进入什么稳定位置；其去留纪律属制度，路由面由记忆系统承接。

能力承载回答"怎么跑"，业务系统回答"做什么"，制度回答"怎么保证做对"。三者是同一工作中的不同职责形态，不是必须依次经过的流程；其具体职责、技术、流程与协同由相应下位规范承接。

工作包生命周期（批准→执行→复核→关闭→交还）是各业务系统被批准工作的共同收口模式，它是模式而非系统；由宿主原生机制（编排）承载运行，其业务面由执行系统承接，主控对业务结果承担整体责任，具体由执行系统规范承接。
```

### 2.4 §3.2（现行 `:118–120`）

```text
### 3.2 两类语义构成要素

规范与事实是 LDVH 组织语义的两类要素，职责分工、互不替代：规范界定行动边界与流程要求，事实锚定判断。

事实对象可以承载规范（事实规范），但事实不能反向创造或放松规则；任何语义要素都不因被记录、承载或呈现而取得规范效力、事实、Human 授权或完成结论。Code、Web、工具输出和宿主能力不构成第三类语义构成要素。要素分类以本节为准，规范的承载与层级见 §3.3。
```

### 2.5 §3.3（现行 `:122–126`）

**首句已于 `50fa5da` 落地，本次只补两处**：

**（a）在第二段之后新增边界原则段**：

```text
元规范约束 LDVH 自身及其机制对项目的运作；项目的内容、结构与工程纪律归事实规范。元规范不替项目定规则，事实规范不替 LDVH 定机制；具体归属判断由规范模型定义。
```

**（b）判据句中原串替换**：

- 原串：「环境入口和开发备忘，不得构成独立于规范源的另一套规则来源。」
- 新串：「环境入口、已退出或未受控的机制文本和开发备忘，不得构成独立于规范源的另一套规则来源。」

### 2.6 §3.4（现行 `:128–132`）

- 标题原串：「### 3.4 技术支撑与机械保障」→ 新串：「### 3.4 技术与机械保障」
- 首句原串：「技术只用于支撑根方案」→ 新串：「能力承载所依赖的技术只用于支撑根方案」
- 原串：「机械保障只执行规范源已经定义且能够确定检查的内容」→ 新串：「机械保障只执行规范已经定义且能够确定检查的内容」

### 2.7 §3.5 第一段与末段首句（现行 `:136`、`:138`）

```text
DSH 原生插件是 LDVH 的统一交付形态，承载 AI 工作引导、LDVH CLI 确定性执行、Web 信息呈现、Git 机械守护四种价值交付方式：AI 工作引导向主控提供规则引导的语义文本；LDVH CLI 确定性执行向主控提供规则与事实的受控读取、写入和校验等确定性服务；Web 信息呈现向 Human 呈现项目事实、状态、依据、风险和待决定事项；Git 机械守护在真实 Git 事件中执行规范已经定义的确定性检查。
```
（末段首句原串「插件是能力的承载形态，不是新的**规范源**、事实源、授权来源或责任主体。」→ 新串「插件是能力的承载形态，不是新的**规则来源**、事实源、授权来源或责任主体。」）

### 2.8 §3.6（现行 `:142`，第一段尾补一句）

```text
…不得新增语义构成要素、建立第二套权威，或改变、重新解释本章定义。元规范与事实规范的两级承载不构成第二套权威：两级规范共用同一套受控建立程序，事实规范不得放松元规范、不得自授效力（详见 §3.2、§3.3）。
```

### 2.9 §4.2 根决定清单（现行 `:156`，两处原串替换）

**替换 1**
- 原串：「改变技术支撑、业务系统或制度的数量、名称或职责，改变三类语义构成要素、规范源、事实源或四种价值交付方式」
- 新串：「改变能力承载的分类、业务系统或制度的数量、名称或职责，改变两类语义构成要素、规范源、事实源或四种价值交付方式」

**替换 2**
- 原串：「新增、删除、改名或重排规范模型、事实模型、行动模板、Code 或 Web」
- 新串：「新增、删除、改名或重排规范模型、事实模型、Code 或 Web」

### 2.10 §4.5（现行 `:172`）

- 原串：「Human 的决定权不能被主控、协助者、规范、事实、模板、Code、Web、工具或环境入口替代」
- 新串：「Human 的决定权不能被主控、协助者、规范、事实、既有行动结构、Code、Web、工具或环境入口替代」

### 2.11 §5.5（现行 `:202`）

- 原串：「主控的最终责任不能被 Human 决定、协助者输出、复核、测试、机械检查、事实对象或行动模板替代」
- 新串：「主控的最终责任不能被 Human 决定、协助者输出、复核、测试、机械检查、既有行动结构或事实对象替代」

### 2.12 §6.1 V4 判据（现行 `:213`）

- 原串：「执行复用既有行动模板并记录复用依据」
- 新串：「执行复用既有行动结构并记录复用依据，其判据由相应业务系统规范定义」

### 2.13 §7.1（现行 `:244`，段尾补一句）

```text
…AI 自述不得作为事件已经发生的依据。凡声称某项机械规则已被保障，而其实现与该项规则的来源规范不一致或不存在，即构成超出依据支持的声明。
```

---

## 三、写入后的处置口径（决定 ④⑤ 的落地，不写入 00 正文）

**窗口期口径（决定 ④ 乙）**：
- 以 00 为准；下位未同步期间的偏差**强制报备**；
- 仅当某具体行动依赖某条已被 00 改变的旧条文、且无法按 00 执行时，才暂停受影响范围并提请。

**05 并行线（决定 ⑤ 丙）**：
- 05 修订与 00 本批**并行起草、紧邻提交**；
- 05 为**同一职责改名**（01 §11.3）：**编号 05 保留、`spec_key` 保持 `helper-service-contract`、不设 `supersedes`**；
- 05 的实质改动仅两处：§5.1 否定句（「不要求另建 CLI」→ CLI 为规范形态、会话内经 DSH 工具面薄适配）、§7（删「v5 不设 CLI 响应投影档位」），另加标题与正文 "Helper" 命名统一。

---

## 四、仍需在提交说明/报备中列明的事项

1. **00 引用尚未存在的承接载体**：§3.1 尾句指向"执行系统规范"——执行系统已按决定 ① 写入 00，但其规范（业务系统区块）尚待起草；
2. **下位引用漂移 11 处**（与 05 命名窗口并列报备）：`01:37`／`01:131`／`03:37`／`04:37`／`05:37`／`06:37`／`07:37`／`08:37`／`09:37`／`22:275`／`23:27`／`23:250`；
3. **「规范源」旧术语面（复审实测）**：`specs/` 不含 00 为 **21 处**、含 00 为 **28 处**；连同 `ldvh-base/`、`plugin/` 与根目录合计约 **50 处**。随各批同步修订清理，以 `01.Att.01` 收口为准；
4. **05 改名的连带引用**（随 2.1 同批处理）：`05:37` 自指「§3.5 的 Helper 确定性执行」、`08:37`「四种价值交付方式」、`09:37`「技术支撑与机械保障」、`01.Att.01:34`／`:45`；
5. **`plugin/lib/guidance-text.js`**（受保护内容）自本批落地即与 00 不符，其更新按各自独立提交排期。
