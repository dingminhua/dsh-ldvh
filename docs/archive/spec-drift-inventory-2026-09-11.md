# specs 与 00 现状的偏移盘点（2026-09-11）

> **性质**：**盘点（inventory）**，非候选、非计划。只报告机械可核验的偏移事实，不含处置决定。
> **基准**：`specs/00-理念与构成.md` @ `012605f`，工作区与 HEAD 逐字一致（`git diff --quiet HEAD -- specs/00` 通过）。
> **方法**：全部结论来自对当前工作区文件的实际读取与 `grep` 计数，逐条附命令可复现。**未**采信 `docs/spec-sync-plan-2026-09-11.md` 的结论，因其基于旧版 00（`ca89742`／`77d3703`），本轮逐条复核。

---

## 一、总览

| 编号 | 偏移 | 严重度 | 状态 |
|---|---|---|---|
| **D1** | 「行动模板」为已退役概念，仍在 12 份规范中存活 | 高 | 未处置 |
| **D2** | `04-行动模板基础规范.md` 整份悬空 | 高 | 未处置 |
| **D3** | 21 号 WorkCase 规范被 10 处引用但不存在 | 中 | 未处置 |
| **D4** | `02` 章节号断层（`### 19.x`／`20.x` 挂在 `## 21`／`22` 下） | 中 | 未处置 |
| **D5** | `01.Att.01` 术语表对 `行动模板` 的溯源引用不成立 | 中 | 未处置 |
| **D6** | `01` §8.1 编号表仍登记 04 行、`:135`／`:190` 仍定义 `30–38` 行动模板段 | 中 | 未处置 |
| —— | 批 1 术语替换（技术支撑→能力承载等） | —— | **已完成** |

---

## 二、已完成项（先行澄清，避免重复劳动）

上轮计划中的**批 1（术语机械替换）已经落地**，以下均已清零：

```
$ grep -rc '技术支撑'   specs/ | ... → 0
$ grep -rc '沉淀制度'   specs/ | ... → 0
$ grep -rc '沉淀路由制度' specs/ | ... → 0
$ grep -rc '四大业务系统' specs/ | ... → 0
$ grep -rn '遵守系统' specs/ | grep -v '规则遵守系统' → 无命中
```

**注意**：裸计数 `grep -c '遵守系统'` 返回 4，但**该 4 处全部是 `规则遵守系统`**（子串包含造成的假阳性），逐条核对后为：
- `specs/04-行动模板基础规范.md:51`、`:191`、`:195`
- `specs/12-讨论系统规范.md:124`

→ 术语层**已无「遵守系统」裸用**。**不得**据裸计数重复替换。

`02` 亦已完成六系统结构落地（§6–§8 能力承载三节、§9–§14 六业务系统、§15 复核与校验制度、§16 沉淀与反思）。

---

## 三、偏移明细

### D1：「行动模板」已退役但仍在 12 份规范存活

**依据**：00 中「行动模板」出现 **0 次**；替代术语为「既有行动结构」（`:182`、`:210`、`:225`）。

```
$ grep -c '行动模板' specs/00-理念与构成.md   → 0
$ grep -n '既有行动结构' specs/00-理念与构成.md
  182: Human 的决定权不能被主控、协助者、规范、事实、既有行动结构、Code…替代
  212: 主控的最终责任不能被 Human 决定…既有行动结构或事实对象替代
  225: V4 稳定推进…执行复用既有行动结构并记录复用依据
```

**分布**（共 53 处）：

| 文件 | 处数 |
|---|---|
| `04-行动模板基础规范.md` | 25 |
| `01-规范模型基础规范.md` | 6 |
| `02-工作模型基础规范.md` | 6 |
| `03-事实模型基础规范.md` | 3 |
| `08-DSH环境接入与插件发布规范.md` | 3 |
| `09-Code实践与测试规范.md` | 3 |
| `10-Web呈现与交互规范.md` | 2 |
| `05`／`06`／`12`／`25`／`01.Att.01` | 各 1 |

### D2：04 整份规范悬空

- `04` 文件仍在 `specs/`，身份块 `title: "行动模板基础规范"`、`spec_key: "action-template-foundation"`；
- 其 `basis` 含 `ldvh-root`，但 **00 已无「行动模板」这一概念** —— 上位依据不存在；
- 同时它仍被 `01:66`、`01:135`、`01:174` 引为**「34–38 行动模板规范共同结构」的唯一权威**。

→ 现状是「上位依据已被 00 删除、自身仍生效且被引为唯一权威」的**悬空规范**。

### D3：21 号 WorkCase 规范缺失

```
$ ls specs/21*
ls: specs/21*: No such file or directory
```

被引用 **10 处**，含规范正文（非仅 docs）：

| 文件:行 | 引用形态 |
|---|---|
| `02:149` | 「具体契约由 21 号类型规范…承接」 |
| `12:83`、`12:169` | 「21 号据此砍除需求审核阶段」「WorkCase（21）」 |
| `10:128` | 「Gate 1 / Gate 2 仅为 WorkCase provisional 术语，在 21/34 定稿前不作为触发依据」 |
| `20`／`22`／`23`／`24`／`25` 等 | 各 1–2 处 |

→ 「执行工单」目前**无类型规范承载**，且 00 §2 已将「执行工单」列为承诺功能。

### D4：02 章节号断层（机械可判定）

```
$ python3 扫描 specs/*.md 的 ### N.M 与所属 ## N
specs/02-工作模型基础规范.md  (5 orphaned)
   L217: under ## 21 -> ### 19.1 领域增量事项
   L226: under ## 21 -> ### 19.2 决定边界
   L234: under ## 22 -> ### 20.1 领域触发与影响范围
   L243: under ## 22 -> ### 20.2 暂停期间允许动作
   L247: under ## 22 -> ### 20.3 恢复与再次触发
```

**仅 02 存在此类断层**，其余 18 份规范扫描无命中。成因应是 §14–§17 扩章后未回填子节号。

### D5：01.Att.01 术语表溯源引用不成立

```
$ grep -n '行动模板' specs/attachments/01.Att.01-LDVH双语术语表.md
  32: | 行动模板 | Action Template | — | — | `ldvh-root` §3.2 | 04 承接其具体规则 | defined |

$ grep -n '行动模板' specs/00-理念与构成.md   → 无命中
$ sed -n '124,126p' specs/00-理念与构成.md   → §3.2 现为「两类语义构成要素」
```

→ 该术语声称源自 `ldvh-root §3.2`，但 §3.2 现为两类语义构成要素节，**全文不含「行动模板」**。这是一条**指向不存在依据的溯源**，且 `Att.01` 是附件（术语权威），属实质问题。

### D6：01 号编号表与预留段未同步

| 位置 | 现状 | 与 00 的关系 |
|---|---|---|
| `01:174` | `\| 04 \| 行动模板基础规范 \| 模板共同机制、预检与风险路由 \|` | 登记了已退役概念 |
| `01:135` | 「04 定义 `30–38` 行动模板规范共同结构」 | 同上 |
| `01:190` | 「`30–38` 为行动模板规范预留段」 | 同上 |
| `01:62`／`01:66` | 正文以「行动模板」为相邻规范职责 | 同上 |

→ 另注：`30–38` 段存在**规划冲突**（旧计划拟将其改用作业务系统段），此为待裁定事项，非本盘点的偏移结论。

---

## 四、未处置范围的说明（残留风险）

1. **本轮未逐字通读全部 19 份规范**，D1–D6 为定向机械扫描结果；**语义层面的偏移**（如某节论断与 00 新口径冲突但用词不同）本轮**未系统排查**，不排除存在未发现的偏移；
2. **`docs/` 未纳入**（非规范效力，无回指义务）；
3. `plugin/` 侧代码与受保护文本（`guidance-text.js` 等）**本轮未检查**；
4. **`01.Att.02` 与 `spec-registry.js` 的 `dimensions` 七元组约束**未验证是否与六系统口径一致 —— 属跨文档议题，需单独核查；
5. 本盘点**不构成处置建议**；D1–D6 的处置顺序、是否立项 21、04 空号是否保留，均属 Human 决定范围。

---

## 五、复现命令

```bash
cd /Users/dmh2002/DshProject/dsh-ldvh
grep -rc '技术支撑'        specs/          # 期望 0
grep -rc '沉淀制度'        specs/          # 期望 0
grep -rn '行动模板'        specs/          # 期望 0（当前 53）
ls specs/21*                                # 期望存在（当前不存在）
grep -n '行动模板' specs/attachments/01.Att.01-LDVH双语术语表.md
git diff --quiet HEAD -- specs/00-理念与构成.md && echo "基准=HEAD"
```
