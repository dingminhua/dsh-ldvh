# 十维独立对抗审核报告 · `specs/10-Web呈现与交互规范.md`

- **审核对象**：`specs/10-Web呈现与交互规范.md`（226 行，H1 编号 `10`，`spec_key: "web-presentation-interaction"`）
- **审核者身份**：`session-95426f0f-…` 派生的独立子代理；未参与 10 的起草，未修改任何文件、未 git commit。
- **审核基准快照**：Git 分支 `dev`，工作树干净；10 当前文本已包含最近修复提交 `0a1b2d3`（2026-09-12 20:22）。
- **审核方法**：通读 10 全文（226 行）；定向核对 `00`/`01`/`02`/`07`/`08`/`21` 被引章节；静态核验 `plugin/web/` 源码与 `dist`；机械运行 `parseSpecDocument`；独立复核两份既有审核 `docs/review-10.md`、`docs/review-10-vs-audited-set.md` 是否覆盖实际已发生的改动。
- **一句话问题说明**：10 正文最近刚修掉了两处旧硬伤（§6.5 复述 08 §6、§6.2 的 provisional 表述），文本自洽已明显好转；但我的独立复核新发现三类残留——21 §3.3 仍原样引用 10 已删掉的旧句（反向引用过期）、08 §6 自身写进了"呈现"措辞与 10 §6.5(c) 重叠、§12 反过度设计红线没覆盖"过度动画/信息过载"这类 10 的真实过度设计风险，且修复本身没有对应的一轮事后审核闭环。

---

## 1. 文件路径

`specs/10-Web呈现与交互规范.md`（身份块 `basis: [ldvh-root, specification-model-foundation, work-model-foundation]`；`relation: ""`、`parent_spec: ""`，作为 00–09 平级独立规范，无结构父，符合 `01 §10.1`）。

机械解析：`parseSpecDocument(text, "specs/10-Web呈现与交互规范.md")` 返回 `ok: true`（身份块与 H1/路径一致）。

---

## 2. 十维逐维结论

### D1. 价值判据对齐 —— **通过**
- `10:27` 对 HV1–HV5 均以 `00 §6.2` 的**编号 + 名称原文**逐项/分组映射（HV1 决策提请清晰可决、HV2 授权执行受控可续、HV3 入档闭环节点可验、HV4 积累效用直观可见、HV5 项目演进脉络可循），已修复旧审核 A3 的"缺名称"问题。
- V 轨价值由 AI 侧规范承接、Web 只呈现其可观察结果的边界在 `10:27/29` 明确。符合。

### D2. 职责边界与单一权威 —— **基本通过（有残留边界重叠，见 F-B）**
- **10 §6.5 与 08 §6 的双定义已消除**：当前 `10:144` 首句明确"安装事务的步骤、状态闭集与失败处置由 08 §6 定义，本文不复述"，仅保留 (a)(b)(c)(d) 四项呈现/交互约束。08 §6 独占安装事务过程权威，10 仅持呈现权威——单一权威成立。
- **关于任务重点提示的"10:144 负载型/路由型"争议**：修复前两份审核得出相反结论（`review-10.md` 判路由型、`review-10-vs-audited-set.md` 判负载型）。**独立判定（修复后）**：`10:144` 现为**路由型引用**——其"写入与移除经 07 §5.4/§5.7 和 08 §6 定义的受控流程"只指向权威、不消费 08 的状态闭集或事务步骤（步骤/闭集已显式"不复述"）。判定依据：`01 §10.1` 路由型定义为"只指向职责归属、不消费内容"；本行已删除旧版对 `absent/outdated/incomplete/conflict/unavailable` 闭集的取值复述与事务步骤复述，故不再落入"按其闭集取值"负载示例。反例检验：若 10 仍照 08 §6 闭集逐值定义 Web 可呈现操作，则为负载型；现文本已不含此内容，故反例不成立，路由型成立。
- **残留（F-B）**：08 §6 自身含"设置页对可修复状态…呈现'更新'操作…`conflict` 或 `unavailable` 不可修复状态只呈现原因"的呈现措辞，与 10 §6.5(c)"可修复与不可修复状态须在交互上可区分，前者提供修复入口、后者只呈现原因"重叠。即 08 §6 反向写进了呈现指令（见维度 7 反例）。

### D3. 反过度设计 —— **通过**
- `10:128` 触发条件"以 21 的定稿文本为准"：**21 已定稿**（`21 §3.3` 自称"本文已定稿"、§10 为 Gate 语义权威），故无"等别家不定稿就空转"风险。
- `10:223`（§12 红线 2）"不指定 UI 框架、组件库或预设组件形态——只定义呈现语义与交互契约"，未越界规定实现细节。**但未覆盖过度动画/信息过载风险**（见 F-D）。

### D4. 技术可实现性与能力真实性 —— **部分通过（静态核验，运行时不可核，见 F-C 与维度 9）**
- 实测 `plugin/web/` 结构：`plugin/web/client/` **不存在**（任务提示假定的路径过期），真实前端代码在 `plugin/web/src/`；`plugin/web/api/services/` 与 `plugin/web/api/routes/` 存在。
- 10 描述的四层阅读器标签在源码中的落点：
  - `字段级直读`：在 `src/utils/api.ts`、`api/routes/federation.ts`、`api/routes/cognition.ts` 出现（真实存在）；
  - `扩展阅读`：在 `src/components/Layout.tsx`、`src/i18n/locales.ts` 出现（真实存在）；
  - **`卡片网格`、`语义详情`：在 `src/` 中无命中**（仅出现在 `dist/` 打包产物、测试与 `plugin/web/docs/` 文档），即这两项标签在当前源码中**尚未落地**。
- `10:161` 要求 UI 状态标签"尚未初始化"：在 `src/` 无命中（仅 `unavailable` 类状态存在）。属实现在途信号，因另一会话并发改 `plugin/web/`（26 §9 配色），记为"并发改动"标注，不作为确认不一致。
- 结论：10 所述能力**部分**有源码对应，四层读者命名未完全落到 `src/`；鉴于实现在途，能力真实性只能做静态/文本级核验，不能核运行时（维度 9）。

### D5. 身份、编号、引用和格式 —— **基本通过（1 项反向引用过期，见 F-A）**
- 身份块完整、`parseSpecDocument` 通过；固定八章标题逐字、§3/§4 各三子节、末四章顺延，无跳号/空号（近期"删 §6.5 重复"实为删除 §6.5 内对 08 §6 步骤的复述文本，§6.5 章节号保留，`10:141-144` 仍完整，编号连续）。
- `basis`(3 项) 与 §2(3 项) 1:1，且 §2 各项已附精确章节：`00 §3.5/§4/§6.2/§7.2/§7.3`、`01 §7/§9/§12`、`02 §6/§9`——已修复旧审核 A2 的"§7.2/§7.3 未列"与"02 无 §号"两处缺口。
- 跨规范引用逐处核实目标章节均存在：`00 §3.5/§4/§6.2/§7.2/§7.3/§7.4`、`01 §7/§9/§12`、`02 §6/§9`、`07 §5.4/§5.7/§5/§7/§10`、`08 §6`、`21`（§3.3/§10）。无悬空引用。
- §1 V/HV 以 `00 §6` 编号+名称原文引自 `00 §6.2`（符合 `01 §7.1 item 1`）。
- **残留（F-A）**：10→21 正向引用现已一致，但 **21 §3.3 反向原样引用 10 已删除的旧句**——"10 §6.2 将 Gate 1 / Gate 2 标注为「WorkCase provisional 术语，在 21/34 定稿前不作为触发依据」"，而 10 已不再作此表述。跨规范反向引用过期（21 虽声明"引用同步不由本文执行"，但对任何交叉核对的读者而言是事实性过期）。

### D6. 防自欺与不能单独证明的边界 —— **通过**
- `10:31`（§1 末段）、`10:87`（§4.3）、`10:181`（§9 末段）均明确"页面呈现/组件加载/Human 操作只证明当次呈现与交互范围，不单独证明事实成立、授权覆盖、技术验证、工作完成或价值兑现"。
- §9 七列验证表每行均含"可证明范围"列，明示"不证明内容正确/事实成立/价值兑现"等。防自欺边界陈述充分。

### D7. 规范内部一致性与反例检验 —— **见反例清单（F-D 红线缺口为该维核心发现）**

### D8. 草案、修复、Human 决定和提交闭环 —— **有条件（见 F-E）**
- 两笔关键修复（删 §6.5 对 08 §6 的复述、改 §6.2 触发条件）确实落在提交 `0a1b2d3`，提交说明逐条记录了这两笔变更，与 `git show 0a1b2d3` 一致。
- **闭环缺口**：既有两份审核 `docs/review-10.md`、`docs/review-10-vs-audited-set.md` 描述的是**修复前**状态（它们把"§6.5 复述 08 §6""§6.2 的 21/34 provisional"列为"已证实不一致"）。修复提交落地后，**没有一份事后对抗审核**核验修复是否正确、是否引入新不一致（本报告即填补此缺口）。即"审计发现→修复→再审计"的第三环缺失。
- 10 成熟度态（`01 §6.3` 三态）：普通规范无 `status` 字段；从 git 历史看 10 已多轮审计+修复，但仓库内**无显式 Human 对准确候选+作用范围的基线确认记录**可供我核到"设计基线/当前规则源成员"判定，仅能确认其历经审核，态级未由我独立证实。

### D9. 审核自身是否与风险匹配 —— **见第 6 节风险档位反向核对（本文只核文本自洽 + 静态实现对应，不核运行时呈现）**

### D10. 中文语义清晰、术语一致与机器表示对应 —— **通过（术语一致）**
- 术语统计：`呈现` 75 次、`渲染` 2 次、`展示` 0 次。领域词统一为"呈现"；`渲染` 仅用于技术义（"不渲染关联关系""各层渲染结果"），未与"呈现"混用；无"展示"混入。术语一致。
- 边缘条件处用"显示"（如 `10:161` 显示"尚未初始化"）为 UI 具体动作词，与"呈现"不冲突。
- 机器表示：10 正文未自引任何代码路径，故无与仓库实际路径不符之处；任务提示假定的 `plugin/web/client/` 实不存在（真路径 `plugin/web/src/`），属提示侧信息过期，非 10 缺陷。

---

## 3. 问题清单（分级 + 引文行号 + 复现）

### 已证实问题（须修复 / 须闭环）
1. **[F-A / 中] 21 §3.3 反向引用过期（跨规范不一致）**
   引文：`specs/21-WorkCase-工作项.md:73` 仍写"10 §6.2 将 Gate 1 / Gate 2 标注为「WorkCase provisional 术语，在 21/34 定稿前不作为触发依据」"；而 `specs/10-Web呈现与交互规范.md:128` 已改为"以 21 的定稿为准…不再 provisional"。
   复现：`sed -n '73p' specs/21-WorkCase-工作项.md; sed -n '128p' specs/10-Web呈现与交互规范.md`。
   性质：10 侧修复未镜像到 21；任何从 21 反向核对 10 的读者会读到对 10 现状不实的引述。21 虽声明"引用同步不由本文执行"，但作为 10 的审核方应显式指出此残差，建议 21 §3.3 改写该描述句（或登记待 10 同步的冲突点更新）。

2. **[F-B / 中–低] 08 §6 与 10 §6.5(c) 的呈现指令重叠（职责边界残留）**
   引文：`specs/08-DSH环境接入与插件发布规范.md:126`"设置页对可修复状态…呈现'更新'操作…`conflict` 或 `unavailable` 不可修复状态只呈现原因"；`specs/10-Web呈现与交互规范.md:144`(c)"可修复与不可修复状态须在交互上可区分，前者提供修复入口、后者只呈现原因"。
   复现：`sed -n '126p' specs/08-*.md; sed -n '144p' specs/10-Web呈现与交互规范.md`。
   性质：任务提示的"安装事务 UI 归 10 还是 08"问题——08 §6 作为宿主接入规范，本应只定义事务与状态闭集，却写进了"呈现'更新'操作/只呈现原因"的 UI 措辞，恰与 10 §6.5(c) 重复。判定：设置页安装流程是宿主 UI 面，08 §6 对状态驱动 UI 的权威描述可理解，但其"呈现"措辞与 10 的 Web 呈现约束构成双描述，存在漂移风险（08 改状态值则 10 §6.5(c) 须同步）。建议二选一收敛：或 08 §6 改为"设置页对可修复/不可修复状态分别提供更新操作/仅说明原因（UI 细节归 10）"，或 10 §6.5(c) 明确"可修复/不可修复的区分标准以 08 §6 状态闭集为准"。

3. **[F-C / 低，静态，标注"并发改动"] 四层阅读器两项标签与"尚未初始化"在 `src/` 未落地**
   引文：`specs/10-Web呈现与交互规范.md:99/105`（`卡片网格`/`语义详情`）、`10:161`（"尚未初始化"）。
   复现：`grep -rln "卡片网格\|语义详情" plugin/web/src` 无结果；`grep -rn "尚未初始化" plugin/web/src` 无结果。
   性质：能力真实性仅静态核验。`卡片网格`/`语义详情` 仅在 `dist/` 打包产物与 `docs/` 出现，`src/` 源码未含该命名；`尚未初始化` 标签在源码缺失。因另一会话正并发改 `plugin/web/`，本项仅作信号，不作为确认不一致，待实现稳定后再核。

4. **[F-D / 中–低] §12 反过度设计红线未覆盖 10 的真实过度设计风险（过度动画、信息过载）**
   引文：`specs/10-Web呈现与交互规范.md:220-226`（红线 1–5：互锁/框架/绕过登记/直写事实/重复提请 Gate）。
   复现：`sed -n '220,226p' specs/10-Web呈现与交互规范.md`。
   性质：维度 7 反例核心。10 的实际过度设计风险是"过度动画、信息过载、视觉噪音"，而 §12 五条红线无一条针对呈现密度/动效。建议补一条"不引入非必要的动画、过渡或信息密度超过 Human 单屏可决范围的呈现"。

5. **[F-E / 中] 修复闭环缺口：修复已落地但无事后对抗审核，且 10 三态未独立证实**
   引文：修复提交 `0a1b2d3`（2026-09-12 20:22）；既有审核 `docs/review-10.md`/`docs/review-10-vs-audited-set.md` 均描述修复前状态；`01 §6.3` 三态（设计草案/设计基线/当前规则源成员）。
   复现：`git show --stat 0a1b2d3 | grep specs/10`；`git log --oneline -- specs/10-Web呈现与交互规范.md`。
   性质：审计报告未覆盖实际已发生的改动（它们早于改动）；修复正确性由本报告首次独立核验，发现 F-A/F-B/F-D 等新残差，证明"修复后再审"确有价值且此前缺失。

### 已排除（核查后无问题）
- 10 §6.5 是否仍双定义 08 §6：**否**。当前 `10:144` 显式"不复述"，仅留呈现约束。旧审核 F1/D4 的"近重复复述"已修复。
- 10 §6.2 是否仍与 21 §3.3 冲突：**正向不再冲突**（10 已改）；但 21 反向引用过期（见 F-A，属反向问题，非 10 自身矛盾）。
- 10 是否重新定义 Output Envelope：`10:139` 明示不重新定义，§9 以 `00 §7.4` 为一致性基准。排除。
- 10 是否越权定义管辖判定/Hook 算法：§3.3/§6.5/§7 均让权 07/08。排除。
- §2 精确章节缺口：已补 `00 §7.2/§7.3` 与 `02 §6/§9`。排除旧 A2。
- 固定八章/编号连续/术语混用：均通过。排除。

---

## 4. 反例清单（维度 7）

- **R1（呈现隐含语义选择）**：10 宣"Web 只呈现，不定义业务语义"（`10:29`），但呈现本身即语义选择——如 `10:101`"支持关联计数"把对象间关系压成一个数字，属语义决定；`10:95`"不渲染关联关系"与 `10:101`"关联计数"未界定"计数算不算渲染关系"。该约束可被规避而无显式护栏。建议 §3.2/§5 补一句"呈现层的聚合/计数/合并显示不得改变对象归属或状态语义"。
- **R2（§6.2 触发与 21 冲突的优先级）**：10 §6.2 改"以 21 定稿为准"。若未来 21 修订 Gate 语义与 10 §6.2 表述冲突，谁优先？当前 10 以"21 现行文本为准"完全让权，未声明 21 内部修订时的回溯机制；属可接受的弱项（让权清晰），但无版本锚点。
- **R3（08 §6 安装 UI 归属）**：见 F-B。任务所问"安装事务必然有 UI，归 10 还是 08"——现两规范都写了该 UI 的同一行为。判定：08 §6 拥有状态闭集与事务权威，其"呈现'更新'/只呈现原因"可视为对宿主设置页 UI 的权威描述；10 §6.5(c) 是 Web 通用呈现约束。边界可辩护但**双描述**存在漂移风险，需收敛一方。
- **R4（反过度设计红线覆盖缺口）**：见 F-D。§12 五条红线针对"结构/框架/登记/写事实/重复 Gate"，**未覆盖** 10 最现实的过度设计面——动效与信息密度。反例成立：若实现引入全屏转场动画或单页堆叠 50 张卡片，§12 无一条可据以驳回。

---

## 5. 跳过的维度与依据（维度 9 自陈）

- **运行时呈现核验（整体跳过）**：本审核**未启动页面、未截图、未跑 `pnpm dev`**。依据：任务授权为"只读不写、静态核验"；且 `plugin/web/` 正由他会话并发修改（26 §9 配色），运行时产物不稳定。故维度 4（能力真实性）与维度 9（审核是否匹配风险）仅能做到"文本自洽 + 静态源码对应"，**不能核运行时呈现是否符合规范**。
- **08/09 文件正文正确性（跳过）**：仅把 08 §6、07 §* 作为 10 的引用目标核验其存在性与表面相符，未独立审计 08/09 自身正确性。
- **03/04/05/06 正文（跳过）**：10 对其均为路由型划界引用，仅核验被引章节存在，未通读。
- **21 全文承接性（跳过）**：仅核 21 §3.3/§10 与 10 的对应，未对 21 做承接性审计。
- **全量测试套件（跳过）**：仅跑 `parseSpecDocument` 机械解析，未跑 `plugin` 测试。

---

## 6. 风险档位反向核对结论

- **若 10 错，后果**：Human 依据错误/误导的 Web 呈现做授权、验收、登记决定 → 中等级别（Human 面、但受 07/08 权威与 §9 验证约束，非 catastrophic）。
- **文本自洽残差概率（修复后）**：低–中。旧两处硬伤已修；新发现 F-A/F-B/F-D 为局部中–低残留，不撼动 00/01/02 权威、不缩小 Human Gate/Stop Conditions 根部。
- **运行时呈现错误概率**：**高不确定性**（不可核）。F-C 显示四层读者两项标签与"尚未初始化"在 `src/` 缺失，结合实现在途，运行时与规范一致性的残差风险**未消除**。
- **反向核定风险档位**：**中（medium）**。文本侧风险因修复与本审核下降至低–中；但运行时侧风险敞口高且本审核无法收敛。综合维持"中"档，且明确：本审核降低的是**文本**风险，不降低**运行时**风险。

---

## 7. 未覆盖范围与保证边界

- **未覆盖**：08/09 自身正确性；03/04/05/06 正文；21 全文；运行时页面呈现；全量测试。
- **保证边界**：结论基于 `dev` 分支当前工作树快照（10 = 226 行，含 `0a1b2d3` 修复）。若 07/08 后续改 §5/§6 而未同步 10，F-B 漂移风险将实际化；若 21 不改 §3.3，F-A 反向过期将持续。本审核不预判未来变更。
- **机械证据**：`parseSpecDocument` 通过；`git show 0a1b2d3` 证实两笔关键修复；`grep`/`awk` 枚举见独立性披露。

---

## 8. 独立性披露

- **与起草隔离**：本子代理未参与 10 起草；结论独立于 `docs/review-10.md`、`docs/review-10-vs-audited-set.md`（二者描述修复前状态，本审核描述修复后状态并发现新残差）。
- **与既有审核的分歧点**：
  1. 既有两份审核把"§6.5 复述 08 §6""§6.2 的 21/34 provisional"列为**已证实不一致**；本审核确认这两笔已在 `0a1b2d3` **修复**，当前文本不再构成该冲突——即既有审核的"问题"已被解决，但其文档未覆盖该解决。
  2. 既有审核未发现的**新残差**（本审核独立发现）：F-A（21 §3.3 反向过期）、F-B（08 §6 呈现措辞重叠 10 §6.5(c)）、F-D（§12 红线缺过度动画/信息过载）、F-C（src 未落地标签）、F-E（修复无事后审核闭环）。
- **可复现命令**：
  - `sed -n '73p' specs/21-WorkCase-工作项.md; sed -n '128p' specs/10-Web呈现与交互规范.md`（F-A）
  - `sed -n '126p' specs/08-DSH环境接入与插件发布规范.md; sed -n '144p' specs/10-Web呈现与交互规范.md`（F-B）
  - `sed -n '220,226p' specs/10-Web呈现与交互规范.md`（F-D）
  - `grep -rln "卡片网格\|语义详情" plugin/web/src; grep -rn "尚未初始化" plugin/web/src`（F-C）
  - `git show --stat 0a1b2d3 | grep specs/10`（F-E）
  - `node -e 'import("node:fs").then(async fs=>{const {parseSpecDocument}=await import("./plugin/lib/spec-registry.js");const t=fs.readFileSync("specs/10-Web呈现与交互规范.md","utf8");console.log(parseSpecDocument(t,"specs/10-Web呈现与交互规范.md"))})'`
- **局限**：语义裁量（F-B 是否"双描述"、R1 是否"可规避"）为主观判断；运行时与并发实现状态不可核。

---

## 9. 总体结论

**有条件合格。**

- 10 旧两处硬伤（§6.5 双定义 08 §6、§6.2 的 21/34 provisional 冲突）已在 `0a1b2d3` 修复，当前文本在结构契约、§2 精确章节、V/HV 名称、引用存在性、防自欺边界上**通过**机械与语义核验；§6.5 对 08 §6 的引用**现为路由型**（解决了两份既有审核的相反结论争议）。
- 但本独立审核新发现 **5 项须处理残差**：F-A（21 §3.3 反向引用过期，跨规范不一致）、F-B（08 §6 与 10 §6.5(c) 呈现指令重叠）、F-D（§12 反过度设计红线未覆盖过度动画/信息过载）、F-C（四层读者两项标签与"尚未初始化"在 `src/` 未落地，标注并发改动）、F-E（修复无事后对抗审核闭环、10 三态未独立证实）。
- 以上均为**局部、低–中级**缺陷，不撼动 00/01/02 权威、不改变 07/08 唯一权威声明、不缩小 Human Gate/Stop Conditions 根部；修复集中于：更新 21 §3.3 反向引用、收敛 08 §6/10 §6.5(c) 的呈现边界、补 §12 红线、并就 `0a1b2d3` 修复做一轮事后对抗审核闭环。
- 风险档位反向核定为**中**（文本侧低–中、运行时侧高不确定且本审核不可收）。

**处置建议**：在 `21 §3.3` 反向引用过期与 `08 §6`/`10 §6.5(c)` 边界重叠两项收敛前，10 维持"有条件合格"；其余为建议性收紧，不阻断当前成立。
