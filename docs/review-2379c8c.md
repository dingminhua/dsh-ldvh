# 提交 `2379c8c` 独立对抗复审报告

- **审核对象**：提交 `2379c8c33a1ebeef47cfbf53fe8e509cd1847331`（`fix(specs): 04 §9.3 补必填结构关系说明——消除十项即全部骨架的误读`），改动 `specs/04-业务系统基础规范.md`（9 插入 / 2 删除）。
- **审核性质**：依 `01 §12.4` 补做的独立对抗复审（第 4 轮累积审计 F3/I-E：该提交改变可强制执行规则却无独立复审记录）。本报告即为该复审记录的入档。
- **操作边界**：只读 specs/、plugin/、docs/ 与 git 历史；仅写本文件；未修正任何发现；未 git commit。
- **独立性**：见 §8。

---

## 0. 机械核验总表（全部可复现）

| # | 核验项 | 命令 | 结果 |
|---|---|---|---|
| 0.1 | 完整 diff | `git show 2379c8c` | 逐行通读。**实际含三处改动**：① §9.3 新增「本十项与其它必填结构的关系」说明；② §9.3 新增「判定粒度说明」；③ **扩展规则 5 被实质改写**（`definition_ref` 澄清）——提交信息**未披露** ③（见 P2）；④ §10 套用结果表第 7 项更新 |
| 0.2 | §9 标题结构 | `grep -n '^### 9\.\|^## 9\.' specs/04-业务系统基础规范.md` | §9.1(279)/§9.2(290)/§9.3(300)/§9.4(333)，与提交信息所述位置一致 |
| 0.3 | 01 固定章节要求 | `grep -n '固定前置\|固定收尾\|逐字' specs/01-规范模型基础规范.md` | 01:129「前四个 H2 与末尾四个 H2 的编号和标题固定」「固定标题文字必须逐字保留」；§7.1(131)/§7.3(156) |
| 0.4 | 声明落点现状 | `grep -n '业务系统声明' specs/04-*.md specs/30-*.md specs/31-*.md` | 04:292（§9.2 要求**唯一 H3**）；30:73 `### 业务系统声明`（H3，合规）；31:113 `### 业务系统声明`（H3，合规） |
| 0.5 | 提交时点的 §9.2 | `git show 2379c8c:specs/04-业务系统基础规范.md \| sed -n '288,292p'` | §9.2 在该提交时已要求「唯一的 H3 标题」——新增说明是对既有规则的澄清，未改 §9.2 本身 |
| 0.6 | H2 误置历史考证 | `git log -S'## 1. 系统定位与价值' --oneline -- specs/31-讨论系统规范.md`；`git show 70e51e8 --stat` | 70e51e8 引入 `## 1. 系统定位与价值` 等（十项替换前四章），644eafe「31 号恢复 01 §7 固定章节」移除——§10 第 7 项「已致实际违规…已在本轮修复中恢复」的陈述**属实** |
| 0.7 | 30 号 H2 误置 | `git show 70e51e8 -- specs/30-调研系统规范.md \| grep 业务系统声明` | 提交信息明言「业务系统声明按 04 §9.2 修正为唯一 H3 形式（原误置于 H2）」——两起误置均发生在 70e51e8 之前，70e51e8 已机械修复，2379c8c 是**规则层**补救 |
| 0.8 | 2379c8c 后续改动 | `git diff 2379c8c HEAD -- specs/04-业务系统基础规范.md` | 新增文字（关系说明/粒度说明/规则 5）**自 2379c8c 起未再被改动**；后续提交改的是 Helper→LDVH CLI 更名、20–26→20–27、§10 行标签、§1 负担段 |
| 0.9 | parseSpecDocument 全量 | 以**文件内容为第一实参、`specs/<f>` 为第二实参**逐份调用 `plugin/lib/spec-registry.js`（25 份） | 22 份 OK；3 份 FAIL：20（fence_missing）、24（title 含「/」）、25（fence_missing）。经 `git show 2379c8c:specs/{20,24,25}-*.md` 复核，**三者在 2379c8c 时点即失败，与本提交无关**（本提交只改 04，04 解析通过）。注意 API 陷阱：若把路径当第一实参会全量误报 `fence_missing`（本审核首次运行即踩中，已纠正） |
| 0.10 | 插件测试 | `cd plugin && node --test test/*.test.mjs` | 558 tests：**553 通过 / 5 失败**。5 个失败文件（adr/friction/pitfall/research/spark-tools.test.mjs）全部因 `ERR_MODULE_NOT_FOUND`（模块解析指向 `/Applications/DSH Desktop.app/.../dsh-tools`），属**环境性失败，与本提交无关**（2379c8c 未触及 plugin/）。`spec-registry.test.mjs` 单独运行 **69/69 通过** |
| 0.11 | 机械校验覆盖 | `grep -rn '业务系统声明\|固定前置\|固定收尾\|价值判断' plugin/lib/ plugin/test/` | **零命中**——plugin 无任何对声明 H3 唯一性或固定八章的机械校验（见 D2） |

---

## 1. A——新增内容的正确性

### A1.「十项不是全部必填结构」的声明：**准确（已证实）**

- `01 §7`（01:129）确凿要求：「除 00 外，普通规范前四个 H2 与末尾四个 H2 的编号和标题固定」；§7.1 逐字列出 `## 1. 价值判断`、`## 2. 规范依据`、`## 3. 职责边界`、`## 4. 适用范围`；§7.3 逐字列出 `验证与证据边界`、`Human Gate`、`Stop Conditions`、`反过度设计红线`，且「固定标题文字必须逐字保留在标题中」。30/31 均为普通规范，受其强制。✓
- `04 §9.2`（04:292）确凿要求「唯一的 **H3** 标题 `业务系统声明`」，且该要求在 2379c8c 时点即已存在（0.5）。✓
- 因此「满足十项不足以形成合规规范」的核心判断成立，且 `04 §10` 套用结果表第 2/7 项的实证（30 号声明误置 H2、31 号十项替换前四章）佐证该误读**实际造成过违规**。
- **但前置四章的转述有一处失实**：「各含规定 H3」（04:306）——`01 §7.1` 第 1/2 项（价值判断、规范依据）按**段落顺序**规定，**无任何 H3 要求**；只有第 3/4 项（职责边界、适用范围）规定 H3（§3.1–3.3、§4.1–4.3）。直接反例：31 号 §1/§2 无 H3 且被判合规（31:26–36 之间无 `###`）。→ **P1**。
- **列举完备性存疑**：说明以「还**同时**必须满足：1… 2…」的穷举式语气列出两类，未含 `01 §6` 身份块与 `01 §8` 编号登记（两者同为「成为规范」的必填结构）。未自称穷举，故非硬错误。→ **Q2**。

### A2.「判定粒度说明」：**成立，未削弱原规则（已排除 + 一处措辞张力）**

- 原句（04:302）：「10 项之间不规定固定线性顺序，但**每一项都必须出现且只能以一项对应**」。读原文判断：其紧邻上文是「下列**十项**共同结构」，「每一项」的语法主语确为十项，释义「每项在正文中有唯一落点」是可辩护的读法，且与前一提交 ad93781 对 §10 第 1 项的复核结论一致（该结论先于本说明独立得出）。
- **未削弱**：反方向（多章并一项、删并跳过）由扩展规则 1「不得删除、合并、跳过或降低」继续禁止；粒度说明放开的是「一项多章衔接展开」，并**新增**锚点章显式标注归属的义务——方向上是**强化**可验证性。两项合起来对两个方向都有覆盖。
- 残留张力：「并列的必填集合」（04:306）未说明十项落点可否**位于固定章之内**，而 30/31 的现行实践恰是嵌入式（31 第 2 项→§3.4、第 8 项→§13；30 第 1 项→§1.1、声明置于 §1 内）。严格读「并列」可判两实例不合规。→ **Q1**。

### A3. 两类列举：**不重复、无交叉，但各有一处精度问题**

固定八章（01 强制）与 H3 声明（04 §9.2 强制）确为互不重复的两类。精度问题即 P1（H3 表述）与 P4（引用不精确，见 B/C）。

---

## 2. B——是否越权

### B1. 04 是否有权解释 01 §7：**有权澄清关系，无权改写内容——本提交基本在权限内，P1 是唯一越界点**

- `01 §7.2` 明文指派：「基础模型规范还必须定义其下属规范共同结构、必填内容和扩展规则：……**04 定义业务系统规范（30–35 段）的共同结构**」。说明十项与 01 固定章节的关系，正是该指派职责的题中之义。
- 新增说明的主体是指针（「这八章由 `01` 强制」），把内容权威留给 01，不构成 00 §3.6 意义上的「改变、重新解释」；与 04 §2 自我承诺「本文不重新解释 00/01/02」总体相容。
- **唯一越界点即 P1**：「各含规定 H3」为 01 §7.1 未要求的第 1/2 章增加了 H3 义务——这属对上位规则要求的**失实转述**，严格读者会据此判 31 号（§1/§2 无 H3）不合规。方向是加严而非放宽。

### B2. 是否新增/降低 01 §7 义务：**对 01 §7 新增了（失实的）义务（P1），未降低；扩展规则 5 的改写降低的是 04 自身义务（权限内但未披露）**

- 扩展规则 5 改写（0.1 之 ③）把「技术细节……**必须回指到唯一 H2/H3 的 `definition_ref`」放宽为「不要求技术细节逐项回指……只需保证 (a) 整体唯一 `definition_ref`；(b) 不改变 00 §3.1 职责、不引入十项与 01 §7 之外的强制要求」。这在**实质上改变了一条可强制执行规则**。就实体而言该改写与 §9.2 对 `definition_ref` 的定义（「每份规范一个的登记入口」）一致、消除了 §10 第 6 项确认的真实歧义，**方向可辩护**；但它属 04 对自身规则的下调，且**提交信息完全未披露**（见 P2）。
- 另注意规则 5(b) 的枚举「本节十项与 `01 §7` 之外」与关系说明的必填集合（十项 + 01 §7 + §9.2 声明）**不一致**——漏了声明与 04 §5–§8/§13。极轻，随 P2 一并清理。

### B3. 与 01 §7.2 相容性：**相容（已排除）**

04 定义共同结构的义务由 01 §7.2 直接指派；本说明是该义务的履行而非另立权威。

---

## 3. C——是否引入新问题

### C1. 与 04 自身条文的冲突

- 与 §9.1 准入条件：无冲突（已排除）。
- 与 §9.3 扩展规则 1–4：无冲突（规则 1 禁合并、粒度说明放开一项多章，两方向互补；「不属系统扩展」的归类与规则 2 相容）（已排除）。
- 与 §10 验证表：**有残留缺口**——「30–35 共同结构」行的成立条件仍是「十项必填结构可定位、准入条件重检通过、扩展未挤占必填结构」（04:351），**未纳入固定八章与 H3 声明**。验证入口仍停留在「十项」视野，恰是本提交要消除的误读在验证层的残留。→ **P3**。
- §10 套用结果表簿记不一致：第 7 项标注了「本项已在 §9.3 补关系说明」，但第 2 项（结构性遗漏）与第 6 项（规则 5 表述）**同样被本提交实质处理**（关系说明对应第 2 项、规则 5 改写对应第 6 项）却未标注；收尾段「成立的 4 项为待修订素材」在本提交后已过时（真正未修的只剩第 5 项）。→ **P2**。

### C2. 与 01 §7/§8/§9/§10/§12 的冲突

- §7：P1（H3 失实转述）。
- §8：无冲突（「编号随领域正文连续顺延」与 01 §7.3 一致；对前置四章而言该语不准确——01 §7 规定前四章编号固定 1–4——极轻，见 Q3）。
- §9：说明第 2 条的括注「（`01 §9.2` 成立条件适用）」引用不精确——`01 §9.2` 是规范成立 12 条件，**并未提及业务系统声明**；声明的强制力来自本文 §9.2 自身。该括注易被读作上位依据。→ **P4**。
- §10/§12：无冲突。

### C3. 后续提交是否使其部分过时

- §9.3 新增文字本身：**未过时**（0.8，自 2379c8c 未被改动，现行有效）。
- f82bf01 已把 §10 行标签「尚无实例」更新为「已建立 30/31 两份实例」——注意**在 2379c8c 时点该标签仍是陈旧的**（30/31 已建立多提交），即该提交自身遗留了一处内部不一致，后被 f82bf01 修复。
- 52e7a92 在 §1 新增的负担段引用「§9.3 的十项结构与 `01 §7` 的固定八章」——与本说明一致，无冲突。
- **仍需同步的后续项**：见 §6。

---

## 4. D——补救的实效性

### D1. 能否防止「声明误置 H2」重演：**规则文本层有效，机械层未闭合（部分有效）**

- `04 §9.2`（04:292）确凿明文要求 **H3**（非 H2）。✓
- 新说明**明确指向** §9.2，且以「（**不是 H2**）」加粗强调，并冠以「（重要，避免遗漏）」。✓
- 现行两消费者（30:73、31:113）均为唯一 H3——误置未复发。✓
- 但防复发手段仅为文本：plugin 中无任何机械校验（0.11 零命中），§10 验证行成立条件未含声明检查（P3）。

### D2. 可机械核对的判据：**判据存在且可机械核对，但无机械实现——残留缺口在实现层**

「唯一 H3 标题 `业务系统声明`」本身是**完全可机械核对**的判据（标题级别 + 全文唯一性，`grep -c '^### 业务系统声明'` 即可判定；固定八章的标题逐字保留同理）。残留缺口不在判据层而在实现层：plugin 的 spec-registry 只解析身份块，不做章节结构校验。若某规范仍把声明置于 H2，当前只能靠人读发现。

---

## 5. 问题清单（分级 + 依据 + 复现）

### 已证实问题

| 编号 | 级别 | 问题 | 依据与复现 |
|---|---|---|---|
| **P1** | **重要** | 「各含规定 H3」失实：`01 §7.1` 只为第 3/4 章规定 H3，第 1/2 章（价值判断、规范依据）按段落顺序规定、无 H3 要求。该表述为上位规则**增加了**义务，严格照此核对会误判 31 号不合规（其 §1/§2 无 H3） | 04:306 vs 01:131–137。复现：`sed -n '131,137p' specs/01-规范模型基础规范.md`；`sed -n '26,36p' specs/31-讨论系统规范.md`（两 H2 间无 `###`） |
| **P2** | **重要** | 提交信息**未披露**扩展规则 5 的实质改写（放宽「技术细节必须回指 definition_ref」为整体唯一 + 两条件），且 §10 套用结果表簿记不一致：第 2/6 项与第 7 项同样被本提交处理，仅第 7 项标注「已补」；收尾段「成立的 4 项为待修订素材」已过时（实余第 5 项） | `git show 2379c8c`（第三个 hunk，`-5.…+5.…`）；04:330、363–375 |
| **P3** | 中等 | §10 验证表「30–35 共同结构」行成立条件仍只覆盖十项，未纳入固定八章与 H3 声明——验证入口仍编码着本提交要消除的「十项即骨架」视野 | 04:351。复现：`grep -n '十项必填结构可定位' specs/04-业务系统基础规范.md` |
| **P4** | 轻微 | 「（`01 §9.2` 成立条件适用）」引用不精确：01 §9.2 未提及业务系统声明，声明强制力来自本文 §9.2；括注易被误读为上位依据 | 04:307 vs 01:282–303（§9.2 全文无「声明」字样） |

### 存疑

| 编号 | 问题 | 说明 |
|---|---|---|
| **Q1** | 「并列的必填集合」与嵌入式落点的解释张力 | 严格读「并列」可判 30/31 不合规（31 第 2 项→§3.4、第 8 项→§13；30 第 1 项→§1.1、声明在 §1 内）；宽松读（集合并列、非空间互斥）则相容。建议 §9.3 明示落点可否位于固定章内。复现：04:306；31:23（映射注记）；30:34（§1.1） |
| **Q2** | 两类列举的穷举语气 | 未含 01 §6 身份块与 01 §8 编号登记；「还同时必须满足：1…2…」易被读作完整清单。未自称穷举，非硬错误 |
| **Q3** | 「编号随领域正文连续顺延」对前置四章不准确 | 01 §7 规定前四章编号固定 1–4；该语仅准确描述收尾四章。极轻 |

### 已排除（经核验不成立）

| 编号 | 排除项 | 依据 |
|---|---|---|
| E1 | 「十项不是全部必填结构」核心声明不准确 | 0.3/0.4：01 §7 固定八章与 04 §9.2 H3 声明确为十项之外的强制结构 |
| E2 | 粒度说明削弱原规则 | A2：反方向由扩展规则 1 覆盖；粒度说明新增锚点标注义务，强化可验证性 |
| E3 | 整体越权（重新解释 01 §7） | B1：01 §7.2 直接指派 04 定义共同结构；说明主体是指针；P1 为唯一失实点 |
| E4 | 与 01 §7.2 不相容 | B3：正是指派义务的履行 |
| E5 | §10 第 7 项历史陈述不实 | 0.6：70e51e8 引入 `## 1. 系统定位与价值`、644eafe 恢复，经 git 考证属实 |
| E6 | 误置仍在发生 | 0.4：30/31 现行声明均为唯一 H3 |
| E7 | parseSpecDocument 失败与本提交有关 | 0.9：3 个失败（20/24/25）在 2379c8c 前即存在；04 解析通过 |
| E8 | 插件测试失败与本提交有关 | 0.10：5 个失败全为 `ERR_MODULE_NOT_FOUND` 环境性失败；2379c8c 未触及 plugin/；spec-registry 69/69 通过 |

---

## 6. 需同步的后续项

1. **修 P1**：04:306「各含规定 H3」改为「其中 `## 3.`/`## 4.` 各含规定 H3」（或删去该括注，回指 01 §7.1 原文）。
2. **修 P3**：§10 验证表「30–35 共同结构」行成立条件补入「固定八章齐备且标题逐字保留、唯一 H3 业务系统声明存在」。
3. **修 P2**：为 §10 套用结果表第 2/6 项补「已修」标注（与第 7 项同款），更新收尾段「成立的 4 项为待修订素材」；对规则 5 改写补一次显式披露（可在本报告入档提交信息或 04 修订说明中说明）。
4. **30 号陈旧指针**（与本提交无关、属 §9.3 消费者同步项）：30:36「system_key 见 **§5** 业务系统声明」——声明实际位于 §1 内（30:73）。复现：`grep -n '业务系统声明' specs/30-调研系统规范.md`。
5. **30 号 H3 编号错位**（观察项）：`## 5.` 下为 `### 6.1`、`## 16.` 下为 `### 18.1` 等，H3 编号与所属 H2 系统性错位。无现行规则明文禁止，但削弱「机械定位」与交叉引用可靠性，建议下轮 30 修改时顺带清理。
6. **Q1 裁定**：明确十项落点可否置于固定章内（现实践为可以，条文未明说）。
7. **（可选）机械校验**：plugin 增加声明 H3 唯一性与固定章标题的校验，闭合 D2 的实现层缺口。

---

## 7. 该提交是否达到其声称目的

**达到（规则文本层）**。「消除十项即全部骨架的误读」的目的在规范文本层面已实现：§9.3 现在明文声明十项不是全部必填结构、逐类列出并列必填集合、并以「（不是 H2）」直指曾实际致错的 H3 声明；两消费者现行结构合规；§10 第 7 项从「存在解释空间」升级为「已致实际违规 + 已补说明」的如实记录。**未完全达到（验证与实现层）**：§10 验证行未同步（P3）、无机械校验（D2）、且提交夹带了未披露的规则 5 改写（P2）——补救本身引入了新的簿记债。

## 8. 独立性披露

- 本审核由**独立子代理**执行（DSH Harness 委派，模型 glm-5.3），**未参与** `2379c8c` 及其前后修复链的任何形成环节（该提交 LDVH-Provider: workbuddy / LDVH-Model: deepseek-v4.1-flash，与本执行体无上下文共享）。
- 审核依据为 `dev` 分支当前工作树（`git status` 干净，HEAD = `52e7a92`）的规范文本 + git 历史的直接考证；**未采信** docs/ 既有报告的结论，仅用其定位 F3/I-E 背景——其「无独立复审记录」的判断经本报告直接 grep 复核成立（`grep -rn 2379c8c docs/` 仅命中原发现报告与任务表，均非复审）。
- 依 `01 §12.2` 披露边界：同一仓库工作区内执行的隔离会话审核，无法证明模型供应商层面的完全隔离；采用对抗姿态（只攻击、不复述），实际攻击方向为：上位条文逐字比对（P1）、提交信息与 diff 的完备性比对（P2）、规则语义削弱方向分析（A2/E2）、历史考证（E5）、机械复跑（0.9/0.10）。

## 9. 未覆盖范围与保证边界

- 本报告为**文本层对抗审核**：未通过 LDVH CLI 受控读取（`ldvh_read_specification_content`）获取回执，规范原文经直接文件读取获得；未访问 Human 对话历史、仓库外证据或 30/31 起草过程的会话记录。
- 「有条件」类结论指文本层判断，**不证明** 04 已具备完整当前规则源成员资格（I-D 等资格闭环事项不在本报告范围）。
- 插件测试的 5 个环境性失败（0.10）未做根因分析（超出本提交审核需要）。
- 30 号 §1 内 H3 细分与 `01 §7.1`「不得用子标题拆分该映射块」的关系、30 号整体结构合规性——属 30 号自身审核范围，本报告仅作观察记录，未判定。
- 32–35 尚未建立，本说明对未建系统的实际防误效果无法实证。

## 10. 总体结论

**有条件确认。**

- **该修改正确、可确认的部分**：核心声明（十项≠全部必填骨架）准确且必要；两类并列必填结构的列举方向正确；判定粒度说明是对既有歧义的可辩护澄清且未削弱原规则；与 01 §7.2 的指派义务相容、整体不越权；对已发生的两起误读（30 声明 H2、31 替换前四章）有明确的规则层防复发作用；其声称目的在规范文本层达到。
- **条件（须修复后才宜表述为完全确认）**：P1（对 01 §7.1 的失实转述，须改字面）；P2（未披露的规则 5 改写 + §10 簿记不一致）；P3（§10 验证行成立条件同步）。P4 与 Q1–Q3 建议随下轮修订一并处理。
- 本身即为 `01 §12.4` 要求的独立复审记录：**F3/I-E（2379c8c 缺独立复审）经本报告入档而闭合**；但 P1–P4 构成新的待修复发现，按 §12.4 须由主控核对处置，不得静默忽略。
