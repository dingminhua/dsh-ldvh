# 独立复审报告：specs/03-事实模型基础规范.md 的 I-1～I-5 修复

- **复审对象**：`specs/03-事实模型基础规范.md`（工作区未提交改动，基线 HEAD；分支 dev）
- **对应审计**：`docs/review-round3-00-01-02-03.md`（第 3 轮，项 I-1～I-5 / P4 / C-1～C-4 / A-1）
- **操作边界**：仅只读 + 仅写本文件；未修改任何 specs/、plugin/ 文件；未 git commit；未修正任何发现。
- **独立性披露**：见 §9。

---

## 0. 机械核验总表（全部可复现）

| # | 核验项 | 命令 | 结果 |
|---|---|---|---|
| M1 | 03 中「20–24 / 五类」清零 | `grep -nE '20[–-]24\|五类' specs/03-事实模型基础规范.md` | **0 命中**（exit 1）—— 确已清零 |
| M2 | 03 中「事实规范」次数 | `grep -c '事实规范' specs/03-事实模型基础规范.md` | **4**（行 66、70、98、138） |
| M3 | `fact_type_key` 逐类型实际值 | `grep -n 'fact_type_key' specs/2*.md` | 20=spark、21=workcase、22=adr、23=pitfall、24=research、25=不采用(goal_key)、26=friction、27=norm —— 与 03 §6.1 新值域**逐字一致** |
| M4 | `serves`/`serves_sg` 命名分裂 | `grep -n 'serves' specs/2[0156]-*.md` | 21=`serves`、25 引述 WC 的 `serves`；20=`serves_sg`（行 128/145/212/248/272）、26=`serves_sg`（行 124/137）—— 分裂实存，与修复声称一致 |
| M5 | 01 §7.3 是否真有「过渡口径」 | `grep -n '过渡' specs/01-规范模型基础规范.md` | **有**：行 172「过渡期内…既有成员在下一次实质修改或 Human 指定的迁移时点，按当时有效契约核对并修复」——修复 §11 的「01 §7.3 的过渡口径」引用**有效，非虚构** |
| M6 | `parseSpecDocument` 全量（`specs/<f>` 实参） | `node -e '...parseSpecDocument(text, f)...'` | 00/01/02/03/21/26/27 = ok:true；**20、25 = fence_missing**（`identity/fence_missing`，前轮已记的解析器口径问题，**非本修复引入**） |
| M7 | 规范注册测试 | `cd plugin && node --test test/spec-registry.test.mjs` | **69/69 通过**，0 失败 |
| M8 | 下游是否残留「20–24 / 五类」陈引 | `grep -rn '20–24\|五类' specs/2*.md specs/0*.md` | **残留 4 处陈引破 03 新表述**（见 §5 / E.2） |

---

## A. I-1 的正确性与完整性

**结论：已证实闭合（含 1 项残留下游陈引，记为 E.2，不影响 03 自身闭合）。**

1. **「20–24」「五类」是否真清零**——✅ 已证实。M1 返回 0 命中，覆盖 frontmatter scope（行 11）、§3.2 项 3（行 51）、§3.3（行 70）、§4.3（行 86）、§5.1（行 96）、§5.3（行 115）、§6.1（行 123/138）、§7.2（行 164/191/198/201）、§8（行 322）、§11（行 348/350/354/366/395/468）、§15.2（行 468）。16 处 + 标题 + frontmatter 全部改写，与 round3 表 0.1 对应。

2. **新口径「各事实类型规范（当前登记 20–27）」是否满足 01 §7.2**——✅ 满足，且已加防误读护栏。重读 `01 §7.2`（行 142）：「覆盖范围按职责确定，不按段号或 `parent_spec` 推定」「段号不同亦不自行免除约束」「该覆盖义务不因受辖规范的编号落在预留段之外而免除」。03 §11（行 350）改写为「覆盖范围按职责确定，不按编号段号或 `parent_spec` 推定（01 §7.2）」并显式列举「当前按 01 §8.1 登记为 20–27」。**残留风险（已排除/低）**：孤立地看「20–27」仍可能被误读为段号式推定，但紧跟的「不按编号段号…推定」护栏句已直接消解该误读；且「未来…段外扩展号…一经成立即落入」句把边界锚定在「职责+登记」而非段号。故不构成新推定。**已排除 A.2 的「又一种段号推定」疑虑。**

3. **§11 新增覆盖范围声明是否自洽**——✅ 自洽且与 01 §7.2 一致。§11（行 350）「预留号（28/29）上尚无规范成立，不产生义务；未来…无论占用预留号还是段外扩展号，一经成立即落入本覆盖范围，无需修改本文」。核对 `01 §8.1`（行 205）：预留号确为 28/29。该表述与 01 §7.2「不因段外编号免除」**同向且互补**——「28/29 不产生义务」是「未登记即无义务」的平凡结论，「未来成立即落入」正是「不因段外编号免除」的正面表述。**无冲突。**

4. **§11 过渡句（「01 §7.3 的过渡口径」）是否虚构**——✅ 非虚构。M5 确认 01 §7.3（行 172）确有过渡核对口径。修复引用有效。

5. **是否有该改而未改的「20–24」**——在 03 内部：**无**（M1）。但**下游 specs 残留 4 处陈引破 03 新表述**（见 §5 / E.2），属「该修而未修」的连带缺口。

---

## B. I-2 的正确性

**结论：已证实闭合（含 1 项轻微措辞过伸，记为 Sq-A，存疑级）。**

1. **§5.1 新增「事实规范」条目是否准确承接 00 §3.2/§3.3**——✅ 已证实，两道关键限定俱在。03 §5.1（行 98）原文：「事实对象承载规范内容，不使该对象、其载体或其路径取得规范效力（00 §3.3），事实也不能反向创造或放松规则（00 §3.2）」。逐句比对 00：
   - 00 §3.3（行 132）：「事实对象本身不因存放位置而取得规范效力」→ 03 转述为「不使该对象、其载体或其路径取得规范效力」✅ 覆盖。
   - 00 §3.2（行 128）：「事实不能反向创造或放松规则」→ 03 逐字引回 00 §3.2 ✅ 覆盖。
   - 00 §3.3「以事实对象形态承载于事实源」→ 03 §3.3（行 70）与 §5.1 均承接 ✅。
   两限定**全部在场**，无遗漏。

2. **§3.2 新增「不负责项」（项 8，行 66）是否缩小/扩张 03 职责**——✅ 未越界。项 8 将「事实规范的规则内容语义、方向体系与元规范/事实规范两级归属判断」分别归 00 §3.3 / 01 §9.0 / 27，声明 03「只为承载规范内容的事实对象提供公共模型约束」。这与 03 的「事实公共模型」职责一致，且明确把内容语义推给 27 —— **缩小的是 03 对规范内容的主张，符合职责分工，未扩张。**

3. **§3.3 为 27 留的「权威位」是否与 27 实际声明一致**——✅ 基本一致（见 Sq-A 轻微过伸）。03 §3.3（行 70）：「27 是事实规范作为事实对象的类型权威（00 §3.3：事实规范以事实对象形态承载于事实源）」。核对 27 §3.3（行 65）：27 声明自身为「Norm 类型自身的边界、字段、方向清单、状态、关系与消费点」的唯一权威；27 确用 `fact_type_key`=`norm`（行 124/138）。当前 27 是唯一的「事实规范」承载类型，故「27 是事实规范…类型权威」作为**当前态描述成立**。

4. **§6.1「事实规范同样承接本表公共身份，其 `fact_type_key` 即 `norm`」是否与 27 一致**——✅ 一致。27 实际 `fact_type_key`=`norm`（M3），且 03 §6.1（行 138）补句「承载规范内容不改变字段公共语义，也不使对象取得规范效力」与 27 §3.3（行 65「不复制相邻规范规则」）及 00 §3.3 同向。

**Sq-A（存疑·轻微）**：03 §3.3「27 是事实规范**作为事实对象**的类型权威」一语，把「事实规范（两级承载中的一类）」与「27 Norm（一个具体类型）」做了等同化表述。严格地说 00 §3.3 的「事实规范」是**承载层级概念**（与元规范并列），27 是该层级下**当前唯一**的类型实例；若未来出现第二个事实规范类型，该句需修订。当前态可接受，记为存疑级提示，不阻断闭合。

---

## C. I-3 的正确性

**结论：已证实闭合。**

1. **七个短名是否与各类型规范逐字一致**——✅ 已证实。03 §6.1（行 123）新值域：`spark`(20)、`workcase`(21)、`adr`(22)、`pitfall`(23)、`research`(24)、`friction`(26)、`norm`(27)。M3 机械核验各类型实际登记的 `fact_type_key`：**全部逐字匹配**（20=spark / 21=workcase / 22=adr / 23=pitfall / 24=research / 26=friction / 27=norm）。无列不存在的类型，无遗漏已登记类型。

2. **25 单例豁免是否与 25 实际一致**——✅ 一致。03 §6.1（行 123）注明「25 Goal 为单例类型，声明不采用本字段（见本行出现条件栏的单例豁免）」，并指向出现条件栏「25 号先例：Goal 以 `goal_key` 固定值 `project-goal` 替代本字段」。核对 25 §5（行 77）：`fact_type_key` = 不采用，`goal_key` 固定值 `project-goal` —— **逐字一致**。

3. **是否遗漏/多列类型**——✅ 无。八类事实类型（20–27）中七类用 `fact_type_key`、一类（25）豁免，与 01 §8.1（行 205）登记及 `specs/2*.md` 实际文件完全对应。

---

## D. I-4 的正确性与风险

**结论：有条件闭合（锚点定义成立；命名统一宣布但未执行，构成「悬空要求」，记为 D-1 已证实问题）。**

1. **「锚点型非 relations 字段」定义是否与 01.Att.03 及 03 §7.2 既有 relations 规则相容**——✅ 相容，且未制造第二套关系契约。03 §7.2（行 198–201）将锚点型字段**显式定义为「不进入 `relations`」「不参与本节关系条目的证据 locator 闭集」**，即它本身就是 `relations` 之外的独立标量形态，而非 `relations` 的第二种形状。核对 `01.Att.03`（规范关系与层级表）：该附件只管辖**规范间关系**（`parent_spec`/`relation`/`basis`/`supersedes`/`related_specs`，见其 §1–§3），与事实对象层 `relations` 字段（03 §7.2 的管辖域）是**两层不同对象**，无交叉约束。故锚点定义不与 01.Att.03 冲突，也不与 03 §7.2 既有的 `object_uid` 关系契约冲突。

2. **命名统一决定 `serves` 的推理是否成立**——✅ 推理成立（核验 M4）。25 确实用 `serves`（25 引述 WC 的 `serves`，25 §9/§11）；21 确实用 `serves`（21 §130/§234）；20 确实用 `serves_sg`（20 §128）；26 确实用 `serves_sg`（26 §124）。03 判定「20/26 的 `serves_sg` 属同义命名分裂」**与事实一致**。且 21 §285 已明示「命名是否统一由 03 侧公共契约决定；本文暂与 25 号保持一致」——即 21 **已将命名决定权显式委托给 03**，03 据此宣布 `serves` 为公共登记名**在其既有 §6.1 跨类型共享字段权威（行 138「跨类型共享字段的统一身份…由本节承接」）范围内，不构成越权**。

3. **「未完成同步前既有字段继续有效、语义不变」是否规避修改义务（悬空要求）**——⚠️ **D-1（已证实·中等）**：03 §7.2（行 201）宣布「指向 `goal.md` SG-n 锚点的服务归属字段，公共登记名为 `serves`」，同时登记「20/26 现行登记的 `serves_sg` 属同义命名分裂，统一到公共登记名**须同步修改相应类型规范**……**未完成同步前既有字段继续有效、语义不变**」。这是一项**已宣布但自身未执行的统一要求**：03 定义了公共名 `serves`，却**未改 20/26**，把修改义务推给「未来同步」。结果是 03 自身新立的命名纪律与 20/26 现行 `serves_sg` **当场不一致**，且 03 用「继续有效」条款使该不一致合法化。**这不构成规则违反**（它明确保留过渡有效性），但构成「悬空要求」：规范宣布了统一，却未闭合统一，留下 20/26 与 03 §7.2 自身体例的长期背离。建议：要么在 03 内直接登记 20/26 的同步任务为待办并设 Human 迁移时点，要么本轮一并修改 20/26（`serves_sg`→`serves` 含字段表/验证/迁移条目）。当前状态判为**有条件闭合**。

4. **该定义是否越权**——✅ 未越权（见 D.2 分析）。03 仅对**跨类型共享**的锚点字段作命名纪律裁决，且该字段确为 20/21/26 三类共用（同指 goal.md SG-n），属 §6.1「跨类型共享字段的统一身份、定义位置、适用类型、登记与提升规则由本节承接」的明示授权范围。对**类型专属**字段（如 20 的 `question`、26 的 `phenomenon`），03 未置喙。

---

## E. I-5 与连带

**结论：I-5 随 I-1 闭合；但修复**未**引入新四方不一致，却**残留/未修**下游陈引（E.2），并暴露一处修复前的既有缺陷（E.3）。

1. **§3.3 / §11 约束是否扩到 25–27**——✅ 已扩。§3.3（行 70）改「各事实类型规范（当前登记 20–27）」并含 27 权威位；§11.2（原 §11.2 第 7 项，现行 396 附近）改「各事实类型规范（当前登记 20–27）不得新增…」；§11.3 第 5 项改「各事实类型的编号与职责以 01 登记和 Human 决定为准」。25–27 已被纳入共同结构约束。

2. **是否引入新的不一致（与 00/01/02/04 或 20–27）**——⚠️ **E-2（已证实·中等·该修而未修）**：修复只改 03，但**下游 4 处规范逐字引用了 03 的旧表述「20–24」**，修复后这些引用变成**悬空误引**（所引 03 文本已不存在）：
   - `specs/21-WorkCase-工作项.md:44`：「03 §11 定义的 **20–24** 事实类型规范共同结构」—— 03 §11 现称「全体事实类型规范（20–27）」。
   - `specs/25-Goal-项目目标.md:82`：「03 §6.1「**20–24** 分别登记本类型实际采用的公共字段」」—— 03 §6.1 现称「各事实类型规范（20–27）分别登记」。
   - `specs/27-Norm-事实规范.md:148`：「03 §6.1「**20–24** 分别登记专属字段增量」」—— 同上，03 §6.1 已无「20–24」。
   - `specs/05-LDVH CLI规范.md:58` 与 `specs/06-事实源与信息溯源规范.md:58`：「20–24」/「五类事实类型语义归 20–24」—— 同口径陈引（05/06 不在本轮修复范围，但随 03 改写而滞后）。
   
   这些**不是修复「新引入」的逻辑矛盾**，而是修复**只动 03、未同步下游逐字陈引**造成的「该修而未修」缺口；其中 21/25/27 三处是对 03 的**直接前向引用破断**，最为棘手。修复若要「闭合」，应至少将 21/25/27 的三处陈引一并更新（05/06 可列入后续）。**此点削弱 I-1 的「完全闭合」判定——I-1 在 03 内部闭合，但在规范集群层面留有未修陈引。**

   **E-3（已证实·轻微·修复前既有，非本修复引入）**：`specs/25-Goal-项目目标.md:26`「本类型与 **20–24** 其余类型最重要的形态差异」—— 这是 25 自身在修复前的自述（25 本就不在 20–24 段内），修复后「20–24 其余类型」一语与 03 新「20–27 全体事实类型」框架略显错位，但属 25 自身历史措辞，建议 25 后续改为「与 20–24 多例类型」或「与其它多例事实类型」以保持精确。不计入本修复的引入缺陷。

3. **是否动摇第 1/2/3 轮结论**——✅ 未动摇。第 1 轮（00↔01 互洽）、第 2 轮（00↔01↔02 有条件自洽）结论不受影响；第 3 轮 I-1～I-5 的定性被本修复正面处置，未反转。round3 表 0.10 的 `fence_missing`（20/25）属解析器口径，本修复未触动，M6 复现确认仍同态——**非本修复引入**。

---

## 1. 各项闭合判定（I-1～I-5）

| 项 | 原问题 | 修复判定 | 理由 |
|---|---|---|---|
| **I-1** | §11 自限 20–24 漏 25/26/27，违 01 §7.2 | **有条件闭合** | 03 内部 16 处 + 标题 + frontmatter 全清零并加防误读护栏（A.1/A.2/A.3/A.4 全过）；但下游 21/25/27/05/06 残留 4+ 处「20–24」陈引破 03 新表述（E.2），规范集群层面未完全闭合。 |
| **I-2** | 零「事实规范」，未承接 00 §3.3 | **闭合** | 4 处新增，两道关键限定（不取得规范效力 / 不反向创造或放松规则）俱在（B.1）；§3.2 项 8 未越界（B.2）；27 权威位与 §6.1 承接与 27 实际一致（B.3/B.4）。仅 Sq-A 轻微措辞过伸（存疑）。 |
| **I-3** | `fact_type_key` 值域不完整 | **闭合** | 七值逐字匹配各类型实际登记（C.1）；25 单例豁免与 25 实际一致（C.2）；无遗漏/多列（C.3）。 |
| **I-4** | 未定义锚点型字段；`serves` vs `serves_sg` 分裂 | **有条件闭合** | 锚点定义与 01.Att.03 / 03 §7.2 相容、不造第二关系契约（D.1）；命名统一推理成立、未越权（D.2/D.4）；但宣布 `serves` 统一却未改 20/26，留下悬空要求（D-1）。 |
| **I-5** | §3.3/§11.2 未约束 25–27 | **闭合（随 I-1）** | 约束已扩至 20–27（E.1）；未引入新四方不一致（E.2 为残留陈引、E.3 为修复前既有）。 |

---

## 2. 问题清单（分级 + 依据 + 复现）

### 已证实问题

- **D-1（中等）· I-4 悬空要求**：03 §7.2（行 201）宣布 `serves` 为公共登记名并指 20/26 的 `serves_sg` 为「须同步修改」的分裂，但 03 自身未改 20/26，以「未完成同步前继续有效」使不一致合法化。复现：`sed -n '198,201p' specs/03-事实模型基础规范.md`；`grep -n 'serves_sg' specs/20-Spark-火花.md specs/26-Friction-摩擦账本.md`（行 128/124）。
- **E-2（中等）· 该修而未修的下游陈引**：修复只动 03，未同步 21/25/27 对 03 旧「20–24」表述的逐字前向引用，致其破断。复现：`grep -rn '20–24' specs/21-WorkCase-工作项.md specs/25-Goal-项目目标.md specs/27-Norm-事实规范.md`（行 44 / 82 / 148）；另 `specs/05-LDVH CLI规范.md:58`、`specs/06-事实源与信息溯源规范.md:58`。

### 存疑

- **Sq-A（轻微）· I-2 措辞过伸**：03 §3.3（行 70）「27 是事实规范作为事实对象的类型权威」把层级概念与具体类型等同化。当前态可接受，未来第二事实规范类型时需修订。复现：`sed -n '70p' specs/03-事实模型基础规范.md` vs `sed -n '65p' specs/27-Norm-事实规范.md`。
- **E-3（轻微）· 修复前既有陈引**：`specs/25-Goal-项目目标.md:26`「与 20–24 其余类型」与 03 新框架略错位。复现：`sed -n '26p' specs/25-Goal-项目目标.md`。

### 已排除

- **A.2 段号推定疑虑**：「20–27」紧跟「不按编号段号…推定」护栏，未构成新推定。依据：`sed -n '350p' specs/03-事实模型基础规范.md`。
- **A.4 虚构过渡授权**：01 §7.3 确有过渡口径（行 172）。依据：M5。
- **A.3 与 01 §7.2 冲突**：§11「28/29 不产生义务 + 未来成立即落入」与 01 §7.2「不因段外编号免除」同向。依据：行 205 vs 行 350。
- **D.1 第二关系契约**：锚点字段显式「不进入 relations」，与 01.Att.03（规范间关系层）无交叉。依据：03 §7.2 行 198–201 + 01.Att.03 §1–§3。
- **D.4 越权**：03 仅裁跨类型共享字段命名，在 §6.1 明示授权内；21 已显式委托（21 §285）。依据：03 §6.1 行 138 + 21 §285。
- **解析器 fence_missing（20/25）**：前轮已记、非本修复引入，M6 复现同态。

---

## 3. 是否有该修而未修、或修得过头

- **该修而未修**：① 21/25/27 三处对 03 旧「20–24」的逐字前向引用（E-2 核心）；② 05/06 的「20–24 / 五类」陈引（E-2 延伸）；③ 25:26 自身「20–24 其余类型」措辞（E-3）；④ I-4 的 `serves_sg`→`serves` 实际同步（D-1）。
- **修得过头**：**未见**。03 新增内容（§3.2 项 8、§3.3 27 权威位、§5.1 事实规范条目、§6.1 值域与承接、§7.2 锚点定义、§11 覆盖范围）均在其「事实公共模型」职责内，未扩张到类型语义或规范内容（恰被项 8 收窄）。Sq-A 仅为轻微措辞，不构成实质越权。

---

## 4. 已排除项汇总

见 §2「已排除」及 A.2/A.3/A.4/D.1/D.4。所有排除项均经独立机械核验（M1–M8）确认，未采信 round3 结论为终判。

---

## 5. 未覆盖范围与保证边界

**本复审证明**
- 03 内部的 I-1（清零）、I-2（4 处承接）、I-3（值域逐字一致）、I-5（扩至 20–27）修复**实质正确**，与 00/01 对应条款相容；`parseSpecDocument` 对 03 返回 ok:true，规范注册测试 69/69 通过。
- I-4 的锚点定义与命名推理成立、未越权。
- 修复未动摇第 1/2/3 轮既判自洽结论。

**本复审不证明 / 未覆盖**
- 21/25/27/05/06 的「20–24」陈引修订（E-2）是否会在本轮一并处置——当前**未处置**，列为该修而未修缺口。
- 20/26 实际将 `serves_sg` 同步为 `serves` 的字段表/验证/迁移条目修订（D-1 的落地）——当前**未执行**。
- 27 是否完整遵守 03 §11.1 十角色（round3 已观测结构不对齐）——本轮未做完整逐条合规判定，仅确认 03 侧已将其纳入覆盖范围。
- 01 §9.2 成立条件、Human 决定、受控提交等效力维度——文本审计无法自证。
- 解析器对 20/25 的 `fence_missing`（M6）——属工具口径，非规范文本问题。

---

## 6. 独立性披露

1. 本执行体为隔离会话独立子代理，未参与 03 的 I-1～I-5 修复起草及此前任何审核，无候选形成上下文，符合 01 §12.2 独立性要求。
2. `docs/review-round3-00-01-02-03.md` 仅作复核起点；其中全部被复核断言（I-1～I-5、P4、C-1～C-4、A-1）均经本轮独立机械核验（§0 M1–M8）重新确认或推翻，未采信为结论。
3. 与规范作者可能使用同类模型；依 01 §12.2 以上下文隔离维持独立。共享 worktree 仅只读使用。
4. 本复审仅发现与报告，未修正任何发现、未修改任何 specs/、plugin/ 文件、未 git commit。

---

## 7. 总体结论

**有条件（conditionally acceptable）。**

- I-2、I-3、I-5 及 I-1/I-4 的**实质论证**均正确闭合，与 00/01 相容，测试与解析通过。
- 但存在两项**该修而未修**缺口，使「完全可提交」不成立：
  1. **E-2（中等）**：修复只动 03，未同步 21/25/27（及 05/06）对 03 旧「20–24」表述的逐字前向引用，造成 3 处**悬空误引**——这是本轮修复最实质的完整性缺陷。
  2. **D-1（中等）**：I-4 宣布 `serves` 统一却未改 20/26，留下悬空要求。

**处置建议（不阻断 I-1～I-5 论证正确性，但提交前应闭合）**：
① 将 `specs/21-WorkCase-工作项.md:44`、`specs/25-Goal-项目目标.md:82`、`specs/27-Norm-事实规范.md:148` 的「20–24」陈引更新为「20–27 / 全体事实类型规范」（必要）；② 评估将 05:58、06:58 一并更新（建议）；③ 在 03 或 20/26 中落地 `serves_sg`→`serves` 同步（含字段表/验证/迁移条目），或显式登记为带 Human 迁移时点的待办（D-1 闭环）；④ Sq-A / E-3 轻微措辞后续顺带精化。

（报告结束。唯一交付物为本文件；未改动任何 specs/、plugin/ 文件，未 git commit。）
