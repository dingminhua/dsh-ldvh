# 规范源同步计划（00 改动后的下位调整）

> **性质**：**计划**，非候选。经 Human 审阅后再逐批执行。
> **依据**：`ca89742`（00 §2 定稿）＋ `77d3703`（00 §3–§5 同步）；行动计划总原则「每一处下位修订都必须能回指 00 的哪一条改动」。
> **范围**：**规范源（`specs/`）内全部载体**。`docs/` 留档与已作废候选**不在范围**（非规范效力）。
> **执行状态（2026-09-14 补记）**：本计划批次已基本执行完毕——批 1／2／3／6 执行记录见 `docs/spec-sync-batch1-log.md`；批 5 编号表与 30–39 业务系统段随 `1fbc862`／`6438652` 落地；批 7（21 号 WorkCase 立项）已由 `d4ee39e` 执行（现行 `specs/21-WorkCase-工作项.md`）；§八「04 还要吗」已由 Human 裁定为**改造**而非退出（现行 `specs/04-业务系统基础规范.md`，`spec_key: business-system-foundation`，`supersedes: action-template-foundation`）；§8.4 的 30–38 复用问题已定（30–39 业务系统段，镜像 00 列举序）。本文作为同步计划与裁定依据的溯源保留；批次细节以各批提交说明与 batch1-log 为准。

---

## 一、00 到底改了什么（下位必须回指的清单）

这是所有下位调整的**唯一依据**。以下每条都会被下位引用：

| # | 00 的改动 | 位置 | 下位影响面 |
|---|---|---|---|
| **A1** | 业务系统 **五 → 六**，并改名 | `§2:77`、`§3:92`、`§3.1` | 全部引用系统名录的规范 |
| **A2** | 六系统名录：**调研／讨论／工作执行／规则遵守／目标与蓝图／记忆与反思** | 同上 | 同上 |
| **A3** | **「沉淀」不再是制度**，并入记忆与反思 | `§3.1` 制度段删条 | 引用「沉淀制度」的全部规范 |
| **A4** | 制度定义补「**贯穿并约束所有系统**」 | `§3.1:114` | 定义制度的节 |
| **A5** | 责任原则改为**按发起者分派**（主控发起／宿主闲置辅助任务） | `§2:75`、`§5`、`§5.1`、`§5.3`、`§5.5`、`§7.2` | 责任、停止条件、编排相关 |
| **A6** | 「**技术支撑**」已改称「**能力承载**」 | `§3.1:98`（含两族） | 全部写「技术支撑」的规范 |
| **A7** | 两族能力承载：**LDVH CLI 确定性服务／宿主原生机制** | `§2:76`、`§3.1:100-101` | 读写编排相关 |
| **A8** | 灵感／方向归 **Spark**，由记忆与反思的沉淀环节承接 | `§3.1:110` | 记忆系统相关 |

---

## 二、受影响文件清单（按改动性质分三类）

### 类别 ①：**名称与名录替换**（机械可查，风险低）

| 文件 | 失配处 | 需替换为 |
|---|---|---|
| `02-工作模型基础规范.md` | `:10` positioning「读、写、编排三大**技术支撑**」 | 「能力承载（LDVH CLI 确定性服务／宿主原生机制）」 |
| 同上 | `:10`「**四大业务系统**」（记忆/调研/讨论/遵守） | **六业务系统** |
| 同上 | `:49`、`:58`、`:85`、`:205`「技术支撑」 | 「能力承载」 |
| 同上 | `:103/110/117` 章标题「读（技术支撑）」等 | 「读（能力承载）」等 |
| `04-行动模板基础规范.md` | `:51/191/195`「**遵守系统**」 | 「**规则遵守系统**」 |
| `12-讨论系统规范.md` | `:124`「遵守系统」 | 「规则遵守系统」 |
| `06`／`07`／`08`／`09`／`10`／`11`／`01.Att.01` | 各处「技术支撑」 | 「能力承载」 |
| `01-规范模型基础规范.md` | `:172` §8.1 编号表「技术支撑、业务系统、制度」 | 与新口径一致 |

### 类别 ②：**「沉淀制度」失效**（实质，须改语义）

| 文件 | 失配处 | 处置方向 |
|---|---|---|
| `02` | `:97`「先由**沉淀制度**判断去向」 | 改为「由记忆与反思的**沉淀环节**判断」 |
| `02` | `:126`「按**沉淀制度**分流决定去留」 | 同上 |
| `02` | `:159`「**## 14. 沉淀（制度）**」整节 | **须重写为记忆与反思系统的内部机制**，或整体移出（见 ③） |
| `02` | `:189` 依赖「记忆/沉淀」并列表 | 重述 |
| `11` | `:81`「由**沉淀制度**与 03 承载」 | 改为「由记忆与反思系统与 03 承载」 |
| `11` | `:236`「由**沉淀路由制度**决定后续去向」 | 改为「由记忆与反思系统的沉淀环节决定」 |
| `03` | `:39`「记忆业务系统与**沉淀制度**」 | 改为「记忆与反思系统的沉淀环节」 |
| `10` | `:39`「读技术支撑与**沉淀制度**」 | 改为「读能力承载与记忆与反思系统」 |

### 类别 ③：**结构缺口**（须新增内容）

| 缺口 | 说明 | 落点 |
|---|---|---|
| **02 无「目标与蓝图」章节** | 六系统之一，02 应有对应节 | 02 新增 §N |
| **02 无「工作执行」章节** | 00 有「工作执行」，02 的 §12 是「遵守」 | 02 新增 |
| **02 §14 沉淀（制度）须重定位** | 沉淀不再是制度 | 改写或移除 |
| **02 缺「记忆与反思」合并后的表述** | 现 §9 只叫「记忆」 | 改名并补反思 |
| **21 号 WorkCase 规范缺失** | 12 份规范引用，规范不存在（跳号） | 见行动计划 §5.3 |
| **`01.Att.01` 术语表旧口径** | 「技术支撑」「四大业务系统」「制度（复核/沉淀）」 | 随 ① ② 批 |

---

## 三、建议的执行批次（每批一笔独立提交）

**排序原则**：先低风险机械替换，后实质改写；每批可独立审核、独立回滚。

| 批 | 内容 | 文件 | 前置 | 门 |
|---|---|---|---|---|
| **批 1** | **术语机械替换**：「技术支撑」→「能力承载」；「遵守系统」→「规则遵守系统」 | `01`／`03`–`12`／`01.Att.01` | 无 | 独立审核（轻） |
| **批 2** | **02 号深改**：positioning、名录、章节名、§14 沉淀重定位、补目标与蓝图与工作执行 | `02` | 批 1 | **独立审核＋Human** |
| **批 3** | **沉淀引用面同步**：`03`／`10`／`11` 的「沉淀制度」 | `03`／`10`／`11` | 批 2 | 独立审核 |
| **批 4** | **术语表收口**：`01.Att.01` 全表 | `01.Att.01` | 批 1–3 | 依 `01 §8.3` |
| **批 5** | **编号表与登记**：`01 §8.1` 表（业务系统段 30+）、`01:172` | `01` | 批 2 | 独立审核 |
| **批 6** | **受保护内容同步**：`guidance-text.js`、插件 README 等 | `plugin/lib/` | 批 2 | **受保护内容流程（各自独立提交）** |
| **批 7** | **21 号 WorkCase 规范立项** | 新建 `specs/21` | 批 5 | `01 §11` ＋独立审核＋Human |

---

## 四、须先由 Human 裁定的事项

| # | 事项 | 为何需 Human |
|---|---|---|
| **H1** | **「沉淀」在 02 的地位**：是「记忆与反思系统的机制」还是保留独立节？ | 涉及 00 已定的层级归属如何在下位展开 |
| **H2** | **02 是否新增「目标与蓝图」「工作执行」两节**，还是先挂空？ | 六系统为承诺，02 应逐节承接 |
| **H3** | **21 号 WorkCase 是否本批立项** | 缺其规范则「执行工单」无类型承载 |
| **H4** | **批序是否按上表**，或先做某批 | 执行顺序属 Human 决定 |
| **H5** | **`dimensions` 七元组**（`spec-registry.js` 机械强制）是否随六系统扩维 | 跨文档（00 身份块＋`01.Att.02`＋Code） |

---

## 五、不属本计划的事项（明确排除）

| 事项 | 为何排除 |
|---|---|
| `docs/` 下的留档、已作废候选 | 非规范效力，无回指义务 |
| `docs/memory-system-design-prompt.md` 等设计稿 | 非规范源 |
| 3.2／3.4 两项机械层议题（签名取值来源／outcome 漂移） | 独立议题，另有候选 |

---

## 六、工作量估计（供排期）

| 类别 | 文件数 | 性质 |
|---|---|---|
| ① 机械替换 | **12** | 低风险，可批量 |
| ② 沉淀语义 | **4**（02／03／10／11） | 中，须逐处判断 |
| ③ 结构缺口 | **2–3**（02 新节＋21 号） | 高，须起草新规范 |
| ⑥ 受保护内容 | **2–3** | 各自独立流程 |

**总计涉及规范源 15 份左右**，其中**实质改写 4 份**、**新建 1 份**。

---

## 七、本主控的建议

**先做批 1（术语机械替换）**，理由：

1. **零语义风险**——纯改名，有明确的 00 依据（A6／A7）；
2. **可机械验证**——`grep` 计数即可确认清零；
3. **解锁后续**——批 2 的 02 深改会大量引用新术语，先统一术语可减少返工；
4. **独立可回滚**——即使后续批次调整，批 1 不会白做。

**再做批 2（02 深改）**——它是六系统的**核心承接地**，也是当前失配最重的文件（positioning 一句里就有 3 处过期）。

---

## 八、专题：04 号「行动模板基础规范」还要吗？

**Human 2026-09-11 问**：「04文档还要吗」

### 8.1 答案：**不要了——但这是【已定事项】，不是我新提的**

**决定性证据**：`3b1e3e6`（00 结构重排，上一会话的定稿提交）的提交信息自述：

> §3.3 补边界原则段与判据句（**删已退役的行动模板**、补已退出或未受控的机制文本）；§4.5 与 §5.5 的**模板改既有行动结构**；§6.1 V4 判据改**既有行动结构**

**核验结果**：

| 检查 | 结果 |
|---|---|
| `00` 中「行动模板」 | **0 处**（已被删） |
| `00` 现用替代术语 | **「既有行动结构」**（`:180`／`:210`／`:223`） |
| 行动计划 §5.1 | **「04 退出」**已列为待执行项 |
| 行动计划 §4.7 | **「01 §8.1 编号表…删 04 行」** |
| 待决清单 F | **「04 空号是否永久保留」**（Human 待决） |

**→ 即：04 的退出已由上一会话定案并写入行动计划，只是尚未执行。**

### 8.2 但退出**尚未执行**，现状是「半退出」

| 事实 | 状态 |
|---|---|
| `04` 文件仍在 `specs/` | 未移除 |
| `01:174` 编号表**仍有 04 行**「行动模板基础规范｜模板共同机制、预检与风险路由」 | 未删 |
| `01:135`、`:190` 仍写「04 定义 **30–38** 行动模板规范共同结构」 | 未改 |
| `02:62`「04 是行动模板共同结构与风险路由的**唯一权威**」 | 未改 |
| `02:142/149/156/168` 仍以行动模板为机制指向 | 未改 |
| `03` 中「行动模板」3 处 | 未改 |
| `04` 自身身份块 `basis: [ldvh-root, specification-model-foundation]` | **其上位依据 00 已无此概念** |

**→ 04 现在是**「上位依据已被 00 删除、但自身仍生效」的悬空规范**。这比单纯"要不要"更严重：**它仍被 01/02 引为唯一权威**。

### 8.3 因此本计划的批次须调整

原计划**遗漏了 04 的处理**。现补入：

| 批 | 内容 | 依据 | 门 |
|---|---|---|---|
| **批 2.5（新增）** | **04 退出的执行**：按 `01 §9.3` 四项（Human 决定退出候选、登记变化、受影响引用处理、职责去向说明、受控提交） | 行动计划 §5.1 | **Human 决定＋独立审核** |
| **并入批 5** | `01` §8.1 编号表删 04 行；`:135`／`:190` 的「30–38 行动模板段」处置 | 行动计划 §4.7 | 独立审核＋Human |
| **并入批 1／3** | `02:62/142/149/156/168`、`03` 的「行动模板」引用改为「**既有行动结构**」 | 00 已改称 | 独立审核 |
| **待 Human** | **04 空号是否永久保留**（待决 F） | —— | Human |

### 8.4 一个须一并处理的问题：**30–38 段怎么办**

若 04 退出，则其下属的 **`30–38` 具体行动模板段**同时失去共同结构依据（`01:135` 明写「04 定义 `30–38` 行动模板规范共同结构」）。

**而行动计划 §5.2 又拟把 `30–38` 用作【业务系统段 30+】**（30 调研／31 讨论／32 执行／33 记忆／34 遵守）。

**→ 两处对 `30–38` 的规划冲突**：一处当作废的行动模板段，一处当新的业务系统段。

**须 Human 裁定**：`30–38` 是否**复用**为业务系统段？（复用即意味着行动模板段不再存在。）这属**待决清单 E／F** 的延伸。
