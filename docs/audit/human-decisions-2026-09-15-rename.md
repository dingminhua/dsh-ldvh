# Human 决定登记 · 业务系统更名与 WorkCase 中文规范名（2026-09-15）

> **性质**：Human 决定留痕。`00 §4.2` 规定「改变能力承载的分类、业务系统或制度的数量、名称或职责」与「新增、改名或重排规范模型、事实模型」为根决定事项，须由 Human 决定。本文件登记本次相关决定的事实、范围与依据。
> **效力**：本文件是**决定留痕**，不取得规范效力。规范效力来自 `specs/` 各规范正文（`00 §3.3`：规范源承载可强制执行规则）。

---

## 一、决定清单

| # | 决定事项 | 决定内容 | 决定日期 | 影响面 |
|---|---|---|---|---|
| D-1 | 业务系统「工作执行」更名 | 六大业务系统第三系统更名为**「工单执行」**；`00 §3.1` 定义行同步改为「执行已批准的工单，以 WorkCase 承载其结论」；`§2` 功能名「执行工单」不变 | 2026-09-15 | `00` §2/§3/§3.1；`01` 号位表、`02`、`21`、`27`、`30`、`31`、`01.Att.01`、`01.Att.02`、guidance-text.js |
| D-2 | WorkCase 中文规范名更名 | WorkCase 的中文规范语义定为**「工单」**，不再是「工作」或「工作项」；21 号文件改名 `specs/21-WorkCase-工单.md`，title/H1/canonical_path、`01 §8.1` 登记行、术语表行同步；规范树中以「工作」指称 WorkCase 级单元的短语一并统一（量词「一份工单」） | 2026-09-15 | `21`（改名，27 处）、`01 §8.1`、`03`、`02 §23`、`27`、`30`、术语表 L103、web docs 路径引用 |
| D-3 | 内部 work_items 术语保留 | WorkCase 内部 `work_items` 成员的「工作项」称法（Web 呈现层、术语表「工作项检查点」）为**独立子概念，保留不改**——本次更名恰好消除「工作项」同时指类型名与内部单元的既有过载 | 2026-09-15 | plugin/web 工作项文案、术语表 L112 |

## 二、决定依据与过程

- **触发**：Human 直接指示「00里，工作执行，我希望改为工单执行，工单会更靠近WorkCase，工作会显得没有特指」；就 00 候选经结构化确认「按候选执行」后，补充裁定（原话）：**「我强调一下，WorkCase的中文语义就是工单，而不再是工作或者工作项」**；扩展范围确认「全量落地」。
- **受保护内容程序（`00 §4.3`）**：展示现有内容与准确候选 → 说明差异与原因 → 独立对抗审核两轮（`docs/review-00-rename-gongdan-2026-09-15.md`，均「放行但需修正」、无阻断项；F1/F8/F9/F10/F12 修正项已全部落实，F9 机械加载验证 25 文件 0 缺口）→ Human 明确同意（如上）→ 四段独立提交。
- **声明（T2 表面张力，审核 F2）**：业务系统名采用「工单」、WorkCase 中文规范名定为「工单」，**不改变** `27.Att.01` 方向清单 T2「Issue/工单 Ticketing」**未采纳**的结论——T2 拒绝的是「以 Issue/Ticketing 生命周期管理为 Norm 方向」；本次是称法与术语定名，不引入 ticketing 语义。
- **术语关系（分层）**：**工单** = WorkCase 的中文规范名（类型级，含其承载的已批准工作单元）；**工作项** = WorkCase 内部 `work_items` 成员（子单元级）；**工作** = 泛指。三词分层、互不替代。

## 三、落地的提交

| 提交 | 内容 |
|---|---|
| `43cef5c` | 00 四处更名（受保护内容独立提交；Git Gate 通过） |
| `a696383` | 规范树同步批：01/02/03/27/30/31/两附件 + 21 号改名（git mv，27 处正文）+ web docs 路径引用（Git Gate 通过） |
| `a5e4fbc` | guidance-text.js 注入锚点同步（受保护内容独立提交；Git Gate 通过） |
| 本次提交 | 审核记录归档 + 本决定留痕 + Spark `dbffddd2` 受控术语补充 |

## 四、未决事项与后续

- `30 §15.6` 存在**先于本次更名**的既有变体「研究执行（work execution）」（L458，同行的「不执行已批准工单」已同步），非本次范围，未改动；是否清理留待后续裁定。
- Web 呈现层 WorkCase 类型级显示名**统一为「工单」**：Human 已于同日裁定执行（含独立检查），属实现层后续提交；本次规范批仅同步了 web docs 中的规范路径引用。
- `docs/**` 与 `ldvh-base/**` 其余历史记录保持原文（点时性档案，不改历史）。
