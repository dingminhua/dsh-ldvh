# 审计记录：规范声明的实现状态登记

> **性质**：**实现状态登记（历史事实）**。原分散于各规范正文的「如实声明／未实现范围」段落，依 Human 决定（2026-09-12）移出规范正文。
> **移出理由**：规范正文承载**要求**（「当 X 时应做 Y」），不承载**状态**（「现状是 X」）。读者照状态无事可做；而由状态派生的**要求**已在各规范中改写为条件式保留。
> **基准**：`530202b` 之后。

## 一、01 §8.2：已退出职责标识符表的机械校验

**状态**：`parseSpecDocument` 只校验 `supersedes` 的字段格式（字符串、非空、成员唯一），**尚未校验已退出 key 表本身**。

**规范侧的替代内容**（`01 §8.2`）：条件式要求——机械校验覆盖本表之前须以人工核对代替且不得声称已通过机械校验；覆盖之后须以机械结果为准。

## 二、21 §15.2：WorkCase 前提未满足项

| 依赖项 | 状态 |
|---|---|
| attempt 的宿主实现 | **未实现**（应由 08/09 承接） |
| 34 号规范 | **未建立**（`10 §6.2` 的 provisional 措辞未随之更新） |
| 锚点型字段命名统一 | **未同步**（`serves`(21/25) vs `serves_sg`(20/26)） |

**规范侧的替代内容**（`21 §15.2`）：条件式声明限制——依赖未满足时不得声称相应能力已承接。

## 三、27 §11：Norm 唯一性的三层实现

| 层 | 设计 | 实现状态 |
|---|---|---|
| 第一层：受控写入前拒绝 | 已定义（`ERR_DIRECTION_ALREADY_EXISTS`） | **未实现** |
| 第二层：Git Gate 拦截 | 已定义 | **未实现** |
| 第三层：消费端 fail-closed | 已定义 | **未实现** |

**核验**：`grep -rn 'ERR_DIRECTION_ALREADY_EXISTS' plugin/` 无实现命中。

**规范侧的替代内容**（`27 §11`）：条件式要求——三层全部实现之前该约束只有规范要求、须人工核对、不得声称已获机械保证。

## 四、04 §10：业务系统规范段与定义迁移

| 项 | 状态 |
|---|---|
| 业务系统规范段 32–35 | **未建立**（30/31 已建立） |
| 六系统定义迁移 | **未启动**（仍内联于 `02 §9–§14`） |

**规范侧的替代内容**（`04 §10`）：条件式要求——未建齐期间不得声称已完备、未迁移期间以 02 为唯一定义来源。

## 五、方法说明

本文件是**状态记载**，不产生规范效力。规范效力来自各规范中**改写后的条件式要求**。若某状态发生变化（如实现补齐），应更新本文件，并检查对应规范的条件式要求是否需相应调整。

## 六、00 §3.1：沉淀与反思的机制形态

**状态**：沉淀与反思二者的机制形态**尚未设计定案**。

**原表述**：`00 §3.1` 记忆与反思条目末句曾写「沉淀与反思二者的机制形态待重新设计后定」。

**处置（Human 决定 2026-09-12）**：**彻底删除**该句。理由——规范正文只承载要求，不承载状态；该句既不构成对读者的要求，其"未设计"的事实亦不属于根规范需要声明的内容。

**该事实的记录位置**：本文件（`docs/audit/`，无规范效力）。**后续若设计定案**，应在相应下位规范（如 02 或该系统规范）中定义机制，而非回填到 `00`。

**边界**：删除该句**不改变**记忆与反思作为六大业务系统之一的地位与其职责定义（`00 §3.1` 其余表述未动）；也不产生「该机制已定案」的推定。
