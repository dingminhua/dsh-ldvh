# 00 修订候选 v5.1 —— 【已作废 SUPERSEDED】规划目标＝系统（待建），同步蓝图＝功能（空闲触发）

> ## ⛔ 本文档已作废（SUPERSEDED），不得作为审议或执行依据
>
> **作废时间**：2026-09-11T01:25+08:00
> **作废依据**：独立对抗审核 `caafad43` 判决「**不得进入 Human Gate；且应被 SUPERSEDED（作废重立），不是修订**」；本主控复验后接受。
>
> **保留原因**：供追溯本议题的完整证据链（三次定位变更 + 两次审核作废）。
>
> ### 作废理由
>
> | # | 理由 | 本主控复验 |
> |---|---|---|
> | **C-1** | 「送审即冻结」**实际不成立**：审核对象在审核期内被本主控改写，审核者按我声明的算法实算得 `be6ff6b1…`，与我所声明不符 | **实质成立**。审核者读到的是**旧快照**（其见首个 `## ` 在第 5 行，现为第 16 行）。指纹方案本身可复现（现文本实算 = 所声明值），**但「审核对象漂移」是真实缺陷，且系本主控流程失败** |
> | **C-2** | 与 Human **第三次澄清**相悖：现文写「同步蓝图＝功能，由记忆承载」，而新结构是「**对话沉淀系统** → 产物 → 同步蓝图是其一」 | **成立** |
> | **C-3** | 「对话沉淀系统」与既有**制度「沉淀」**（`00:113` 定义制度＝**不独立产出**；`00:116` 沉淀为制度）分类冲突，二者**不可并存** | **成立** |
>
> ### 审核另指出、本主控承认的缺陷
>
> - **F-5**：`00:111` 首句「各业务系统**以自身事实对象承载其结论**」——「尚未构建」的系统**当下即不可满足**该条。**本主控漏检了此条**（只查了 `00:140`）。
> - **F-6**：「构建状态」入 `00:164` 属**越位**——建设状态归 `01:118` §6.3 管辖；且会使行动计划 §5.2 各批次全部升级为根决定。
> - **F-2**：§4.1 对 `specs/25` 的边界论证**过度外推**（`25:51` 指定的承载方是**行动模板段**，非新业务系统）。
> - **F-7**：§2.3 补语是**开放式分类断言**，会吞并 `02:126`（空闲维护记忆健康）与 `26:187`（空闲评审攒账）——**同型于 v3 被推翻的缺陷**。
> - **F-9**：路径错（实际为 `01.Att.01-LDVH双语术语表.md`）。
> - **F-12**：`specs/02` **无「执行」章节**（仅记忆/调研/讨论/遵守四节），而 `00:109` 有执行。
>
> ### 本主控的流程失败（如实记录）
>
> 1. **需求未定即反复起草送审**——本议题三次澄清、两次审核作废，根因是**在 Human 结构意图未确定时就开始起草**；
> 2. **送审后改写正文**——v5 → v5.1 正文确被改动，复犯了上一轮 `8aaa0cd3` 判定的 B-1；
> 3. **只查上位原则冲突，未自查与既有条文的冲突**——漏检 `00:111`。
>
> ### 教训（写入后续流程）
>
> - 送审对象锚点应写入**独立文件**（`sha256sum` 命令 + 输出），不在候选内自证；
> - **需求未定前不起草候选**；
> - 候选自查须覆盖 `00:111`／`00:113` 等**既有条文**，不止上位原则。
>
> ---
>
> 以下为作废正文，仅存档。

> **candidate_id**: `00-fn-plane-v5.1`
> **规范正文指纹**：`sha256:9bc1110b4e3047477d44202cae2fb538f7b436e33174aa5791da2614cfff2706`
> **算法**：取文件首个 `## ` 起至文末（即规范正文，不含本头部块）计算 sha256——头部不参与，故无自指循环。
> **冻结时间**: 2026-09-11T00:58+08:00
>
> **版本沿革（如实记录，不掩盖）**：
> - **流程改进（自此生效）**：候选一旦送审，**只允许追加"审核期发现"附录并递增小版本号**，不改动正文；否则先撤回送审再改。
>
> 依据：Human 2026-09-11 澄清（§0 逐字）；`8aaa0cd3` 独立对抗审核（v3 判整体高风险）。
> 状态：**草案，未执行、未提交。** v3/v4/v5 已被本稿取代。

---

## 0. Human 澄清（2026-09-11，逐字）

> 规划目标是一个系统呀，但是现在还没有构建，需要 ai 引导用户创建目标以及 ai 引导用户调研，之后再进行创建子目标，同步蓝图是一个功能，会在空闲时触发

**据此修正的定位（非对称，与 v3 的"两个都是功能"、v4 的"都不动" 均不同）**：

| 项 | 定位 | 状态 | 触发 |
|---|---|---|---|
| **规划目标** | **业务系统**（第六个） | **尚未构建** | AI 引导用户创建目标 → AI 引导调研 → 再创建子目标 |
| **同步蓝图** | **功能** | —— | **空闲时触发** |

---

## 1. 本稿为何能成立（而 v3 不成立）

`8aaa0cd3` 的三项阻塞，在**新的定位**下重新检验：

| 审核发现 | 在 v3（两个都是"功能"）下 | 在本稿（一个是系统、一个是功能）下 |
|---|---|---|
| **B-2「功能」与「能力承载」不可区分** | 成立——因为要**新造一个平面**去安放两者 | **部分消解**：只有一个「功能」（同步蓝图），**不再需要为它建平面**；见 §2.2 |
| **R-1 Web 同属两平面** | 成立 | **不再适用**——不建平面，无"归属"可争 |
| **R-2 工作包生命周期不属四平面** | **致命**——说明我的"划分"不完备 | **自动消解**——本稿**不做平面划分** |
| **R-4 改动净效果≈零** | 成立 | **不成立**——本稿有实效：① `00:111` 承诺条款实际生效；② 消除「规划目标被记忆吞并」的归属错误 |
| **R-5「功能」定名撞 `00:239`** | 成立 | **需处理**——见 §2.3，本稿**不把「功能」立为规范分类名**，仅在描述同步蓝图时用普通词 |

**§3.4「未经核验的能力不得写成已经成立」的反对（审核 B-3）也消解**：

- `§3.1:103` 定义**业务系统＝承诺**（承诺承载业务过程），**不是**能力已成立的声明；
- `§3.4:140` 管的是**技术能力**的"设计意图／实现存在／环境接入／当前可用／实际验证"五态须分别判断；
- **承诺"要有这个系统" ≠ 声称"这个系统已建成"**。二者本可并存——`00:105` 已用同一手法写过「其机制形态**待重新设计后定**」。

---

## 2. 拟修改条文（四处）

### 2.1 §3.1 业务系统名录——新增「规划目标」（第六个系统）

**在「记忆」之前**插入：

> - **规划目标**：把 Human 的长期意图确立为可回指、可核对的项目目标——由 AI 引导 Human 创建目标、引导调研，再据以创建子目标；本系统**尚未构建**，其机制形态待设计后定；

**并同批修改承诺条款** `§3.1:111`：

**原**：

> 各业务系统以自身事实对象承载其结论。新增、删除业务系统，或改变业务系统的数量、名称或职责，均须修改本规范，并按第四章取得 Human 决定。

**改为**：

> 各业务系统以自身事实对象承载其结论。新增、删除业务系统，或改变业务系统的数量、名称、职责**或构建状态**，均须修改本规范，并按第四章取得 Human 决定。

> **理由（实证收益之一）**：本稿把系统数由 5 改为 6，**正是 `00:111` 要求"须修改本规范并取得 Human 决定"的情形**——本次修订即是该条款的**首次实际适用**。v3 把两项当"功能"加行，**不触发该条款**，故 R-4 判定其无实效；本稿**触发**。

### 2.2 「记忆」条——删除错误承接

**原**：

> - **记忆**：留存和取回项目的经验认识，从目标、判断、行动、结果、摩擦和反馈中形成候选认识；沉淀的分流路由与反思子机制由本系统承接；**目标规划与蓝图同步由本系统承接，其机制形态待重新设计后定**；

**改为**：

> - **记忆**：留存和取回项目的经验认识，从目标、判断、行动、结果、摩擦和反馈中形成候选认识；沉淀的分流路由与反思子机制由本系统承接；**承载「同步蓝图」功能**，其机制形态待重新设计后定；

**改动实质**：删除「目标规划」的归属（它已**升为独立系统** §2.1），仅保留「同步蓝图」并**改称「功能」**。

### 2.3 「同步蓝图」的安放——**不新造平面**

**不新增「功能」平面**（v3 的失败正在于此）。改为在 §3.1 的**总括句**中如实补一句：

**原**（`§3.1:118`）：

> 能力承载回答"怎么跑"，业务系统回答"做什么"，制度回答"怎么保证做对"。三者是同一工作中的不同职责形态，不是必须依次经过的流程；其具体职责、技术、流程与协同由相应下位规范承接。

**改为**：

> 能力承载回答"怎么跑"，业务系统回答"做什么"，制度回答"怎么保证做对"。三者是同一工作中的不同职责形态，不是必须依次经过的流程；其具体职责、技术、流程与协同由相应下位规范承接。**此外，系统可派生由空闲触发运行的同步功能（如「同步蓝图」），功能不构成独立职责形态，其归属与机制由相应系统规范承接。**

> **用语纪律（应对审核 R-5）**：此处用**普通词「同步功能」**，**不立"功能"为规范分类名**——`00:239` 已有普通语义的「功能已呈现」，立分类名会造成**未登记的规范位移**。本稿仅在描述同步蓝图时使用该词。

### 2.4 §3 导语——五系统 → 六系统

**原**（`§3:92`）：

> …**记忆、调研、讨论、执行、遵守五个业务系统**承载业务过程，制度贯穿其间…

**改为**：

> …**规划目标、记忆、调研、讨论、执行、遵守六个业务系统**承载业务过程，制度贯穿其间…

---

## 3. §2 的配套修改

### 3.1 §2:77 承诺面

**原**：

> - 以业务系统承载规划目标、同步蓝图、存取记忆、吸收调查、收敛讨论、执行任务、落实规则、沉淀反思**等**能力；

**改为**：

> - 以业务系统承载业务过程——规划目标、记忆、调研、讨论、执行、遵守，以能力对用户兑现价值——存取记忆、吸收调查、收敛讨论、执行任务、落实规则、同步蓝图等；

**说明**：① **删「等」**（消除与 `00:111` 的抵触）；② **「规划目标」移入系统一侧**（Human 澄清其为系统）；③ 保留 v4 已确立的「系统承载业务过程」表述。

### 3.2 `§4.2` 根决定清单

**原**：

> …改变**能力承载的分类、业务系统或制度的数量、名称或职责**…

**改为**：

> …改变**能力承载的分类、业务系统或制度的数量、名称、职责或构建状态**…

> **理由（实证收益之二）**：本稿新增第六个业务系统，**属 `§4.2` 根决定清单事项**，须 Human 决定。**这正是审核 I-1 指出的漏洞**（v3 自设保留事项却未补入 §4.2）——本稿同批补齐，**不留不一致**。

---

## 4. 待处理（**须 Human 授权，本稿未动**）

| # | 事项 | 为何未动 |
|---|---|---|
| 1 | 「沉淀反思」在 `§2:77` 的平面混列（制度·沉淀 ＋ 记忆·反思子机制） | Human 未提及，**纳入即夹带** |
| 2 | 「复核」在 `§2:77` 缺失 | 同上 |
| 3 | `00` 身份块 `dimensions` 七元组 | 跨文档（`01.Att.02` ＋ Code），违约「只含 00 的独立提交」 |
| 4 | 下位引用面：`guidance-text.js:84`（**受保护内容**）、`01:172`、`02:205` | 行动计划已列相关批次 |
| 5 | 蓝图投影指向 10 号而 10 号零承载（`25:50`；`20:204`/`22:200`/`23:175`/`26:188`） | **独立议题** |

### 4.1 已核验：本稿与 `specs/25` 的边界（备审）

Human 描述的流程「**AI 引导用户创建目标 → AI 引导调研 → 再创建子目标**」与既有规范的关系，经逐条核对：

| `specs/25` 条文 | 内容 | 对本稿的意义 |
|---|---|---|
| `25:12` | Goal 类型的「**唯一类型权威**」 | 25 权威覆盖**对象**（字段、SG-n、生命周期） |
| `25:117` | sub-goal 精化次序：创建时**允许只落目标陈述**，首次精化由 AI 提议、Human 逐条确认 | **与 Human 描述的流程一致**（先目标、后子目标） |
| `25:51` | 「项目初始化对话的**执行流程**（行动模板段承载，本文只定义**产出契约**）」 | **25 明确不拥有"流程"**——流程正是本系统所要承载的 |
| `25:13` | scope 明写「**不定义调研执行流程**」 | 调研由调研系统承载，本系统只做**引导** |

**结论**：`规划目标` 作为**业务系统**（承载流程：引导创建、引导调研、据以创建子目标），与 `specs/25` 作为**Goal 对象类型权威**（承载产出契约），**职责不重叠**：一个管"怎么引导出目标"，一个管"目标落档的字段与生命周期"。**本稿 §2.1 的表述须体现该分工**（已在待审问题 3 要求独立复核）。

> **待补强**：§2.1 现文尚未明写「以 Goal 事实对象落档」——审核若判定需要，应在定稿时补入，以避免与 25 重复权威。


---

## 5. 待审问题（独立审核）

1. **§2.2 把「同步蓝图」定为**记忆**承载的功能，是否与 `25:50`（蓝图投影格式由 10 号承载）冲突？**两者是否同一件事？** 请重点攻击。
2. **§2.3 的总括句补语是否又引入了一个"平面"**（v3 的失败模式）？审核 R-2 曾证明 `00:120` 的「模式」不属任何平面——本稿补语与「模式」体例是否一致？
3. **「规划目标」系统与 `specs/25`（Goal 类型唯一权威）的边界**：§2.1 只说"由 AI 引导创建"，是否足以避免与 25 重复权威？
4. **§2.1 写入"尚未构建"是否恰当**？`§3.4:140` 要求"设计意图、实现存在…必须分别判断"——在**根规范**里写构建状态，是否会随实现推进而迅速过期？
5. **「构建状态」作为 `§3.1:111`／`§4.2` 的保留事项是否过宽**？会不会导致任何实现进度变化都要改 00？
6. **§3.1 §2:77 的改写是否仍有平面错位**？特别是"同步蓝图"被放在"以能力对用户兑现价值"之后，而 §2.2 说它由记忆承载——**是否仍属未分组的混列**？

## 6. 风险

- **风险 1（高）**：本稿新增业务系统，**属 `§4.2` 根决定清单**，必须 Human 明确同意。
- **风险 2（中）**：§2.1 写"尚未构建"可能与既有 `§3.4:140` 的"未经核验不得写成成立"被误读为冲突（本稿已在 §1 论证不冲突，请审核复核）。
- **风险 3（中）**：§2.3 补语是**新增规范文本**，须防重蹈 v3「引入不可判定边界」的覆辙。
- **风险 4（低）**：编号顺序「规划目标」在记忆之前，与行动计划 §5.2 的编号方案（33 记忆）不一致，须先统一。
