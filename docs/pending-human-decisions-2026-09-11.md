# 待 Human 决定 · 三批清单（一次性裁定用）

> **性质**：待决定清单。**本主控不自行执行**这三批——依规则它们必须由 Human 决定。
> **依据**：`specs/00-理念与构成.md` §4.2；`specs/01-规范模型基础规范.md` §9.3、§11；`docs/action-plan-2026-09-10.md`。
> **背景**：批1／批2／批3／批4／批6 已完成并提交；规范源内旧口径已清零。
> **决议登记（2026-09-13 补记）**：本清单所列问题均已由 Human 决定并落地，文件作为决定过程溯源保留——批5（H5-a～h）：30–39 业务系统段建立（`01 §8.1` 号位表镜像 00 列举序：30 调研／31 讨论／32 工作执行／33 规则遵守／34 目标与蓝图／35 记忆与反思，`1fbc862` 号段化、`6438652` 30–39 更正；H5-f 六系统各配独立规范——30 调研（`specs/30-调研系统规范.md`）、31 讨论（`specs/31-讨论系统规范.md`）已建，32–35 待建）、04 行改为「业务系统基础规范」、05 行改为「LDVH CLI规范」（`a3b3035`）、`01:190` 预留段改「业务系统规范段」；批2.5（H2.5-a～e）：04 改为**改造**而非退出——保留 04 号、`spec_key: business-system-foundation`、`supersedes: action-template-foundation`（现行 `specs/04-业务系统基础规范.md`）；批7（H7-a～c）：21 号 WorkCase 立项（`specs/21-WorkCase-工作项.md`，`d4ee39e`）；附 N-1：02 章序对齐 00 §3.1 列举序（`80e9524`）；附 N-2：dimensions 七元组表达力缺口 → Spark `bdb9623d`（open，待 Human 裁定）。Human 决定登记见 `docs/audit/human-decisions-2026-09-12-segment.md`（D-1～D-9）。

---

## 批 5 · 编号表与 30–38 归属

### 为什么必须你决定

`00 §4.2` 根决定清单明列：**「新增、删除、改名或重排规范模型、事实模型、Code 或 Web」只能由 Human 决定**。改编号 = 重排规范模型。

### 现状（三处已发现的问题）

| # | 问题 | 现状 |
|---|---|---|
| **5.1** | **04 仍在编号表** | `01:174`「04 ｜ 行动模板基础规范 ｜ 模板共同机制、预检与风险路由」，而 00 已无「行动模板」概念 |
| **5.2** | **30–38 被两处当作不同东西** | `01:190`「30–38 为**行动模板**规范预留段」；行动计划 §5.2「30 调研／31 讨论／32 执行／33 记忆／34 遵守」当**业务系统段** |
| **5.3** | **05 行标题过期** | `01:175`「05 ｜ **Helper 服务规范** ｜ Helper 确定性服务共同契约」，而 05 已改名「**LDVH CLI规范**」（`spec_key` 保留 `helper-service-contract`） |
| **5.4** | **01:172 行描述过期** | 「02 ｜ 工作模型基础规范 ｜ 能力承载、业务系统、制度…」—— 此行的「技术支撑」已在批1 改为「能力承载」，但 02 已含六系统，行描述是否需更新待定 |

### 需要你裁定

| 问 | 选项 |
|---|---|
| **H5-a** | **04 是否从编号表移除**？（与批2.5 是同一决定的两面，见下） |
| **H5-b** | **30–38 复用为业务系统段吗**？复用即意味着「行动模板规范预留段」不再存在 |
| **H5-c** | 若复用，**六系统如何编号**？行动计划拟「30 调研／31 讨论／32 执行／33 记忆／34 遵守」——**这是五系统版**，现已是六系统，须补「目标与蓝图」并定其号位 |
| **H5-d** | `01:175` 的 05 行是否更新为「**LDVH CLI规范**」？ |
| **H5-e** | **编号是否镜像 00 的列举序**（调研→讨论→工作执行→规则遵守→目标与蓝图→记忆与反思），还是按行动计划原序（调研/讨论/执行/记忆/遵守）？ |

**本主控建议**：
- **H5-a**：**是**（与 04 退出同批）
- **H5-b**：**是**（04 退出后该段自然空出，正好复用）
- **H5-c**：按 00 列举序编号：**30 调研／31 讨论／32 工作执行／33 规则遵守／34 目标与蓝图／35 记忆与反思**（六系统，镜像 00，便于对照）
- **H5-d**：**是**（标题过期属明显遗漏）
- **H5-e**：**镜像 00**（减少两处名录不一致的风险）

---

## 批 2.5 · 04 号退出

### 为什么必须你决定

`01 §9.3` 原文：**「规范退出当前规则源必须经 Human 对准确退出候选明确决定」**，且须完成「§8 登记变化、受影响引用与关系处理、受控提交」，并说明「职责去向与仍适用规则的保留、转移或退出」——**四项共同构成退出，不得由任一单项替代**。

### 现状：04 是**悬空规范**

| 事实 | 依据 |
|---|---|
| `00` 中「行动模板」**0 处** | `3b1e3e6` 自述「**删已退役的行动模板**…模板改**既有行动结构**」 |
| `00` 现用替代词 | 「**既有行动结构**」（`:180`／`:210`／`:223`） |
| `04` 自身 `basis: [ldvh-root, specification-model-foundation]` | **其上位依据 00 已无此概念** |
| 仍被引为唯一权威 | `02:62`「04 是行动模板共同结构与风险路由的**唯一权威**」 |
| 行动计划已立案 | §5.1「**04 退出**」；§4.7「01 §8.1 编号表…**删 04 行**」 |

### 需要你裁定

| 问 | 选项 |
|---|---|
| **H2.5-a** | **是否批准 04 退出**？（退出候选已就绪） |
| **H2.5-b** | **04 空号是否永久保留**？（行动计划待决清单 F） |
| **H2.5-c** | **04 的职责去向**：其「预检落实记录」「风险路由」「完成门禁」等职责，是**转移**到既有规范（如 02／09），还是**随 04 一并退出**？ |

**本主控建议**：
- **H2.5-a**：**批准**——04 的上位依据已被 00 删除，继续生效会造成「下位引一个 00 不承认的概念」
- **H2.5-b**：**永久保留空号**（`01 §9.3` 明写「保留空号不复用」已是不复用原则；且 30–38 复用为业务系统段后，04 更无复用必要）
- **H2.5-c**：**逐项甄别**——「预检落实记录」已由 `02 §13 规则遵守` 与 `04` 共同承载，须判断其是否随 04 退出或迁移（**这一项我建议单独起草分析**）

---

## 批 7 · 21 号 WorkCase 立项

### 为什么必须你决定

`01 §11` 规定规范新建须走「判断 → 独立审核 → Human 决定」。且 `01:190` 明写「预留**不等于**文件已存在、已登记或已进入当前规则源」。

### 现状：**21 号被 12 份规范引用，但规范不存在**

| 事实 | 依据 |
|---|---|
| `specs/` 有 20、22–26，**无 21**（跳号） | 文件系统实证 |
| **12 份规范引用 WorkCase** | 03／04／06／08／10／12／20／22／23／24／25／26 |
| `20:60`「21 号是 **WorkCase 授权语义**的唯一权威」 | 已把它当既有权威引用 |
| `25:56`「21 号是 **Gate 1 授权语义**的唯一权威」 | 同上 |
| `12:169`「WorkCase（21）」 | 同上 |
| `23:52`「**预留编号 21 号承载**——21 建成前创建入口只有 AI 提案与 Human 直接提出两路」 | 如实登记未建 |
| 行动计划 §5.3 | 「**21 号 WorkCase 事实类型**（`serves` 字段契约——级联的前置空洞）」 |

**另注**：批2 新增的 `02 §12 工作执行` 条文已写「以 WorkCase 承载其结论…具体契约由 **21 号类型规范**承接」——**即 00 与 02 现在都指向一个尚不存在的规范**。

### 需要你裁定

| 问 | 选项 |
|---|---|
| **H7-a** | **是否批准 21 号立项**（起草 WorkCase 事实类型规范）？ |
| **H7-b** | 若批准，**是否本批做**，还是排入后续批次？ |
| **H7-c** | 若不批准，**00／02 对 21 号的指向如何处理**？（现为悬空指向） |

**本主控建议**：
- **H7-a**：**批准立项**——「执行工单」已在 `00 §2:77` 列为功能，`02 §12` 已立为系统，**无类型规范则 WorkCase 的字段、状态与 `serves` 契约无出处**
- **H7-b**：**排在批5 之后**（编号先定，21 号地位才明确）
- **H7-c**：若暂不立项，至少须在 00／02 把「21 号」的表述改为「待建」口径，避免悬空指向

---

## 汇总：需要你回的 13 问

| 批 | 问 | 我的建议 |
|---|---|---|
| 5 | H5-a 04 移出编号表 | 是 |
| 5 | H5-b 30–38 复用为业务系统段 | 是 |
| 5 | H5-c 六系统编号方案 | 30调研／31讨论／32工作执行／33规则遵守／34目标与蓝图／35记忆与反思 |
| 5 | H5-d 05 行改为「LDVH CLI规范」 | 是 |
| 5 | H5-e 编号是否镜像 00 列举序 | 是 |
| 2.5 | H2.5-a 批准 04 退出 | 批准 |
| 2.5 | H2.5-b 04 空号永久保留 | 是 |
| 2.5 | H2.5-c 04 职责去向 | 须单独起草分析 |
| 7 | H7-a 批准 21 号立项 | 批准 |
| 7 | H7-b 是否本批 | 排批5 之后 |
| 7 | H7-c 暂不立项时的处置 | 改「待建」口径 |

（另有两项**非阻塞**待办，见下）

---

## 附：两项非阻塞待办（不影响上述三批）

| # | 事项 | 说明 |
|---|---|---|
| **N-1** | **02 章序与 00 列举序不一致** | 02 为 `9记忆与反思/10调研/11讨论/12工作执行/13规则遵守/14目标与蓝图`；00 为 `调研/讨论/工作执行/规则遵守/目标与蓝图/记忆与反思`。**HEAD 已如此**（原为 9记忆/10调研/11讨论/12遵守），批2 未改变相对次序。审核判为「既有状态，非本批缺陷」。**是否调整由你定。** |
| **N-2** | **`dimensions` 七元组表达力缺口** | `plugin/lib/spec-registry.js:24` 机械强制七维（`read/write/orchestrate/memory/research/discussion/comply`），**无法表达六系统**（无「目标与蓝图」「工作执行」维度）。改动需跨 00 身份块＋`01.Att.02`＋Code。**是否扩维由你定。** |

---

## 附二 · Human 追问引发的重新审视（2026-09-11）

**Human 问**：「04 文档不应该有一个业务系统基础规范吗？」

**本主控核后认为：该问题指向一个比「04 去留」更实质的缺口，但 04 并不是那个位子。**

### 两个必须分开的问题

| 问 | 答 |
|---|---|
| **A. 04 这个编号是否应给「业务系统基础规范」？** | **不是。** `04` 现登记为「**行动模板基础规范**」——一个 **00 已删的概念**（`3b1e3e6` 自述「删已退役的行动模板」）。它的处置是**退出**（批2.5），**不是改造成业务系统**。两者是不同性质的动作。 |
| **B. 六个业务系统是否各缺一份「基础规范」？** | **缺 4 份。** `01 §8.1` 登记表中：调研＝`11`、讨论＝`12` **已有**；**工作执行／规则遵守／目标与蓝图／记忆与反思 无编号登记**。 |

### B 项的先例与既有规划

**先例（已成立）**：`02 §10` 给出调研系统的**职责框架**（职责/判据/机制指向/边界四行），`11` 号是调研系统的**独立展开规范**（入口澄清、三层收敛、三态证据、交付合同）。**讨论**同理（`02 §11` ↔ `12` 号）。

**→ 即：业务系统规范的分工体例是「02 给框架，独立规范给展开」。**

**既有规划**：`docs/action-plan-2026-09-10.md §5.6` 已列「**30／31／33／34 业务系统规范**：调研（←11）、讨论（←12）、记忆、遵守；记忆与遵守现为 02 §9／§12 章节内容迁出」。

### 但既有规划是【五系统版】，现已过期

| 规划（§5.2／§5.6） | 现行 00 |
|---|---|
| 30 调研／31 讨论／**32 执行**／33 记忆／34 遵守 | 六系统：调研／讨论／**工作执行**／**规则遵守**／**目标与蓝图**／**记忆与反思** |
| 未含「**目标与蓝图**」 | **00 已含** |
| 命名为「执行」「遵守」「记忆」 | 现名为「工作执行」「规则遵守」「记忆与反思」 |

**→ 故 §5.2／§5.6 须按六系统重写**，且**「目标与蓝图」是新系统规范（无既有来源可迁）、「工作执行」亦为新立**。

### 由此对批 5 / 批 7 的修正

**批 5（编号表）新增待决项**：

| 问 | 我的建议 |
|---|---|
| **H5-f**：**六系统是否各配一份独立规范**（30–35 段），还是维持「02 给框架、部分系统另有规范」的现状？ | **各配一份**——00 已把六系统定为**承诺**，而承诺若无独立承接，`02 §9–§14` 的四行框架不足以承载执行、遵守、目标与蓝图、记忆与反思的完整契约 |
| **H5-g**：既有规划 §5.6 的**五系统名目**是否按六系统重写？ | **是**——须补「目标与蓝图」「工作执行」的独立规范；「工作执行」依赖 21 号 WorkCase（与批7 联动） |

**批 7（21 号立项）的联动性增强**：

`02 §12 工作执行` 已写「以 WorkCase 承载其结论…具体契约由 **21 号类型规范**承接」。若 21 号不立项，则**「工作执行」系统的基础规范亦无类型承载**——**批5 与批7 因此相关联**，宜一并裁定。

### 本主控的修正建议（汇总）

1. **批2.5**：04 退出（其编号不复用）——**不变**
2. **批5**：编号表按六系统重写；**30 调研／31 讨论／32 工作执行／33 规则遵守／34 目标与蓝图／35 记忆与反思**；并**新增 4 份业务系统基础规范的立项**（工作执行、规则遵守、目标与蓝图、记忆与反思）
3. **批7**：21 号 WorkCase 与「工作执行」系统规范**同批考虑**

**注**：上述 2、3 均属 `00 §4.2` 根决定事项（新增规范模型）与 `01 §11` 新建判断，**须 Human 决定后方可执行**。

---

## 附三 · Human 二次追问：04 应改造为「业务系统基础规范」（本主控答错，此处更正）

**Human 说**：「业务基础规范是规范 30-38 这个区段业务规范的书写和基础，就像事实基础规范一样」

**本主控核后确认：Human 的判断成立，我此前的「04 退出」建议需修正。**

### 决定性依据一：`01 §7.2:135` 的对称要求

> 「基础模型规范还必须定义其下属规范共同结构、必填内容和扩展规则：**03 定义 `20–24` 事实类型规范共同结构；04 定义 `30–38` 行动模板规范共同结构**。」

**核验**：
- **03 是【事实侧】基础规范** → 管 `20–24`（现扩至 26）—— 实证：03 正文明写「本文只定义事实公共模型和类型共同结构」「20–24 起草时至少分别回答：类型目的、对象边界、字段与结构…」
- **按对称，【业务系统侧】也该有一份基础规范** → 管 `30–38` 各业务系统规范怎么写
- **04 现占着这个位子**，但它写的是「行动模板」—— **00 已删该概念（职责空了，位置还在）**

### 决定性依据二：`00:223` 已把权交给业务系统规范

> 「V4 稳定推进：…判据：执行复用**既有行动结构**并记录复用依据，**其判据由相应业务系统规范定义**。」

**→ 00 自己已把「既有行动结构」的判据权交给【业务系统规范】。而「既有行动结构」正是 `3b1e3e6` 用来替代「行动模板」的术语。**

**→ 即：00 的逻辑链是「行动模板已退役 → 既有行动结构 → 其判据由业务系统规范定义」。业务系统规范＝30–38 段。**

### 应用 `01 §11.3:300` 的规则判定

| 规则 | 应用于 04 |
|---|---|
| 「同一职责改名、改号或移动时保留原 key，不使用 `supersedes`」 | 不适用——不是改名 |
| 「**实质新职责替代旧职责时使用新 key**，并通过 `supersedes` 与正文处置说明建立可追溯关系」 | **适用**——管的对象从「行动模板」变为「业务系统」，属实质新职责 |

**→ 故 04 的正确处置是【改造】，不是【退出】**：

| 项 | 值 |
|---|---|
| 编号 | **保留 `04`**（不浪费号位，与 03 对称） |
| `spec_key` | **更新为新 key**（如 `business-system-foundation`） |
| `supersedes` | **指向 `action-template-foundation`** |
| 职责 | 定义 `30–38` **业务系统规范**的共同结构、必填内容和扩展规则 |
| 标题 | 「**业务系统基础规范**」 |

### 对既有决定的更正

| 批次 | 原建议 | **更正后** |
|---|---|---|
| **批2.5** | 04 **退出** | **04 改造为业务系统基础规范**（保留 04、新 key、`supersedes` 旧职责） |
| **批5** | 编号表**删 04 行** | 编号表 **04 行改为「业务系统基础规范｜业务系统规范共同结构、必填内容与扩展规则」** |
| **批5** | 30–38 复用为业务系统段 | **不变**——且 `01:190` 的「行动模板规范预留段」须改为「**业务系统规范预留段**」 |

### 新增待决项

| 问 | 我的建议 |
|---|---|
| **H2.5-d**：04 是【改造】还是【退出＋另立】？ | **改造**（编号保留、新 key、`supersedes`）—— 与 03 对称，且不浪费号位；若另立，30–38 段已被业务系统规范占满，基础规范无处放 |
| **H2.5-e**：新 `spec_key` 取何名？ | 建议 `business-system-foundation` |
| **H5-h**：`01:190`「30–38 为行动模板规范预留段」是否改为「业务系统规范预留段」？ | **是** |

**注**：`01 §11.3` 要求实质新职责用新 key + `supersedes`，**这仍属 `00 §4.2` 根决定事项与 `01 §11` 新建判断，须 Human 决定后方可执行。**
