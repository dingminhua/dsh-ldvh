# 独立对抗审核报告：提交 `25871e1`

- **审核对象**：`25871e1a23f5f7248ff579348b4b41c677bad934`（dev 分支，工作区干净）
- **提交主题**：`refactor(specs): 规范正文移出状态与审计台账，改为条件式要求`
- **触发依据**：`01 §12.4` + `01 §12.1`——本提交改变 6 份规范正文中「状态类表述」的实质（改写为条件式要求 / 移出为无规范效力文档），属产生或改变可强制执行规则的实质修改，须经独立对抗审核。
- **审核者身份**：隔离会话 AI（未参与本候选的形成；形成本候选的同一上下文未参与本次审核，满足 `01 §12.2` 独立性要求）。
- **审核方法**：`git show` 全量比对 + 6 份规范相关章节通读 + `docs/audit/` 三份新文件通读 + `01` §7.3/§9.1/§9.2/§12 + `00` §4/§6.3/§7 核读 + 机械核验（grep、parseSpecDocument 全量、node --test）。
- **审核范围**：A 义务是否丢失 / B 移出完整性 / C 改写自洽性 / D 上位一致性 / E 测试与解析。

---

## 一、A. 是否丢失了原本存在的义务

### A1. 改前是状态、改后是要求的处（是否凭空创造约束）
逐处核查，新增「不得声称…」均**非凭空创造**，而是把原状态段里隐含的「如实声明 / 不得伪装」义务显式化为条件式禁令：

| 处 | 改前隐含义务 | 改后显式化 | 是否创造 | 依据 |
|---|---|---|---|---|
| `01 §8.2` | 隐含「尚未校验本表，依赖人工核对」 | 「机械覆盖前须以人工核对代替且**不得声称**已通过机械校验；覆盖后须以机械结果为准**不得**以人工冒充机械证明」 | 否，原义务显式化 | `specs/01`:220→改后行；`git show` diff |
| `21 §10.4#4` | 已有「**不得表述为**已由 08/09 承接」 | 保留「**不得表述为**已由 08/09 承接」 | 否，保留 | `specs/21`:219 改后 |
| `27 §11` | 隐含「由 §6.2 查重 + Human Gate 承担」 | 保留并加「**且不得声称**该约束已获机械保证」 | 否，且增强 | `specs/27`:199 |
| `27 §11 边界` | 已有「**不得表述为**已获完整机械保障」 | 保留并加「**任何情形下均不得**表述为已获完整机械保障」 | 否，且增强 | `specs/27`:214 |
| `30 §5` | 隐含「本节设立正是为约束该偏移」 | 「当核查发现…**须按本节约束调整**…**不得**以已产出为由延续偏移」 | 否，立法意图转为要求 | `specs/30`:70 |

**结论**：所有新增禁止均可在改前状态段中找到对应意图（如实声明 / 不得伪装），未凭空创设无上位依据的约束。

### A2. 改前是状态、改后完全删除的处（删掉内容是否隐含某条义务）
- `04 §9.3` 首次套用 7 项表与逐条复核结论：纯审计台账（历史事实 + 复核结论），**不含规范性义务**。`docs/audit/04-section9-3-first-application.md` 完整承载（§二七行表 + §三统计 + §四方法学）。原「已修的项不因本表标注而取得『无缺陷』结论」属提醒性文字，非强制义务，已随台账移入非规范文件。→ **无隐含义务被删**。
- `08 §7.6` 更正记录段：纯历史更正说明。规范正文保留更正后的正确映射（「经 `system-prompt/assemble` 注入的 `ldvh:minimal-guidance` 段，文本源为 `plugin/lib/guidance-text.js`」）。→ **无义务被删**。
- `04 §10` gaps 表（原 2 行缺口表）：状态表，其附带的「本表不得因上述缺口被改写为『已验证』」禁令已在改后条件式要求中以「未建齐期间不得声称已完备」承接（见 A3 比对）。→ **状态删除，义务经改写保留**。

### A3. 改前有要求、改后被改写弱化的处
逐条比对强制力（完整表见第六节），**未发现强制力下降**。反有 3 处增强（`01 §8.2`、`27 §11`、`27 §11 边界` 新增/加严「不得声称」）。**唯一注意点**：`27 §14` 原「缺口闭合后应回填对应行并移除」这一**表维护义务**，在改写后随表删除而从规范正文消失，仅在 `docs/audit/implementation-status-registry.md §五` 以非规范语气「应更新本文件」保留——见第五节问题 P2（存疑/次要）。

**A 结论**：未发现任何改前存在、改后丢失的规范性义务；状态删除与条件式改写总体保留并显式化了原有「如实声明 / 不得伪装」义务，部分增强。

---

## 二、B. 移出内容的完整性

### B1. `docs/audit/` 三份文件是否完整承载移出信息
- `docs/audit/04-section9-3-first-application.md`：含 §一首次套用（30/31，2026-09-12）、§二 7 项问题表（问题/复核结论/说明，与原 `04 §10` 被删表 7 行逐条对应）、§三统计（3 不成立 / 4 成立 / 全修）、§四方法学。→ **完整**。
- `docs/audit/08-protected-doc-mapping-correction.md`：含事由 / 核验发现 / 更正内容 / 更正性质与边界 / 处置链，覆盖原 `08 §7.6` 被删段落全部要素。→ **完整**。
- `docs/audit/implementation-status-registry.md`：含 5 节（01 §8.2 / 21 §15.2 / 27 §11 / 04 §10 / 方法说明），承载从 01/21/27/04 移出的实现状态。→ **完整**。

### B2. 是否误移（把要求当状态移走）
逐处判断，被移出者均为「现状是 X」「本轮审了什么」「原文写错已于某日更正」类状态/历史，无要求被误移。→ **无误移**。

### B3. 规范中是否残留指向移出内容的悬空引用
- `specs/21-WorkCase-工作项.md:73`：「见 §10.4 第 4 项与 **§15 缺口登记**」——本次改后 §15 已无「缺口登记」子节（原 §15.3 未实现范围表 → 现 §15.2 前提未满足时的声明限制（条件式））。该引用**悬空**。→ **见 P1（已证实）**。
- 其余规范（04/21/27/30/01/08）指向 `docs/audit/` 的引用均指向真实存在的目录/文件，无悬空。
- 复现命令：`grep -rn '缺口登记' specs/*.md` 仅命中 `21:73`；`grep -rn '见 §10\|见 §15' specs/21-*.md` 命中 `21:73`（悬空）与 `21:285`（合法，指向 §10.4 第 4 项，仍存在）。

**B 结论**：移出内容完整、无误移；仅 `21:73` 一处悬空引用（P1）。

---

## 三、C. 改写后的自洽性

### C1. `21 §15` 子节编号连贯性
改后 `21 §15` 结构：`15.1 类型特有验证` → `15.2 前提未满足时的声明限制（条件式）` → `15.3 存量对象迁移` → `15.4 类型退出`。编号连续，无跳号/重号。原 `15.2 存量对象迁移`、`15.3 类型退出` 顺延为 `15.3`/`15.4`。
- 复现：`grep -n '^### 15\.' specs/21-*.md` → `268/279/291/295` 对应 15.1/15.2/15.3/15.4，连续。
- 正文内部是否有指向旧号引用？`grep -n '^### 15\.\|见 §15' specs/21-*.md`：除 `21:73`（见第五节 P1）外，`21:285/40/44/66` 中的「§15」均指向 `02 §15` 复核制度或 `00 §7.2` 等上位，非本文旧 §15.3，无错引。→ **连贯，无内部错引**（外部 `21:73` 除外，归 P1）。

### C2. `04 §10` 七列验证表合规性（`01 §7.3`）
`04 §10` 验证表头为「验证对象 | 验证时机 | 成立条件 | 可接受依据 | 验证入口 | 可证明范围 | 未满足时的处理」七列，顺序与 `01 §7.3:160` 固定七列表头一致；末尾有「机械结果与语义判断互不替代…本章不能单独证明…」声明句。`04 §10` 新增的「未验证范围的处置要求（条件式）」为**段落**，位于验证表之后、Human Gate 之前，未破坏验证章结构（验证章允许「验证表 + 末尾声明」，本条件式段属对未验证范围的要求性收尾，未插入重复验证行）。→ **合规，结构未破坏**。

### C3. `30 §5` 立法意图保留
改后 `30 §5`：「偏移约束（条件式）：调研对象须服务于项目自身规范规划需求；当核查发现某批主要服务于『LDVH 该吸收什么』而非自身需求时，须按本节约束调整…不得以已产出为由延续偏移。该偏移的实测记录（作为设立本节的依据）见 `ldvh-base/researches/...`」。
- 改前「本节的设立正是为约束该偏移」的立法意图以「须按本节约束调整…不得延续偏移」承接；
- 实测记录指针（原 19/20 证据文件）保留为「设立本节的依据」证据指针。
→ **立法意图保留，且更可操作**。

**C 结论**：编号连贯、验证章合规、立法意图保留。

---

## 四、D. 与上位的一致性

### D1. 是否符合 `01 §7.3`（验证章只允许验证表 + 末尾声明）
`04 §10`、`21 §16`、`27 §14` 验证表均为规范七列表头 + 末尾「互不替代」声明；新增条件式段位于验证表之后、作为对未验证范围/未实现范围的要求性收尾，未新增重复验证行、未插入空表。→ **符合**。

### D2（关键合规问题）：`01 §9.2:313` 的 gaps 要求是否仍有落地载体
`01 §9.2:313`：「无法证明某项成立条件时，应把该项和受影响范围记录为 `gaps`，不得伪装为通过。」

**核验**：提交后全仓 `grep -rn 'gaps' specs/*.md` 显示，gaps 的规范承载仍存在且未消失：
1. **验证章七列表头 + 末尾「保留为 gaps」声明**：`04 §10`/`27 §14`/`21 §16` 等末尾均有「未实现与不可用范围保留为 gaps」（如 `specs/27:261`、`specs/04:358`、`specs/21` 验证章末句）。这是 §9.2「记录为 gaps」的文本落地载体。
2. **条件式要求显式禁止伪装**：`04 §10`「未建齐期间不得声称已完备」、`21 §15.2`「不得声称已承接/已获保障」、`27 §11`「不得声称已获机械保证」、`27 §14`「不得声称已获机械拦截/专属拒绝路径」、`01 §8.2`「不得声称已通过机械校验」——这些正是 §9.2「不得伪装为通过」的可执行化。
3. **具名 gaps 仍在规范中可查**：04 的 32–35 未建 / 迁移未启动、21 的 attempt 宿主实现 / 34 号 / serves 命名、27 的三层 / 受控写入器、01 §8.2 机械校验 —— 全部在条件式要求文本中具名出现，未丢失。
4. **`docs/audit/implementation-status-registry.md`** 以非规范形式汇总状态，作为补充记载（无规范效力，符合 `01 §9.1` 候选路径仅为 `specs/`、`specs/attachments/`）。

**结论**：§9.2:313 的「记录为 gaps，不得伪装为通过」**未失去落地载体**。承载形态从「专用如实登记 gaps 表」转为「验证章 gap 保留声明 + 条件式禁止伪装要求 + 具名条件」，反而更可执行。无回归，D2 **合规**。

### D3. `docs/audit/` 定位与 `01 §9.1` 一致性
三份 `docs/audit/*` 均在文首声明「无规范效力」「候选路径仅为 `specs/` 与 `specs/attachments/`（`01 §9.1`）」，与 `01 §9.1:281-284` 一致。`docs/` 不在候选路径，不会混入当前规则源。→ **一致**。

**D 结论**：D1/D2/D3 均合规；`01 §9.2` 的 gaps 要求仍有落地载体（验证章 + 条件式要求），无合规断点。

---

## 五、问题清单（分级）

### P1 — 已证实（中）：`specs/21-WorkCase-工作项.md:73` 悬空引用「§15 缺口登记」
- **现象**：`21 §3.3` 写道「（**该承接尚未实装**，见 §10.4 第 4 项与 §15 缺口登记）」。本次提交将原 `21 §15.3 未实现范围（gaps，如实登记）`改写为 `§15.2 前提未满足时的声明限制（条件式）`，§15 内已无名为「缺口登记」的子节。
- **影响**：读者按引用找不到「§15 缺口登记」；状态登记现位于 `docs/audit/implementation-status-registry.md §二`，但引用未更新指向。属引用与正文不一致（`01 §9.2` 第 10 项「所有受影响引用与实际文件一致」）。
- **复现**：`grep -n '缺口登记' specs/21-*.md` → `73`；`grep -n '^### 15\.' specs/21-*.md` 显示无「缺口登记」子节。
- **修复建议**（供父代理/主控）：将 `21:73` 的「§15 缺口登记」改为「§15.2 前提未满足时的声明限制（条件式）」或「`docs/audit/implementation-status-registry.md §二`」。

### P2 — 存疑 / 次要（低）：`27 §14` 表维护义务随表删除而降为非规范
- **现象**：原 `27 §14` 末尾「缺口闭合后应回填对应行并移除」为规范正文内的表维护义务；改写后 §14 仅留条件式要求（「不得声称已获机械拦截 / 专属拒绝路径」），该维护义务仅在 `docs/audit/implementation-status-registry.md §五`（非规范）以「应更新本文件」保留。
- **影响**：实现补齐后「更新 gaps 记录」的义务从规范性降为非规范性。其余 spec（21 §15.2）保留了「依赖闭合后应删除对应行」的规范表述，而 27 未保留对应规范句，二者不对称。
- **依据**：`specs/27-Norm-事实规范.md:262`（改后 §14 末句无闭合维护义务）；对比 `specs/21-WorkCase-工作项.md:290`（保留「依赖闭合后应删除对应行」）。
- **结论**：非义务丢失（声明限制本身保留），但维护性义务降级，且与 21 不对称。建议要么在 27 §14 补一句规范级「实现闭合后本条条件式要求应相应调整」，要么接受其仅由 `docs/audit` 非规范承载（符合本提交整体意图）。

### 已排除项
- **E1 解析回归**：`parseSpecDocument` 对 6 份改动规范（01/04/08/21/27/30）全量通过，0 错误。
- **E2 测试回归**：`cd plugin && node --test test/*.test.mjs` 失败的 5 个用例（adr-tools / friction-tools / pitfall-tools / research-tools / spark-tools）与本次提交**无关**——提交仅改动 `specs/` 与 `docs/audit/`，未触碰 `plugin/lib/` 或 `test/`；这些用例导入的是 `plugin/lib/*-tools.js` 等写入器，且 `research-tools.test.mjs` 仅在第 3 行注释提及 `specs/30`，不解析改动规范。3 个解析错误（20/24/25）亦在被改文件之外，属预存问题。→ **测试失败非本提交引入，已排除为本次审核范围外**。
- **A 义务丢失**：逐处比对（见第六节）未发现任何改前存在、改后丢失的规范性义务。
- **B 误移 / 内容丢失**：三份 audit 文件完整承载移出内容，无误移。
- **D2 gaps 载体缺失**：已确认仍有落地载体（见第四节 D2）。

---

## 六、逐处义务强度比对表（改前 vs 改后）

| # | 文件·位置 | 改前性质 | 改前义务 | 改后性质 | 改后义务 | 强度判定 | 依据 |
|---|---|---|---|---|---|---|---|
| 1 | `04 §10` gaps 表 | 状态表 + 台账 | 「本表不得因缺口被改写为『已验证』」(禁止) | 条件式要求 + 指针 | 「未建齐期间不得声称完备/六系统已对齐；未建立编号不得当已存在/已登记/已进入规则源；迁移期间以 02 为唯一来源」 | **保留并增强**（状态转可执行禁止） | `git show` diff；`specs/04:360` |
| 2 | `04 §9.3` 7 项表 | 审计台账（历史） | 无 | 移出 `docs/audit/` | 无（非规范） | **无义务，移出** | `docs/audit/04-section9-3-first-application.md` |
| 3 | `08 §7.6` 更正记录 | 历史更正 | 无 | 移出 `docs/audit/`；正文留正确映射 | 无（非规范） | **无义务，移出** | `specs/08:151`；`docs/audit/08-*.md` |
| 4 | `01 §8.2` 机械校验现状 | 状态声明 | 隐含「依赖人工核对」 | 条件式要求 | 「机械覆盖前以人工核对代替且不得声称已通过机械校验；覆盖后以机械结果为准不得以人工冒充机械证明」 | **增强**（新增显式禁止） | `specs/01:220→改后` |
| 5 | `21 §10.4#4` attempt 宿主 | 状态 + 禁止 | 「不得表述为已由 08/09 承接」 | 条件式要求 | 保留「不得表述为已由 08/09 承接」+ 指针 | **保留** | `specs/21:219` |
| 6 | `21 §15.3→15.2` 未实现范围 | 状态表 + 禁止 + 维护 | 「不得因缺口改写为已承接/已保障；缺口闭合后回填行并移除」 | 条件式表 + 禁止 + 维护 | 「不得声称已承接/已获保障；依赖闭合后应删除对应行」 | **保留**（表删故「移除行」改「删除行」） | `specs/21:279-290` |
| 7 | `27 §11` 三层契约 | 状态 + 要求 | 「由 §6.2 查重 + Human Gate 承担」(要求) | 条件式要求 | 保留「须由 §6.2 查重 + Human Gate 承担」+「不得声称已获机械保证」 | **增强** | `specs/27:199` |
| 8 | `27 §11` 边界 | 状态 + 禁止 | 「不得表述为已获完整机械保障」 | 条件式要求 | 保留 +「任何情形下均不得表述为已获完整机械保障」 | **增强** | `specs/27:214` |
| 9 | `27 §14` gaps 表 | 状态表 + 禁止 + 维护 | 「不得因缺口改写为已保障；缺口闭合后回填行并移除」 | 条件式要求（无维护句） | 「不得声称已获机械拦截 / 专属拒绝路径」；维护义务仅非规范(`docs/audit §五`) | **保留声明限制；维护义务降级**(见 P2) | `specs/27:262`；`docs/audit/implementation-status-registry.md §五` |
| 10 | `30 §5` 偏移记录 | 状态(19/20) + 立法意图 | 隐含「本节为约束偏移」 | 条件式要求 + 证据指针 | 「须服务于自身需求；当核查发现偏移须按本节约束调整，不得延续偏移」 | **保留并增强**（意图转要求） | `specs/30:70` |

---

## 七、`01 §9.2` 的 gaps 要求是否仍有落地载体（D2 专答）

**仍有落地载体，无合规断点。** 承载形态：
1. 验证章末尾「未实现与不可用范围保留为 gaps」声明（`01 §7.3` 要求的验证表收尾）——文本落地；
2. 各规范条件式要求中显式「不得声称已保障 / 已验证 / 已通过机械校验」——§9.2「不得伪装为通过」的可执行化；
3. 具名 gaps（32–35、迁移、attempt 宿主、34 号、serves 命名、三层、受控写入器、01 §8.2 机械校验）仍在规范条件式文本中可查，未丢失；
4. `docs/audit/implementation-status-registry.md` 非规范汇总（符合 `01 §9.1`）。

---

## 八、已排除项（汇总）
- 解析回归（6 份改动规范 parseSpecDocument 全过）。
- 测试回归（5 失败 + 3 解析错误均在被改文件之外，提交未触碰 `plugin/lib/`/`test/`，属预存）。
- 任何规范性义务丢失（逐处比对无丢失）。
- 误移 / 移出内容丢失（三份 audit 文件完整）。
- D2 gaps 载体缺失（已确认存在）。

---

## 九、未覆盖范围与保证边界
- 本审核未重新判定各规范 §9.2 成立条件本身（如 Human 决定、独立审核、提交契约）是否满足——仅核本提交未破坏其落地。
- 未对 `docs/audit/` 三份文件的事实准确性（如「19/20」「attempt 在 08/09 零命中」「ERR_DIRECTION_ALREADY_EXISTS 无实现」）做独立事实核验；这些属被移出状态的内容，其真实性由来源提交（`530202b` 后链、`903aa30`、`2379c8c`、`7d5ba4c` 等）承载，不在本次实质修改审核的义务范围内。
- 未运行超出 `parseSpecDocument` 全量 + `node --test` 的其它 CI（如 lint / eslint），因提交未改动代码。
- 5 个失败测试与 3 个解析错误虽已判定为预存、与本提交无关，但其根因未深挖（超出本次审核义务边界）；如需可另立排查。

---

## 十、独立性披露
- 审核执行体为隔离会话 AI，未参与 `25871e1` 候选的形成（候选由 workbuddy/deepseek-v4.1-flash 形成，本审核在不同上下文执行），符合 `01 §12.2`。
- 本审核未修改任何文件、未 git commit、未修正任何发现，仅产出本报告 `docs/review-25871e1.md`。
- 发现的 P1（21:73 悬空引用）由本审核独立发现，非候选形成方自陈。
- 模型同族（deepseek）下已按 `01 §12.2` 保持上下文隔离并披露。

---

## 十一、总体结论

**有条件可确认（确认 + 1 项须修）**。

- 实质修改的正当性（状态移出 / 条件式改写）符合 Human 原则与 `01 §12.4` 触发，且**未丢失任何规范性义务**、**未误移要求**、**gaps 落地载体保留（D2 合规）**、**验证章与编号自洽**、**解析与测试无回归**。
- **须修项（P1）**：`specs/21-WorkCase-工作项.md:73` 的「§15 缺口登记」悬空引用，应在本轮或紧随的受控提交中改为指向 `§15.2` 或 `docs/audit/implementation-status-registry.md §二`，以满足 `01 §9.2` 第 10 项「引用与实际文件一致」。
- **建议项（P2，非阻断）**：`27 §14` 的表维护义务降级为非规范，建议与 21 §15.2 对齐补一句规范级闭合调整要求，或明确接受其仅由 `docs/audit` 非规范承载。

在不修复 P1 前，本提交**不构成可单独立即确认**（存在引用不一致）；P1 修复后结论上升为**改动正确可确认**。
