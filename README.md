# LDVH v5

> 状态：**骨架占位（初稿）**。本仓库为 LDVH 第 5 版（v5）重构仓库，尚处起步阶段，内容将随新 00 逐章定稿逐步补全。
>
> 上下文与既定决策见 [`docs/v5-handoff.md`](docs/v5-handoff.md)。

## v5 定位

- **取代 v4**：v5 是新规则与实现源；v4（`ld-vibe-harness-v4`）整体降级为只读历史档案，不再新建事实、不再作为规则源。
- **DSH 原生插件分发**：以 DeepSeek Harness（DSH）原生插件形态分发（`dsh plugin add ldvh`），同时保持开源。
- **开源**：MIT License（见仓库根 [LICENSE](LICENSE)），**与 DSH 本体同协议**；可进入 DSH 插件市场，接受开源公共契约。开源的本意是让其他人去做移植（DSH 之外平台的移植由社区完成），同协议降低移植门槛。

## 版本号双轨

v5 重新起算，对外发布版本与开发迭代版本分离（决策 D7）：

**当前版本：`1.0.0-dev.1`**（主版本 `1.0.0` 全新起算；对外 `1.0.0` 尚未发布，首次正式发布时才封存）。

| 轨 | 语法 | 递增时机 | 示例 |
|---|---|---|---|
| **对外发布版本（release）** | 纯 SemVer `MAJOR.MINOR.PATCH` | 仅正式发布时 | `1.0.0` → `1.1.0` |
| **开发迭代版本（dev）** | 同主版本 + `-dev.<n>` | 仅开发迭代时 | `1.0.0-dev.1` → `1.0.0-dev.42` |

规则：

- 二者共享 `MAJOR.MINOR.PATCH`，开发版本永远带 `-dev.N` 后缀，正式版本为干净三段式。
- **对外版本**只被发布动作推进，**开发版本**只被开发迭代推进，互不污染。
- 发布时封存干净版本（如 `1.0.0`），之后 dev 进入下一轮（`1.1.0-dev.1`）。
- 凭 `-dev` 后缀即可区分「开发中的版本」与「给用户的版本」。

## 当前里程碑（§6 起步顺序）

1. ~~**骨架**~~：`.gitignore` + `README.md` 占位 ✅
2. ~~起草新 00~~ ✅：`specs/00-理念与构成.md` 六章（§1–§6）全部定稿（多轮独立对抗复核），身份块经 01 校准回填（去 status、加 dimensions、code_consumption 4 键）
3. ~~**登记管辖**~~ ✅：独立配置 `../LDVH-GOVERNED-PROJECTS.yaml`（dsh-ldvh 受辖，governed_single）
4. 内核验证（Helper CLI + Git Gate 在新仓库跑通真实任务）
5. 选择性搬运（ADR / Pitfall / Spark / Study 受控重建）
6. DSH 绑定（插件五件套 + 记忆预热 + 队长式执行）
